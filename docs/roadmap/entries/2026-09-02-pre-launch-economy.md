- 🟢 **L'infra pré-lancement cesse de payer la posture du jour J : ~90 → ~15–22 USD/mois (infra, 2026-09-02).** Décision propriétaire après lecture de la projection GCP. L'anatomie, dérivée des ressources déployées : le pod prod toujours-allumé (`minScale: 1` — l'app + le sidecar SQL-proxy, facturés 24/7) pesait ~2/3 de la facture, les deux `db-f1-micro` l'essentiel du reste. Avec zéro utilisateur public, les seuls appelants sont le propriétaire, les sondes CI et bientôt les reviewers des stores — tous capables d'attendre un démarrage à froid. **Deux boutons, chacun réversible et VERROUILLÉ au retour** : `minScale '1' → '0'` (LAUNCH.md §6.5 est la porte qui inverse AVANT d'annoncer — une case de checklist, pas une mémoire ; `pool_sizing_test` garde délibérément son arithmétique à la valeur de lancement, 0 ne fait qu'affaiblir la borne) ; et la base staging DORT entre les répétitions (`activation-policy NEVER` + les deux crons Scheduler en pause — ~100 réveils/jour qui n'auraient trouvé qu'une base arrêtée ; la routine start/stop dans DEPLOYMENT.md, le boot-smoke d'un déploiement staging échouant contre une base arrêtée). **Vérifié sur l'artefact, pas sur le manifeste** : la révision servie `myweli-api-00033-f6g` porte `minScale: 0` avec le digest répété en staging ; base staging `STOPPED`, crons `PAUSED` ; `/health` à 0,54 s à chaud. **La borne tarifaire tient sans connaître le mode de facturation antérieur** : après le changement, Cloud Run ne facture que les secondes de traitement (~50k vCPU-s/mois au trafic actuel, sous le palier gratuit de 180k), donc ≈ 0–2 USD quel que fût le tarif idle supprimé — reste prod SQL (~11–14) + stockage staging (~2) + divers (~2–6). Surprise mesurable : les sondes uptime (5 min) gardent une instance chaude en tarif requête — les vrais démarrages à froid seront rares. Confirmation empirique : Billing → Reports d'ici 3–4 jours.
