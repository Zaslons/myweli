- 🟢 **« Définir comme photo principale » — promouvoir la photo n°6 ne coûte plus cinq flèches et cinq écritures (app + web, 2026-08-27).** Dernier point du lot de curation 2026-08-26. La couverture EST `imageUrls[0]` sur toutes les surfaces (cartes, détail, Hero, image OG — et les mots du contrat : « Order is preserved: imageUrls[0] is the listing cover ») ; le seul chemin de promotion était la flèche voisine, n gestes = n PUT wholesale. Spec ([gallery-set-cover.md](../../design/gallery-set-cover.md)), zéro changement serveur. **App** : une étoile en haut-gauche des vignettes non-couverture — le seul coin libre (✕ en haut-droite, flèches en bas, badge en bas-gauche de la couverture seule) — même idiome 48px/pastille/`Semantics` que le ✕ ; `setCover(i)` = `removeAt` + `insert(0)` + **UN PUT**, dérivé de la liste COURANTE au moment de l'appel (la règle `restorePhotoAt` : jamais une liste capturée — testé en enchaînant suppression puis promotion). Pas de confirm (réversible, non destructif), pas de snackbar succès (le badge « Couverture » saute sur la vignette même), mais l'échec parle — contrairement aux flèches qui jettent leur futur. **Web** : `toCover` pur à côté de `moveItem`/`removeAt`, ★ `IconBtn` à côté de ↑↓✕ sur les vignettes non-couverture, staged comme tout l'onglet, « Enregistrer » persiste. Pas d'étoile sur la couverture — un contrôle mort portant une promesse. Aucun golden (aucun n'existe pour ces écrans — resté léger, noté). Suites : 1450 app · 739 web, vertes ; mutations regardées rouges contre le travail commité.
