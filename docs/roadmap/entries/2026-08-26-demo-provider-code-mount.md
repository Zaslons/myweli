- 🟢 **Le code du compte de démonstration, frappé et monté (infra, 2026-08-26).** `DEMO_PROVIDER_CODE` créé en Secret Manager — version 1, forme `^\d{6}$` vérifiée **sans jamais imprimer la valeur** (elle se lit par `gcloud secrets versions access`), accès accordé aux deux comptes de service d'exécution. Monté **épinglé** (`key: '1'`) dans les deux manifestes : partagé avec le staging **délibérément** — la valeur est publique par conception (elle ira dans les notes de revue des deux stores), le staging est la répétition de la curation du salon démo (§9 de la spec), et un seul code dans deux environnements est un seul code dans les notes. Ce que le secret achète n'est pas la confidentialité mais **l'interrupteur d'arrêt** (non défini → seam absent) et la surveillance du moniteur d'épinglage. **Les gardes des manifestes ont tiré exactement comme conçues** : la liste des exceptions délibérées de `service_files_test.dart` et le registre du script de provisionnement ont tous deux refusé le nouveau partage jusqu'à ce qu'il soit inscrit avec sa raison — quatre rouges, puis 59 verts. `98-verify-secret-pins.sh` : chaque version épinglée activée et courante.
