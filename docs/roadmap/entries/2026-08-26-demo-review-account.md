- 🟢 **Le compte de démonstration pour la revue des stores — spécifié puis construit (backend, 2026-08-26).** Le plus gros morceau du chemin « Pro d'abord » : l'app Pro s'ouvre sur `/pro/login`, un relecteur sans identifiants ne voit rien, et Play exige des identifiants **réutilisables qui contournent les codes à usage unique**. **Le renversement qui structure tout : l'identifiant est imprimé dans les notes de revue des deux consoles — public par conception.** Donc on ne conçoit pas la serrure, on conçoit **la pièce** : `revue@myweli.test` en **constante de compilation** (RFC 2606 — structurellement immailable, jamais une vraie personne, aucune variable d'environnement ne peut l'élargir : l'argument T60 promu) + un code fixe à 6 chiffres en Secret Manager (`DEMO_PROVIDER_CODE` absent ou mal formé → le seam est **absent**, l'interrupteur d'arrêt ; avertissement au démarrage quand actif en prod). **Zéro changement d'app, zéro nouveau endpoint** : la connexion frappe et consomme immédiatement un **vrai** enregistrement OTP (familles de sessions, rotation, révocation — le chemin de production, pas un chemin parallèle), le créneau de renvoi remboursé — l'usage documenté de `refundEmailOtpResend`. **Deux capacités soustraites, aucune ajoutée** : publier → 403 `demo_account_locked` (le salon démo ne peut jamais entrer dans l'ensemble public ni déclencher la reconstruction — et la route habillait chaque erreur inconnue en 409 `incomplete`, corrigé) et inviter → 403 (la seule surface qui écrit à un tiers arbitraire depuis une session authentifiée). L'enveloppe budgétaire refuse tout destinataire `.test` **avant** la réservation. **La remise à zéro automatique de 7 jours** (décision du propriétaire) : un instantané (migration 0036, une ligne `CHECK (id=1)`) capturé via `POST /admin/demo/snapshot` — cible dérivée de la constante, **jamais d'une saisie** — puis un tick porté par le cron quotidien des abonnements (la règle « la troisième tâche extrait un cron de maintenance » du commentaire de route est amendée sur place : le tick est fenêtré en interne, l'extraire coûterait plus qu'il ne rapporte). La remise à zéro restaure le document (défiguration annulée ; `id`/`slug`/`status` jamais réécrits), purge agenda + fichier clients (`wipeForProvider`, avertissement un-seul-appelant dans la doc), **régénère quatre réservations relatives à maintenant** via le vrai `bookManual` (un agenda vivant pour toujours, pas septembre en janvier), étend `paidUntil` et efface les avis (jamais de bannière expirée, aucune tâche d'opérateur, aucun cas démo dans la facturation), et **refuse bruyamment** un salon non-démo — la condition de sécurité dans le code, pas dans la tête de l'opérateur. **Huit mutations regardées rouges — et deux leçons de harnais.** La première passe tournait en zsh inline : `$FILES` non cité **ne se découpe pas** (déjà dans le livre des leçons), la fonction de restauration n'a restauré silencieusement rien et quatre mutations se sont empilées dans l'arbre **non commité** — réparées à la main, et la règle nouvelle : **jamais de passe de mutation sur du travail non commité**. Ensuite M6 (refus déplacé APRÈS la réservation) a survécu : le plafond de 2 rendait l'assertion « rien consommé » vacueuse — resserré à 1, mutation revue rouge. backend : **1360 tests** (+23) · analyze 0 · format propre · types web régénérés.
