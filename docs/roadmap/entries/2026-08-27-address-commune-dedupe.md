- 🟢 **« marcory, Rue des Nguéssous 93, Marcory » — la commune ne se répète plus, à l'écran ni dans les données structurées (web + app, 2026-08-27).** Vu par le propriétaire sur sa propre page. Deux causes qui se composent : la donnée (la commune tapée dans le champ Adresse, en casse de slug, en écho au sélecteur juste en dessous) ET le code — trois sites d'affichage ajoutaient `, {commune}` sans condition : la ligne Localisation, la réponse FAQ (qui part dans le JSON-LD `FAQPage`, citable par les IA — elle doublait DEUX fois) et `streetAddress` du `PostalAddress` (une vraie erreur de données structurées : `addressLocality` porte déjà la commune). **Dédoublonnage par segment, jamais par sous-chaîne** (`lib/address.ts`, insensible casse/accents) : « Rue de Marcory » à Marcory mérite toujours l'ajout — seule une adresse dont un segment EST la commune la mentionne déjà ; côté `streetAddress`, seuls les segments de tête et de queue sont retirés (sur-retirer perd de la donnée, sous-retirer ne fait que répéter). **Hygiène de saisie en amont** : « Rue et numéro — la commune est choisie ci-dessous. » sous le champ Adresse, web ET app, même français au caractère près (la moitié app atteint les téléphones au prochain envoi Play). Le test de câblage rend `ProviderView` en entier — une première — et affirme UNE mention. Sept mutations regardées rouges. Vérifié sur la page déployée après merge.
