- 🟢 **La page publique d'un salon affichait « 2024-01-01T09:00:00.000Z – … » comme horaires — et le JSON-LD n'avait jamais eu d'`openingHoursSpecification` (web, 2026-08-27).** Capturé par le propriétaire sur sa propre page. `Hours.tsx` interpolait `startTime` brut ; le parseur sûr existait depuis A14d (`timeOfDay`, regex, jamais `new Date()` — l'instant est un porteur, le fuseau du navigateur ou du serveur de build déplacerait l'heure d'ouverture). Il vit désormais dans `lib/time-of-day.ts` **neutre** (la page SEO publique n'a pas à importer le module de l'éditeur pro ; ré-exporté, zéro churn) et la section honore enfin `isAvailable` comme l'app — un créneau indisponible n'est pas une heure d'ouverture, un jour sans créneau disponible lit « Fermé ». **Pourquoi 750 tests verts n'ont rien vu** : le stub e2e et les fixtures utilisaient `'09:00'` nu, jamais le format du fil — le piège exact que `pro-availability.test.ts` documentait déjà pour l'éditeur, corrigé là et jamais revisité côté public. Stub et fixture passent au vrai format (+ un jour scindé, + un créneau indisponible), et l'assertion e2e discrimine dans les deux sens : l'heure formatée visible ET aucun résidu ISO sur la page. **Le trou de spec fermé dans la foulée** : `openingHoursSpecification` (web-m3-provider-page.md la demandait depuis le début) — une entrée par plage DISPONIBLE, `dayOfWeek` schema.org, `opens`/`closes` en HH:mm nus via le même parseur, clé omise sans disponibilité. Huit mutations regardées rouges. Vérifié sur la page déployée après merge.
