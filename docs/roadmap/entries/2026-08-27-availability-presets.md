- 🟢 **Les horaires cessent d'être une saisie de sept jours à la main : trois modèles, « Copier sur les autres jours », et la phrase qui explique enfin les créneaux (app + web, 2026-08-27).** Retour de curation du propriétaire : « pourquoi ne pas leur donner des exemples à choisir directement ? » et « les créneaux, ça se crée où ? » — nulle part, ils sont dérivés, et la section Horaires était la SEULE des quatre sans phrase explicative. Spec d'abord ([availability-presets.md](../../design/availability-presets.md)), zéro changement serveur — le `PUT` wholesale existant porte tout. **Trois modèles** (« Mar–Sam · 9h–18h », « Lun–Sam · 8h–17h », « Tous les jours · 9h–19h ») en chips sous le titre, **chaînes identiques app/web pinnées par un test de parité** qui lit la source Dart depuis la suite web (le mécanisme `dart-tokens`, en plus léger) — libellés, comptes ET heures croisés. **Sélection honnête** : une chip ne s'allume que si la semaine EST exactement le modèle ; éditer un jour éteint tout — et appliquer écrit les jours hors modèle VIDES (un « Mar–Sam » qui garderait un vieux dimanche mentirait sous une chip allumée). App : remplacement d'horaires configurés passe un confirm, semaine vierge s'applique direct (pas de dialogue à vide) ; web : staged, « Enregistrer » persiste. **« Copier sur les autres jours »** : app — visible seulement sur un jour AVEC créneaux (copier « fermé » sur la semaine est un piège), confirm puis UN PUT des sept jours multi-plages comprises ; web — `onCopyToAll` sur les lignes de `DayHoursEditor`, salon ET artistes (même composant), pauses exclues (parité : l'app n'y copie pas), et les plages supplémentaires par jour survivent (`slice(1)`, le modèle une-plage-par-jour honoré). **La ligne créneaux** — « Vos créneaux de réservation sont calculés automatiquement à partir de ces horaires et de la durée de chaque prestation. » — sous les deux titres, même français au caractère près. Golden `pro_availability_w360` régénéré sur Linux (attendu — le harnais A14d a été écrit « pour que cette slice ait un avant »). Suites : 1455 app · 749 web, vertes ; mutations regardées rouges contre le travail commité.
