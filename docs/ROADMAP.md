# Myweli — Current State & Build Plan

**Companion to [PRD.md](PRD.md).** This document establishes where the codebase actually is today, the strategy for building it out (frontend-first, with guardrails), the step-by-step sequence, and how we test, secure, and keep it fast at every step.

| | |
|---|---|
| **Last updated** | 2026-06-20 |
| **Build strategy** | Finish the **V1 frontend** on mock services → design & build backend → integrate → harden |
| **Module map** | [MODULES.md](MODULES.md) — canonical modules (YCLIENTS-replica map); every entry/spec/PR names its module; **full-depth rule**: each module ships complete (every flow/state/detail, best UI/UX) |
| **Non-negotiables** | Security and performance are first-order — continuous gates, not a final phase |
| **Reference device** | Low-end Android (2–3 GB RAM, Android 9) — the perf/UX bar |

---

## Part 1 — Current state (ground truth)

Established by reading the code and running `flutter analyze` on 2026-06-20.

### 1.1 Toolchain & repository
| Item | State | Action |
|---|---|---|
| Flutter SDK | ✅ Installed (`~/development/flutter`) | Pin version in `.fvmrc`/CI |
| App compiles | ✅ `flutter analyze` → **0 errors** | — |
| Lint health | ⚠️ **230 issues** (deprecations + correctness warnings) | Clean to zero (Phase 0) |
| Version control | ❌ **Not a git repo** | **`git init` today** — highest priority |
| CI/CD | ❌ None (`.github` absent) | Add in Phase 0 |
| Tests | ❌ **1 smoke test** only (`test/widget_test.dart`) | Build test harness (Phase 0) |
| Lint config | ⚠️ Default `flutter_lints`, no strictness | Tighten (Phase 0) |
| Secrets/env | ⚠️ No env strategy yet (fine while mock-only) | Define before any key touches the app |

### 1.2 Notable code-health signals from `flutter analyze`
- ~Dozens of `deprecated_member_use` (`withOpacity` → `.withValues`) — SDK drift; mechanical fix.
- **Correctness warnings** (not just style): `unnecessary_null_comparison` and `unnecessary_non_null_assertion` in `lib/widgets/booking/appointment_card.dart` — these usually mean a model's nullability changed and a widget wasn't updated. **Treat as latent bugs, not noise.**
- `unused_import` / `unused_local_variable` (e.g., `mock_pro_artist_service.dart:52`) — dead code to prune.

### 1.3 What's DONE (frontend, on mock data)
Estimates of V1-frontend completeness by area (UI built & wired to mock services):

| Area | Done | Notes |
|---|---|---|
| **Consumer auth** (phone/OTP, splash, session) | ~99% | Real-OTP UX + session persistence + SMS auto-read (OS autofill hint) done. Remaining: real access/refresh tokens + Android SMS Retriever (backend). |
| **Consumer discovery** (home, list, detail, map, search) | ~80% | Strong. Missing: commune filter, price *ranges*, WhatsApp link, verified badge, before/after gallery. |
| **Booking flow** (hub, services, artist, date/time, confirm) | ~90% | Best part of the app (`booking_hub_screen.dart`). Duration-by-length + buffers now respected in slots. Missing: deposit step, rebook polish. |
| **Consumer appointments** (list, detail, cancel, reschedule, history) | ~95% | Done: policy-bound cancel + reschedule; rich visit history with auto-synced status. Remaining: deeper history (photos/receipts) post-backend. |
| **Reviews** (submit post-completion) | ~70% | Missing: photos, verified-booking badge, per-artist attribution. |
| **Favorites** (map + toggle) | ~80% | Solid. |
| **Profile** (view/edit) | ~95% | Account deletion (typed confirm) + data export (JSON copy) + avatar upload (mock pipeline) done. |
| **Notifications (consumer)** | ~10% | **Stub** (`EmptyState`). No feed, no center. |
| **Pro auth + register + KYC + onboarding** | ~95% | KYC + verification status done; guided onboarding checklist done (photos step now actionable). Salon photos + staff avatar upload on a mock pipeline. Missing: real bytes/CDN (backend). |
| **Pro dashboard** | ~70% | Stats wired to mock. |
| **Pro appointments / calendar** | ~95% | Accept/reject/complete/reschedule + no-show marking + manual (walk-in/phone) booking entry. |
| **Pro services / artists / availability** | ~95% | Price ranges + duration variants on services; buffer between appointments; per-staff working hours; break times. Missing: commission (V2). |
| **Pro earnings / reviews / profile** | ~70% | Basic earnings + reviews + profile. (Payouts retired — no-custody model; commission tracking is V2.) |
| **Pro "feature" screens (the 8)** | Gated off | Not routed **and** gated behind `FeatureFlags.futureProviderFeatures` (false) → each renders a "Bientôt disponible" placeholder. Deferred (V2 loyalty/memberships; V3 the rest). |

### 1.4 What's MOCKED (works, but simulated)
All data flows through `lib/services/mock/*` behind `lib/services/interfaces/*`. Simulated: OTP (`123456`), provider/appointment/favorites data, slot generation (30-min, ~90% availability), review eligibility, booking lifecycle. **This is the asset that makes frontend-first viable** (see Part 2).

### 1.5 What's MISSING entirely (no code yet)
> ⚠️ **Original snapshot (project start) — superseded by §1.8.** Most of this is now built (backend, payments-no-custody, notifications WhatsApp/SMS/FCM, image upload, KYC, the admin console). Kept for history; read **§1.8** for the current audit.

- **Backend / API** (everything is mock).
- **Payments / Mobile Money deposits** — the core CI value prop. Not started.
- **Notifications**: push (FCM), SMS, WhatsApp — not started.
- **Image upload pipeline** (provider/artist photos are static assets).
- **KYC/verification flow** (data field exists, no UI/logic).
- **Web** (all 4 surfaces): per-provider pages, marketplace, provider dashboard, admin console.
- **Retention features**: cross-salon loyalty wallet, memberships/abonnements, gift certificates.
- **Security & perf infrastructure**: no CI scanning, no obfuscation config, no perf budgets/profiling.

### 1.6 Honest grade
**A well-architected, ~75%-complete V1 *consumer* frontend prototype, on mocks, with no backend, no payments, no tests, and no version control.** The architecture (interface→mock, Provider state, go_router, DI) is genuinely good and was clearly built to allow a clean backend swap. The risk isn't the UI — it's that everything that makes Myweli *defensible in CI* (deposits, WhatsApp, KYC, performance under real data) is exactly what hasn't been started.

### 1.7 Do this week (before any feature work)
1. **`git init`** + first commit + `.gitignore` review + push to a private remote.
2. Stand up **CI** (analyze + test on every push).
3. **Clear the 230 analyze issues to zero** and fix the `appointment_card.dart` correctness warnings.
4. Add the **test harness + first real tests** (Part 4).

### 1.8 V1 completeness audit — current ground truth (2026-06-27)
Requirement-by-requirement pass over every PRD `[V1]` FR vs the codebase. **109 PRs merged · 224 backend + 296 mobile tests · analyze 0.** §1.6/§1.7 above are the original-start snapshot; this is the live state.

**✅ Built (verified).** Consumer: auth + **account deletion/data export** (AUTH-001..005), discovery incl. commune + sort + available-today (DISC-001..007), booking + rebook (BOOK-001..009), no-custody deposit (PAY-001..005), appointments incl. policy engine + visit history (APPT-001..005, 009), reviews + moderation (REV-001..005), favorites (FAV-001), notifications WhatsApp/SMS + FCM + **in-app center** (NOTIF-001/002), before/after showcase (DISC-006). Pro: auth, KYC, onboarding, profile, media + before/after, services, staff, availability, day-view, accept/reject, manual booking, complete/cancel, no-show, deposit settings, earnings, dashboard, review view, reminders (PRO-*). Admin console: KYC, mgmt + support, disputes, moderation, featured, analytics, audit, RBAC (WEB-AD-001/003..008).

**⚠️ Open V1 gaps (not built / partial) — do not lose these:**
> The four small mobile gaps from this audit are now **all closed** (APPT-006, NOTIF-004, PRO-SUB-001, APPT-008 — see the 🟢 entries below). The only remaining **feature** gap is the public web surface.
- ✅ **Web surface — feature-complete (M0–M8, 2026-06-28); deploys at the accounts phase.** **OQ-8 resolved → Next.js (React/TS) for ALL web** (public SEO pages + consumer web + provider dashboard) on the **shared `dart_frog` API**; mobile stays Flutter; admin stays Flutter Web. Chosen on **UX** (cold-mobile-web first paint + a desktop-grade pro tool) and optimised for **SEO + AEO + GEO**. DTOs generated from `openapi.yaml`; design tokens shared. Milestones: **M0 foundations ✅** (WEB.md + WEB-DESIGN-STANDARDS + `myweli-web-guardrails` skill) · **M1 backend glue ✅** (slug/migration `0021` + `by-slug` + `sitemap/providers` + CORS; threat model **T27**) · **M2 Next scaffold + SEO foundation ✅** (`web/` Next.js App-Router/TS/Tailwind app: typed OpenAPI client + CI drift check, shared tokens, layout + brand `Organization` JSON-LD, robots/sitemap/`llms.txt`, install banner, isolated `web` CI job) · **M3 provider page ✅** (`app/[slug]` SSG/ISR + `LocalBusiness`/`FAQPage`/`Review`/`Breadcrumb` JSON-LD + all sections + install push; 14 web tests) · **M3.1 ✅** (hermetic Playwright e2e — blocking `web-e2e` — + Lighthouse CWV/SEO budgets — report-only `web-lighthouse`) · **M4 SEO landing ✅** (`/(categorie)-(commune)` via the `app/[slug]` dispatcher — provider-first→landing→404; provider cards + internal links + `ItemList`/`Breadcrumb`/`FAQPage` JSON-LD; empty→noindex; sitemap; 18 unit + 4 e2e; category×commune) · **M4.1 service landing ✅** (`/(service)-(commune)` e.g. `/tresses-cocody` — dispatcher's 3rd arm; curated service taxonomy matched **client-side** to provider service names, no backend change; 22 unit + 6 e2e) · **M5 web booking ✅** (`/(slug)/reserver` stepper: service→staff→slot→**OTP**→no-custody deposit; **BFF route handlers** `/api/*` + **httpOnly-cookie** session, no tokens in JS, no backend change; provider « Réserver » → funnel; 25 unit + 7 e2e incl. full funnel) · **M6 consumer account ✅** (`/connexion` phone/OTP + `/mon-compte` profile/my-bookings tabs + `/mon-compte/[id]` detail + **cancel**; BFF **silent refresh** `callApi`→`/auth/refresh` long-lived sessions + logout + backend `GET /me` (threat **T28**); 32 unit + 9 e2e; reschedule/edit/reviews/favorites/delete deferred to the app) · **M7 pro dashboard — building** (**M7.0 ✅** backend `GET /me/provider` (threat **T29**) + **pro BFF** `/api/pro/*` separate `myweli_pro_*` cookies + `callApiPro` silent refresh via `/auth/provider/refresh` + `/pro/connexion` provider-OTP + `/pro` sidebar shell + **Aujourd'hui** today's bookings/counts + logout; login-only, registration stays in app; 4 unit + 2 e2e + backend 6 · **M7.1 ✅** « Rendez-vous » mirroring the app's `/pro/appointments` — **Calendrier** (month grid → day list) **+ Liste** (Aujourd'hui/À venir/En attente/Tous); shared `ProAppointmentRow`; client-side filter, no backend change; 5 unit + 1 e2e · **M7.2 ✅** booking detail `/pro/rendez-vous/[id]` + lifecycle **Accepter/Refuser/Terminé/Absent** (mirrors `pro_appointment_detail`; detail derived from the provider list since `GET /appointments/{id}` is consumer-scoped; deposit justificatif; **no provider-cancel**; status-string fix noShow/« Absent »); 3 unit + 1 e2e · **M7.3 — building** (split): **7.3a ✅** Catalogue : Services (`/pro/catalogue` list + inline create/edit/delete; list reuses `GET /me/provider`; mutations via pro BFF with backend-enforced ownership; 6 unit + 1 e2e) · **7.3b ✅** Catalogue : Équipe (Services|Équipe tabs; artistes list + inline CRUD; per-staff hours deferred; 2 unit + 1 e2e) · **7.3c ✅** Disponibilités (`/pro/disponibilites` weekly hours + tampon + dates bloquées; load reuses `GET /me/provider`, save via pro-BFF PUT; multi-slot/pause round-tripped; 3 unit + 1 e2e) · **7.3d ✅** Abonnement (`/pro/abonnement` read-only PRO-SUB) + **revenue cards on `/pro`** (consume `GET /providers/{id}/dashboard` — **closes parity gap G3**); 4 unit + 1 e2e · **7.3e — building** (split): **7.3e-i ✅** **editable** Profil (new backend **`PATCH /providers/{id}`**, owner-only field allowlist, threat **T30**) + Acompte (deposit policy form) + home « Configurer mon profil » nudge; backend +2 tests, web +8 unit + 1 e2e · **7.3e-ii ✅** Médias (`/pro/medias` Photos grid reorder/remove + **full browser upload** via `/uploads/sign`→R2 + Avant/Après pairs; 5 unit + 1 e2e)) — **M7 COMPLETE: the pro dashboard reaches parity with the pro app (web-adapted).** **M8 consumer parity pass — building** (web-own design: desktop-creative + responsive, Planity-informed, brand identity; memory `web-design-latitude`): **M8.1 ✅** discovery home `/` + `/recherche` (hero service+commune search → existing landings/`/recherche`; category tiles; Salons populaires; **"Partout à Abidjan"** commune×category directory → SEO landings; value props; FAQ; WebSite+SearchAction & FAQPage JSON-LD; on-brand SVG hero; no backend change; 12 unit + 3 e2e; **closes G1**) · **M8.2 ✅** provider page extras — Avant/Après + Galerie + interactive **OpenStreetMap** embed + **two-column desktop layout + sticky booking panel** (mobile fixed bottom bar); no backend change; 3 unit + 1 e2e; **closes G2** ([web-m8-2-provider.md](design/web-m8-2-provider.md)) · **M8.3 ✅** account extras — **rebook** + **laisser un avis** (completed bookings) + **favoris** (account section + provider-page heart); no backend change; 3 unit + 3 e2e; **closes G4** ([web-m8-3-account.md](design/web-m8-3-account.md))) — **M8 COMPLETE.** **→ The entire web epic (M0–M8) is feature-complete; web (public SEO · consumer · pro) at flow-parity with both apps; parity audit G1–G4 all closed.** See [web-parity-audit.md](design/web-parity-audit.md). Spec in [docs/design/public-web.md](design/public-web.md). Buildable + CI in-repo now; **deployed in the accounts phase** — runbook: [docs/DEPLOYMENT.md](DEPLOYMENT.md) (domain `myweli.com`; stack — **Render** (backend + Postgres; GCP rejected prepaid cards) · Vercel · Cloudflare R2 · Twilio · FCM; backend `Dockerfile` + `render.yaml` Blueprint). **No-account deployment-readiness done:** backend `Dockerfile`, app push seam (#2/#2b), Android scaffold (#3), web `next/image`+OG/brand (#4 — [web-images-og-brand.md](design/web-images-og-brand.md)). **Accounts phase underway:** Twilio SMS configured (sender `Myweli`); backend moving to Render via `render.yaml`. The PRD's "single biggest organic-acquisition channel."
- 🟢 **Parity-fix P2b — review photos end-to-end + « Signaler » (audit 2.13/2.14, 2026-07-11).** Deeper than audited: review photos could not be submitted against the REAL backend from ANY surface — `/uploads/sign` had no consumer purpose (the app signed `gallery` under the provider session). **(1) Backend** — `purpose=review`: consumer-only, PUBLIC bucket, key `review/{userId}` server-derived, images ≤5 MB, publicUrl returned; role gate deposit/review→user; contract + threat row T13 updated; 3 new sign tests. **(2) App** — `ApiImageUploadService` gains purpose/refreshPath knobs; a consumer-session DI instance now powers `uploadReviewPhoto`; « Signaler » on review tiles (opt-in `onReport` — pro Avis stays read-only) → dialog (raison optionnelle) → `POST /reviews/{id}/report` via the new `reportReview` seam (interface+mock+API). **(3) Web** — ReviewForm attaches ≤3 photos (sign→direct-to-storage→publicUrl, thumbnails+remove); the public ReviewList renders photo strips + a fullscreen lightbox AND « Signaler » (inline reason; anonymous → « Connectez-vous » with returnTo). Spec: docs/design/reviews-photos-reporting.md. analyze 0 · **348 backend** (+3) · **399 mobile** (+3) · **177 web unit** (+2) · **51 e2e** (+1 journey, photo attach + both Signaler states asserted).
- 🟢 **Parity-fix P2a — web search + booking-funnel parity (audit 2.1/2.2/2.10/2.11/3.2, 2026-07-11).** **(1) « Trier »** on /recherche — the app's ProviderSort (Pertinence/Mieux notés/Prix croissant) as a select riding `?sort=` (SSR refetch; default now relevance like the app — the page had hardcoded rating). **(2) « Disponible aujourd'hui »** — toggle pill → `?dispo=1` on the existing `availableToday` param; all controls preserve each other. **(3) Booking notes** — « Notes (optionnel) » on the confirm step, forwarded by the booking BFF (salons already see notes in the journal). **(4) Mobile sticky bar** — under `lg` the hub gets the app's pinned Total + « Confirmer » bottom bar (desktop aside unchanged). **(5) Duration variants** — the web service form gains « Durée selon la longueur de cheveux » (Court/Moyen/Long minutes; toggle-off clears, exactly the app's payload semantics — the web hub already RENDERED variants, salons just couldn't author them). **No backend change** (sort/availableToday/notes/durationVariants all pre-existed in the contract). Spec: docs/design/web-search-funnel-parity.md. tsc/lint/build clean · **175 web unit** (+3 variant round-trips) · **50 e2e** (+2: Trier/dispo journey · mobile pinned-bar journey; notes + variants asserted in the existing journeys).
- 🟢 **Parity-fix P1c — the last P1 web surfaces: notifications + « Revenus » (audit 5.1/5.2/9.1 + 9.2, 2026-07-11).** **(1) Notification center (5.1)** — `/mon-compte/notifications` mirrors the app's screen: type-glyph cards (unread = bold + dot), tap = mark-read + follow the deep link (`webRouteFor`: `/bookings` → `/mon-compte`), « Tout lire »; entry points = header bell (ONE fetch on mount, unread dot, anonymous 401 → nothing) + an account row. **(2) Préférences (5.2)** — the app's three toggles (rappels / offres / push) as a block on the same page, optimistic with revert + « toujours envoyés » note. **(3) « Revenus » (9.1)** — `/pro/revenus` mirrors earnings_screen: Aujourd'hui/Semaine (lundi)/Mois/Tout tabs → realized total + transaction ledger; sidebar entry. **(4) Week card (9.2, P3 folded in)** — « Revenus cette semaine » on the pro home (`weekRevenue` was in the DTO all along). Five new BFF routes; **no backend change** (all four endpoints already served the app). Spec: docs/design/web-notifications-revenus.md. tsc/lint/build clean · **172 web unit** (+9: webRouteFor/unreadCount + periodRange) · **48 e2e** (+2 journeys). **ALL P1 PARITY FINDINGS NOW FIXED** — 14 of 42; next: P2 funnel/UX batches.
- 🟢 **Parity-fix P1b — reschedule on web, both sides (audit 1.1/1.9, 2026-07-11).** **(1) Consumer « Reporter »** — mon-compte detail (pending/confirmed + future, the app's rule via `canReschedule`): date input + slot chips prefilled from the booking (provider/services/durée/artist through `fetchSlots`) → new BFF `POST /api/appointments/{id}/reschedule` → « Rendez-vous reporté ✓ »; 409 → « Ce créneau vient d'être pris ». **(2) Pro « Reprogrammer » (cross-day)** — date + heure editor on BOTH the pro detail page and the journal panel (the app's 365-day flow; drag stays the single-day fast path); 409 → « Créneau indisponible ». Backend untouched (the role-aware reschedule endpoint existed; web simply never called it). tsc/lint/build clean · **163 web unit** (+1 canReschedule) · **46 e2e** (consumer Reporter journey + pro 409→success journey).
- 🟢 **Parity-fix P1a — the capability trio: the capacity engine's inputs are finally settable (audit 3.1/3.4/3.8, 2026-07-10).** **(1) `artistIds`** — « Qui peut réaliser ce service ? » checkbox list (« Aucune sélection = toute l'équipe ») on BOTH service forms (app `ServiceFormScreen` + web catalogue): the booking hub's capability dimming and the per-artist capacity pool now work for REAL salons, not just seed data. **(2) Web per-staff hours** — the Équipe form gains « Horaires personnalisés (sinon : les horaires du salon) » with a weekly editor ({} = inherit — the engine's `_hoursCover` rule). **(3) Web breaks** — Disponibilités gains « Pauses » (une pause récurrente par jour, ex. déjeuner) riding the same PUT (blocks slots + hatches the journal grid). One shared `DayHoursEditor` + generic `scheduleToDays`/`daysToSchedule` (extra-slot round-trip preserved) now powers salon hours, pauses and artist hours. Backend untouched (it accepted all three all along). analyze 0 · **396 mobile** · **162 web unit** (+4) · **46 e2e** (capability block + pause save asserts).
- 🟢 **Parity-fix P0b — web account deletion + data export (+ name edit) (audit 11.1/11.2/11.3, 2026-07-10).** The legal-grade web gaps closed, mirroring the app's flows: mon-compte gains a **« Confidentialité »** section — **« Supprimer mon compte »** with the app's type-SUPPRIMER confirmation and consequence copy (BFF `DELETE /me` → T48 CRM anonymization server-side → session cookies cleared → home) and **« Exporter mes données »** → `/mon-compte/donnees` (profil + rendez-vous + favoris assembled client-side into the SAME JSON shape as the app's `data_export.dart`; Télécharger (JSON) + Copier). The display **name is now editable** inline (PATCH /me already accepted it). **→ P0 complete.** tsc/lint/build · **158 web unit** (+2 export shape) · **46 e2e** (+1 full Confidentialité journey incl. the disabled-until-SUPPRIMER gate).
- 🟢 **Parity-fix P0a — deposits are now truly KYC-gated + the « Vérifié » badge exists (audit 8.1/15.1, threat T52, 2026-07-10).** The audit's two trust headliners fixed end-to-end: **(1)** `PUT /providers/{id}/deposit-policy` with `depositRequired: true` now requires a VERIFIED account → 403 `verification_required` (was copy-only — an unverified salon could demand screenshot deposits); both editors lock the toggle with « Les acomptes sont disponibles après la vérification… » + a link to Vérification, and surface the server error. **(2)** Admin KYC approve/reject now **denormalizes `verified` onto the public listing** (contract field + app `Provider` model + regenerated web types) and the badge finally renders: app list card + detail header, web result card + salon-page hero (`✔ Vérifié`); seeds demo-verified; drafts start unverified. Threat **T52**. analyze 0 · **348 backend** (+2: gate + denormalize/flip) · **396 mobile** (badge model + locked-editor widget test) · **156 web unit** (+1 badge) · 45 e2e (+hero badge assert).
- 🟢 **Salon profile editing in the pro app L2 (docs/design/pro-salon-lifecycle.md §2c, 2026-07-10).** The pro app had NO listing editor at all (web-only since 7.3e-i) — an app-only owner couldn't even satisfy the publish gate. Now: **« Profil du salon »** (`/pro/salon-profile`, from Profil + the onboarding checklist) edits every allowlisted field + **category dropdown** + the **pin map** (flutter_map on the app's CARTO basemap — tap to place, « Utiliser ma position » via geolocator); `updateSalonProfile` through the interface/mock/API seam (PATCH). **The app's go-live checklist is now the exact server-gate mirror**: `location` step added; `photos` REQUIRED (the stale « pipeline pending » optionality removed); `deposit` demoted to recommended; `profileComplete` computed from the LISTING (description+address+commune) instead of the always-true businessName — « Mettre mon profil en ligne » can no longer disagree with the server. analyze 0 · **396 mobile tests** (+4: location/photos/deposit gate mirror + salon-profile provider load/save/error). **→ The lifecycle program (B1…B5, L1, L2) is complete.**
- 🟢 **Salon map pin L1 — salons can finally place themselves on the map (docs/design/pro-salon-lifecycle.md §2c, 2026-07-10).** **User find:** nothing could EVER set a salon's coordinates (the PATCH allowlist had no latitude/longitude) — no real salon would ever appear on the /recherche map or get a pin on its own page. Now: **`latitude`/`longitude` join the editable allowlist** (validated as a PAIR, sane ranges) + **`category` becomes editable** (canonical enum — was frozen at registration); the **publish gate gains `location`** (no pin → no go-live; contract `missing` enum updated); web Profil gets the **pin picker** on the shared MapLibre/Positron map (tap to place · drag to adjust · « Utiliser ma position ») + a category dropdown; the go-live checklist shows « Position sur la carte ». Audit leftovers parked: app profile editing (L2, next) · `logoUrl` never settable (minor). analyze 0 · **346 backend** (+1 pin/category validation) · **155 web unit** (+1) · 45 e2e. 
- 🟢 **Pro salon lifecycle B1 — registration creates the salon; « publier » gates visibility (docs/design/pro-salon-lifecycle.md, 2026-07-10).** **Production bug found by the user:** a freshly registered web pro saw « Une erreur est survenue. Réessayez. » on EVERY dashboard page — root cause: `register` created the ACCOUNT only; **no code path anywhere created a salon record** (salons existed solely from seed), so `GET /me/provider` 403'd on `providerId == null`. (Not the reused Google — that would have 409'd; the app had the same latent hole; the web e2e stub masked it with a pre-linked account.) Now: **`createSalon`** (both repos — seed-shaped doc, empty catalogue, unique slug, **`status: 'draft'`**) + **`SalonProvisioningService.ensureSalon`** as the ONE create+link path used by `register` (all three identities) AND by **`GET /me/provider` as a SELF-HEAL** — pre-fix stuck accounts (incl. the user's) get their draft salon on first read, no manual repair. **Go-live** (user sign-off: « visible only after set up, with pictures and location and everything »): `POST /providers/{id}/publish` — owner-only (T50), server-recomputed gate mirroring the app's onboarding thresholds (profile description+address+commune · **≥3 services** · **≥3 photos** · ≥1 open weekday) → 409 `incomplete`+`missing[]` or `draft`→`active`, idempotent; KYC deliberately NOT required to list (badge + deposits stay verified-gated). **Drafts are invisible everywhere public** (T51): discovery/landings/sitemap exclude them, `by-slug` 404s them, booking refuses them like suspended. Contract (`/publish` + register note) + regenerated web types + **T50/T51**. analyze 0 · **345 backend tests** (+9: provisioning at register, the /me/provider heal, publish gate/authz/idempotency, draft hiding, PG createSalon/linkProvider round-trip). **B2 web done (same day):** the pro home shows the **draft banner + go-live checklist** (the four gate items with live counts, each deep-linking to its page — Profil/Catalogue/Médias/Disponibilités) + **« Mettre en ligne »** (enabled only when complete; 409 → « Vérifiez la liste ») → 🎉 « Votre salon est en ligne ! »; `status` added to the Provider contract; pure `publishChecklist` mirror in `lib/pro/onboarding.ts` (server stays authoritative); stub pro salon starts as a complete draft. tsc/lint/build · **154 unit** (+2) · **45 e2e** (+1, order-proof against the shared stateful stub). **Next: B3 app goLive wiring.**
- 🟢 **Consumer web discovery map — the /recherche split view (docs/design/web-discovery-map.md, 2026-07-10; layout signed off in chat).** The app's map tab, desktop-shaped, **no backend change**: `/recherche` becomes **results list left + STICKY map right** (Planity/Airbnb pattern) with **two-way sync** — hovering a card enlarges its marker, clicking a marker opens a **popup mini-card** (name · ★ · commune · « à partir de » · Voir/Réserver) and scrolls + rings the matching card; **category chips** (« Tous » + the canonical taxonomy) re-query with the `category` param; **« Autour de moi »** geolocation (denial → the app's copy); auto-fit to result bounds, Abidjan default (the app's center). **Mobile web = the app experience**: list + a floating **« Carte »/« Liste »** toggle to a full-height map. Tech: **Leaflet + react-leaflet@4** **dynamically imported `ssr:false`** — the chunk loads only on `/recherche` (noindex), public-page CWV untouched; salons without coords stay list-only; favorites-hearts-on-markers deferred (session on a public page). **Design revision (same day, user):** the map is **FULL-BLEED** — no framed box; flush to the viewport edges, `sticky top-0 h-screen` (Planity feel), search+chips moved into the left column — and carries the **app MapScreen's exact design**: the **CARTO light basemap** (the app's tile layer + attribution), **`_SalonMarker` replicas** (44px white circle · 2px category ring · category icon, §7 tokens via `currentColor`; hover/selection scales via a class toggle on the marker element — no remount, popups survive; Leaflet owns the root transform so scale sits on the inner pin), the app's **info-blue user dot** on « Autour de moi », and a `ResizeObserver`→`invalidateSize` fix so the mobile toggle's display:none mount renders tiles (found in browser preview). **Renderer upgrade (same day, user):** **Leaflet retired for MapLibre GL** (`maplibre-gl` + `@vis.gl/react-maplibre`) with the **CARTO Positron vector style** — the open-source version of Planity's Woosmap/OpenMapTiles stack, still keyless/free, same design language as the app's raster basemap but crisper with smooth zoom; markers become plain React elements (focusable buttons, §7 tokens); the popup sets `closeOnClick={false}` (the selecting click bubbles to the map after React mounts the popup and would close it in the same gesture — a race found via a step-by-step probe); MapLibre observes container size natively (the manual invalidate fix retired). tsc/lint/build · 152 unit · 44 e2e still green. **Consistency follow-up (same day, user):** the salon page's Localisation section drops its OSM iframe for the **same MapLibre + Positron + salon-pin identity** (shared `components/map/salon-pin.tsx`), **lazily mounted on approach** (IntersectionObserver + `cooperativeGestures`) so the indexed page's CWV budget never pays for the below-the-fold map; `osmEmbedUrl` retired; provider e2e aborts the tile host (hermetic). Pure helpers in `lib/discovery/map.ts`. tsc/lint/build · **152 unit** (+5) · **44 e2e** (+2, tile requests aborted = hermetic). **→ The web parity list is CLEARED — web has full flow-parity with both apps.**
- 🟢 **Web pro KYC — « Vérification » on the pro dashboard (docs/design/web-pro-kyc.md, 2026-07-10).** The pro app's `ProKycScreen`, web-adapted, **no backend change** (`GET/POST /me/kyc` + `POST /uploads/sign?purpose=kyc` already existed) — a web-born salon (web-pro-registration) can finally get verified without the app: Profil gains a **« Vérification » row with the live status hint** (from the already-loaded `account.verificationStatus`) → `/pro/verification` with the app's **status banner** (pending/vérifié/refusée + `rejectionReason`) over the **four document tiles** (Pièce d'identité · Photo du visage · RCCM · Justificatif d'adresse; « (optionnel) » per the `businessType` rules — RCCM waived for `other`/à domicile, unknown stays conservative) — upload at add time (presign → **direct-to-private-storage** POST, only `{fileName, key}` kept client-side, NFR-SEC-002 posture) → « Soumettre pour vérification » posts the full list (resubmit clears a rejection); verified = read-only. The pro-BFF sign route's purpose is now **allowlisted** (`gallery`|`kyc`; `deposit` stays consumer-only). Pure rules in `lib/pro/kyc.ts` + `uploadKycDocument`. tsc/lint/build · **147 unit** (+7) · **42 e2e** (+1: profil → tiles → 2 uploads → submitted). **Parity follow-ups remaining: consumer web discovery map (last one).**
- 🟢 **Web pro reviews — « Avis » on the pro dashboard (docs/design/web-pro-reviews.md, 2026-07-10).** The pro app's ReviewsScreen, web-adapted, **no backend change** (reads the public paginated `GET /providers/{id}/reviews` behind the pro session): sidebar « Avis » → `/pro/avis` with the **summary card** (average to 1 decimal + « n avis » + the **5→1 rating distribution** bars) over **review cards** (initial avatar · name · stars · date · text · the visit context `serviceName`/« avec {artistName} » · `photoUrls` thumbnails) + « Charger plus » (pageSize 50). Read-only like the app — salon replies/pro-side reporting stay a future reviews-module item (report is consumer-only, FR-REV-005). Pure `reviewStats` in `lib/pro/reviews.ts`. tsc/lint/build · **140 unit** (+3) · **41 e2e** (+1). Remaining parity follow-ups: web KYC upload · consumer web discovery map.
- 🟢 **Web manual booking — multi-service + the missing entry points (docs/design/web-manual-booking.md, 2026-07-10).** Closed the two flagged parity gaps in one dialog, **no backend change** (the contract already took `serviceIds[]`+`notes`+`sendSmsInvite`): the journal quick-create's single-service `<select>` becomes the app's **multi-service checkbox list** (price range · duration · running Total) + **Note** + the **SMS-invite switch** (« bientôt disponible » — server no-op until notifications), keeping the signed-off C1 client search-or-create; the dialog works **standalone** (date + time inputs, future-only guard « Choisissez une date et une heure à venir ») so web now has the app's three entry points: journal grid cell (time fixed by the cell) · **« + Nouveau rendez-vous » on `/pro/rendez-vous`** (all views, prefilled with the journal day) · **the client card CTA** pre-picked with the client (the C1b deferral, closed). Pure helpers (`manualBookingTotal`/`combineDateTime`/`isFutureIso`/gate) in `lib/pro/manual-booking.ts`. tsc/lint/build · **137 unit** (+5) · **40 e2e** (+2 standalone/card journeys; grid journey now books 2 prestations + note). Remaining parity follow-ups: web pro reviews · web KYC upload · consumer web discovery map.
- 🟢 **Web booking hub — the app's order-free flow on web (docs/design/booking-capacity-web-hub.md K2, 2026-07-10).** The linear wizard at `/(slug)/reserver` is replaced by the app hub's faithful port: **three sections always visible** (Prestations · Spécialiste · Date et heure) as summary cards — stacked on mobile, **sticky summary column on desktop**; the FIRST interaction fixes the entry point and the auto-advance follows the app's three orderings (`services→artist→time`, `artist→services→time`, `time→services→artist`). **Constraint graph 1:1:** services⇄artists capability (incompatible stylists dim/disable; an incompatible chosen stylist is dropped) · artist→slots (`artistId` on every fetch — consumes K1) · time-first books slots on the 30-min default then **silently re-validates/clears** the chosen time when services/variants change · artist-first + services → **earliest-slot auto-pick within 14 days** + « Prochain créneau » hint · **length variants** court/moyen/long (selector when the selection declares them; drives duration + slot fetches; the create call stays default-duration like the app — server-side variant duration is an open follow-up) · slot fetches carry a request id (stale drops). **Pay-later deposit proof on web:** the done step becomes the deposit sheet when the booking carries an acompte (operator + number + **screenshot upload**: consumer BFF `/api/uploads/sign` purpose-locked to `deposit` → direct-to-R2 POST → `/api/appointments/{id}/deposit`); the mon-compte detail gains the same attach block (+ « Justificatif envoyé » state) — no backend change, the app's endpoints. **Rebook prefill:** « Réserver à nouveau » carries `?services=…&artist=…`, sanitized against the live catalogue, landing on Date et heure. Hub machine = pure `lib/booking/state.ts`. tsc/lint/build · **132 unit** (+23: orderings, capability, variants, auto-pick, sanitize, deposit client) · **38 e2e** (+5: all three entry orders, capability dim, variant re-validation, in-flow deposit upload, rebook prefill). **→ The booking-capacity program (K1+K2+K3) is complete: web + app share one flow on a truthful multi-chair engine.**
- 🟢 **Multi-chair capacity — the per-artist slot engine (docs/design/booking-capacity-web-hub.md K1+K3, 2026-07-10).** The deepest correctness fix pre-launch: a salon was ONE chair (salon-level overlap exclusion; `/availability` ignored artists; even the app dropped the chosen `artistId`). Now: **per-artist capacity** — `availableSlots(artistId:)` validates the CHOSEN artist (capability per the app's rule · merged working-hours windows when defined, salon-inherited otherwise · no assigned overlap) and « Sans préférence » books against a **free-chair pool** (capable artists minus assigned-busy minus one per overlapping unassigned booking) — **all chairs taken ⇒ NOTHING bookable, unassigned included**; zero-artist salons keep v1 single-chair semantics. Booking + reschedule (incl. the J1 grid's cross-column drag) validate against the TARGET artist; busy windows use the stored `durationMinutes` (variant-accurate). Migration **0026**: salon-level `appointments_no_overlap`/`slot_unique` → per-artist partial unique + `btree_gist` EXCLUDE (assigned only; unassigned = documented app-level pool, race accepted pre-launch); PG `create` catches 23505/23P01 → `slot_unavailable`. `GET /availability?artistId=` (unknown → 400 `invalid_artist`); contract + regenerated web types; **T11 revised**. **K3:** the app now SENDS `artistId` (one line — the hub's artist choice is real); the web wizard + BFF pass it too (the K2 hub redesign consumes it next). analyze 0 · **336 backend** (+6 capacity: busy-artist exclusion · **all-chairs-busy blocks « Sans préférence »** · unassigned pool consumption · capability restriction · artist hours · invalid_artist) · 389 mobile · web tsc/lint/e2e green.
- 🟢 **Web pro registration + the sign-in/sign-up identity audit (docs/design/web-pro-registration.md, 2026-07-10).** Closed the P4 parity gap: salons can now register on the web — **`/pro/inscription`** (business fields + REQUIRED intl salon phone + identity: Google GIS « S'inscrire avec » env-gated | email→code, ONE submit → pro httpOnly cookies → dashboard; `provider_exists`/`otp_invalid`/`invalid_phone` copy); `/pro/connexion` links it (not-found CTA « Créer mon compte » + permanent footer; stale phone-era subtitle fixed); consumer `/connexion` now says **« Se connecter ou créer un compte »** + « créé automatiquement à votre première connexion » (sign-in IS sign-up — find-or-create). **Identity matrix VERIFIED against code+tests** (documented in the spec §1): Google→Google re-login by sub ✅ · email-registered→Google links on VERIFIED email (T33/T35, one account) ✅ · Google-registered→email code resolves by inbox proof ✅ · unverified email never links ✅ · same email may hold consumer AND salon accounts (separate spaces) ✅ · salon login never auto-creates ✅. No backend change. tsc/lint/build · 108 unit · **33 e2e** (+3). **Parity follow-ups (open):** web multi-service manual booking · web pro reviews · web KYC upload · consumer web discovery map.
- 🟢 **Module `journal` J1 — the web journal grid (docs/design/journal-j1-grid.md) — J1a backend + J1b web done (2026-07-09).** The YCLIENTS flagship screen. **J1a backend done:** `GET /providers/{id}/journal?date=` — ONE payload (weekday hours + breaks, null when closed/blocked · artist columns · the day's appointments, all statuses, ascending, enriched with `salonClientId`/`clientNoShowCount`/`arrivedAt`) · `POST /appointments/{id}/arrive` (« Client arrivé », J2 riding along: migration `0025`, confirmed-only, same-UTC-day, idempotent — T43) · reschedule gains `artistId` (drag across columns; salon-owned artists only → 400 `invalid_artist`; every move re-validated by the slot engine + DB exclusions — T42) · threats **T41–T43** · contract + regenerated web types. analyze 0 · **330 tests** (+10: day payload/closed/blocked, T41 negatives, arrive guards + idempotency, drag validation + taken-slot 409). **J1b web done:** « Journée » is the new DEFAULT view at `/pro/rendez-vous` (Calendrier + Liste kept) — artist columns + « Sans artiste », sticky 15-min axis, live « Maintenant » line, hatched breaks, cancelled-ghosts toggle, status colours incl. arrivé; **side panel** with the C2 client mini-card (audited read) + state-aware actions (Accepter/Refuser · Client arrivé/Terminé/Non présenté); **optimistic drag** (HTML5 DnD, snap to 15 min, 409 → « Créneau indisponible ») incl. cross-column reassign; **quick-create popover** with C1 client search-or-create → manual booking; day switcher (‹ › · date · Aujourd'hui). Pure grid geometry in `lib/pro/journal.ts`; 5 BFF/API additions (journal GET, reschedule, arrive action, manual-booking POST); stub + e2e. tsc/lint/build clean · **108 unit** (+9 geometry) · **30 e2e** (+2). **→ J1 complete: the YCLIENTS flagship grid is live (backend + web). Next: the pro-app day timeline (module J1b, own spec).**
- 🟢 **Module `journal` J1 — the web journal grid (docs/design/journal-j1-grid.md) — J1a backend + J1b web done (2026-07-09).** The YCLIENTS flagship screen. **J1a backend done:** `GET /providers/{id}/journal?date=` — ONE payload (weekday hours + breaks, null when closed/blocked · artist columns · the day's appointments, all statuses, ascending, enriched with `salonClientId`/`clientNoShowCount`/`arrivedAt`) · `POST /appointments/{id}/arrive` (« Client arrivé », J2 riding along: migration `0025`, confirmed-only, same-UTC-day, idempotent — T43) · reschedule gains `artistId` (drag across columns; salon-owned artists only → 400 `invalid_artist`; every move re-validated by the slot engine + DB exclusions — T42) · threats **T41–T43** · contract + regenerated web types. analyze 0 · **330 tests** (+10: day payload/closed/blocked, T41 negatives, arrive guards + idempotency, drag validation + taken-slot 409). **J1b web done:** « Journée » is the new DEFAULT view at `/pro/rendez-vous` (Calendrier + Liste kept) — artist columns + « Sans artiste », sticky 15-min axis, live « Maintenant » line, hatched breaks, cancelled-ghosts toggle, status colours incl. arrivé; **side panel** with the C2 client mini-card (audited read) + state-aware actions (Accepter/Refuser · Client arrivé/Terminé/Non présenté); **optimistic drag** (HTML5 DnD, snap to 15 min, 409 → « Créneau indisponible ») incl. cross-column reassign; **quick-create popover** with C1 client search-or-create → manual booking; day switcher (‹ › · date · Aujourd'hui). Pure grid geometry in `lib/pro/journal.ts`; 5 BFF/API additions (journal GET, reschedule, arrive action, manual-booking POST); stub + e2e. tsc/lint/build clean · **108 unit** (+9 geometry) · **30 e2e** (+2). **J1b-app done (2026-07-09):** « Ma journée » — the mobile-first day timeline is the pro app's DEFAULT appointment view (`/pro/journal`; calendar/list behind an Agenda toggle): date row + **week strip** (7-day count dots) + **artist filter chips**, time-ascending cards (status chip incl. **arrivé** · deposit · no-show badge), tappable **« Libre » gap slots** → prefilled manual booking, **flutter_slidable** swipe actions (right = Accepter/Arrivé/Terminé · left = Reprogrammer) + a long-press action sheet a11y fallback; `JournalDay` model + `getJournalDay`/`markArrived` (interface+mock+API) + `ProJournalProvider`; `Appointment` gains `arrivedAt`/`durationMinutes`. analyze 0 · **389 tests** (+12: JournalDay parse, provider load/filter/actions, 5 screen widget tests). **→ journal module complete on every surface (backend + web grid + app timeline).**
- 🟢 **Module `clients` C1 — the salon client base (docs/design/clients-c1.md) — C1a+C1b+C1c done (2026-07-08).** First build slice of the YCLIENTS-replica module program ([MODULES.md](MODULES.md) §4): the CRM is **derived, not entered** — bookings build it. **C1a backend done:** migration `0024` (`salon_clients` + `salon_client_notes` + generic `provider_audit_log`; backfill from historical bookings — linked clients' phones stored only when VERIFIED, guest rows keyed by phone, collisions merge into the user row — T49); `ClientsRepository`/`ClientsService` (+ Postgres impls); 7 routes under `/providers/{id}/clients` (paginated list w/ name/digit search + tag filter + page-1 `availableTags`, card w/ salon-scoped stats + upcoming + authored notes, visits history, manual add w/ `client_exists` 409 dedupe, tags ≤10×24, notes ≤500); **reads audited** (T46); booking-create/complete hooks keep the base fresh; provider `GET /appointments` enriched with `salonClientId` + `clientNoShowCount` (the accept badge); `DELETE /me` anonymizes across salons (T48); threats **T45–T49**; contract + regenerated web types. analyze 0 · **320 tests** (+24: service/routes/PG sections). **C1b web done:** `/pro/clients` (sidebar entry; searchable list w/ tag chips + masked phones + « MyWeli »/no-show badges + educational empty state; « Charger plus ») + `/pro/clients/[id]` card (call/WhatsApp links, tag editing w/ presets, authored notes « visible uniquement par votre équipe », salon-scoped history; web « Nouveau rendez-vous » CTA deferred — no web manual booking, parity gap flagged) + add-client modal (409 dedupe → opens the existing card) + the « {n} absences » badge + « Voir la fiche » link on the rendez-vous detail; 5 pro BFF routes; stub + 3 new e2e. tsc/lint/build clean · 99 unit · **28 e2e**. **C1c app done:** « Clients » dashboard tile → `/pro/clients` list (search débounced, tag chips, masked phones, MyWeli/absences badges, educational empty state, infinite scroll, BrandRefresh) + client card (Appeler/WhatsApp via url_launcher, tag sheet w/ presets + customs, team-only notes, stats strip, salon-scoped history, « Nouveau rendez-vous » PREFILLED into manual booking) + add-client sheet (dedupe → existing card + toast); no-show badge on the pro appointment rows AND detail (+ « Voir la fiche client »); `SalonClient*` models; `ProClientsServiceInterface` + realistic mock + API impl + `ProClientsProvider`; the flag-hidden `client_database_screen` placeholder retired. analyze 0 · **377 tests** (+20: mock/provider/API units + 9 widget tests over both screens). **→ C1 complete: the first YCLIENTS-replica module slice is Built end-to-end (backend + web + app).**
- 🟢 **Auth overhaul — Google + Apple + Email OTP (phone → contact) — P1–P4 done (2026-07-03).** SMS-OTP login is unaffordable pre-registration (Twilio $0.49 / Firebase $0.29 per OTP to CI; cheap Termii SMS gated on company registration) → launch auth = **Google Sign-In + Apple (iOS+web) + Email OTP (Resend)**; phone becomes an **unverified contact attribute** (verified later via Termii). **P1 backend (PR #166):** `POST /auth/google|apple` (RS256 against the providers' **JWKS** — iss/aud allowlists, exp, Google `email_verified`, Apple `nonce`; fail closed) · `POST /auth/email/otp/request|verify` (SMS-OTP-grade mechanics; no enumeration; new `EmailProvider` seam — Resend + Log) · migration `0022` (email/google_sub/apple_sub/auth_provider + `phone_verified`; phone nullable, UNIQUE dropped; **link only on a verified email** — T33) · **`AUTH_METHODS`** gate (launch: `google,apple,email` → phone-OTP 404; prod fail-fast only when explicit → deploy-safe) · `PATCH /me` contact phone · auto-sync matches **verified** phones only · threats **T31–T34** · 285 backend tests. **P2 web (this PR):** `LoginOptions` (email OTP always on; **Google GIS + Apple JS env-gated** via `NEXT_PUBLIC_*_CLIENT_ID` — email-first ship) on `/connexion` **and inline in the booking confirm step**; **contact phone MANDATORY at registration** (blocking post-sign-in step) and **required at booking** (prefilled, `PATCH /me`); account shows login identity + editable phone (« Non vérifié »); 4 BFF login routes (httpOnly cookies, shared nested-AuthSession helper); stub-api + consumer e2e on the email flow (pro web stays phone-OTP until P4); 92 unit + 25 e2e. **Live in prod (2026-07-02):** Resend configured + `AUTH_METHODS=google,apple,email` set in Render — web login is SMS-free; branded OTP email (brand lockup header) verified end-to-end. **Fix:** an expired/abandoned code no longer counts against the resend budget (was wrongly `otp_resend_limit`-ing a user who came back later) → 290 backend tests. **P3 app consumer done (2026-07-02):** new `LoginScreen` at `/login` (brand lockup · **Google** via `google_sign_in` w/ the web `serverClientId` · **Apple seam flag-hidden** (`APPLE_SIGN_IN`, store phase) · **email → code step** · **MANDATORY contact-phone step**, then `returnTo`); `AuthServiceInterface`+mock+API gain `signInWithGoogle/Apple` + email OTP + `updateUser(phone:)` (shared `_loginFrom` parses the nested AuthSession); `User` mirrors the DTO (nullable phone + `phoneVerified` + `authProvider`); profile gains the editable contact phone (« Non vérifié »); iOS `Info.plist` gets the Google client id + URL scheme; phone-OTP screens dormant (unrouted, kept for Termii verification). analyze 0 · **350 tests** (+12: mock/provider flows + LoginScreen widget incl. the phone-step block). **P4 pro done (2026-07-03):** pro auth off phone-OTP everywhere — backend: provider accounts gain email/google_sub/apple_sub/auth_provider (migration `0023`, unique partial indexes; phone UNIQUE dropped), `POST /auth/provider/google|apple` + `/auth/provider/email/otp/request|verify` (**LOGIN-ONLY** — `provider_not_found` 404, never auto-create; the verify does NOT consume the code so register can reuse it), `register` reshaped to **identity + business fields in ONE submit** (idToken | email+code · REQUIRED contact phone · returns a live flat ProviderSession — no more devCode two-step), pro phone-OTP routes behind `AUTH_METHODS` (**kills the last SMS spend**), threat **T35**, shared `providerSessionResponse`/`verifierError` shapers · 295 backend tests; web: `ProLoginOptions` on `/pro/connexion` (Google env-gated + email→code; not-found → « Créez votre compte dans l'app MyWeli Pro »), 3 pro BFF routes + `proLoginViaBackend`, e2e switched to the email flow (92 unit + 25 e2e); app: interface/mock/API + `ProAuthProvider` gain the six pro methods (login-only `_login`, `errorCode` for the « Créer un compte » CTA), `ProLoginScreen` rebuilt (Google · flag-hidden Apple · email→code · returnTo), `ProRegisterScreen` = business fields + inline identity section, `/pro/verify-otp` unrouted (screens dormant for Termii verification) · analyze 0 · **357 tests**. Existing phone-based test salons simply re-register (wiped pre-launch). **Fix (2026-07-03):** iOS Google sign-in was 401-ing (`token_rejected`) — the iOS Google SDK (AppAuth) mints its own `nonce`, which the verifier treated as unsolicited and rejected; `GoogleIdTokenVerifier` now ignores an unsolicited nonce claim (a presented nonce must still match; Apple stays strict — we issue that nonce) → 297 backend tests. Remaining activation: Apple Service ID (store phase, `APPLE_SIGN_IN`) · Termii phone verification post-registration. Design: [docs/design/auth-social-email.md](design/auth-social-email.md) + [web-auth-social.md](design/web-auth-social.md) + [app-auth-social.md](design/app-auth-social.md) + [pro-auth-social.md](design/pro-auth-social.md).
- 🟢 **Brand & launch-asset integration — P1 web (2026-07-02).** Wired the delivered MyWeli kit into `web/`: Next file-convention icons (`app/icon.svg` · `apple-icon.png` · `favicon.ico`) + `manifest.ts` (android-chrome/maskable, monochrome theme) + a **static branded OG** (replacing the dynamic generator); the **header renders the lockup SVG**; brand name adopted as **MyWeli** across metadata/JSON-LD/copy (generated schema untouched); Organization `logo` → raster PNG. Light + dark brand SVGs staged for dark mode. tsc · lint · 87 unit · 25 e2e green. Phased build — **P2 admin ✅** (`mobile/web/` favicon/icons/manifest + `index.html` → client dark branding, "MyWeli Admin"); **P3 app assets+loader ✅** (Lottie `BrandLoader` backs `LoadingIndicator` → all in-app loading on-brand · brand SVGs · login lockup; 338 tests); **P4 icon+splash ✅** (client iOS/Android app icon + native splash + `loader_v2` open animation on `/splash`); **P5 flavors+pro** 🟡 partial (Android `pro` flavor already existed → installed the **pro launcher icon** (black-on-`#FAFAFA`) + launcher names **MyWeli / MyWeli Pro**; the pro **splash** + **iOS flavor schemes** are a Mac/Xcode playbook — spec §8 — folded into the mobile store-deploy work, since blind `.pbxproj` edits can't be CI-verified and pro isn't in the first launch). **→ Consumer + web + admin branding complete.** Polish (2026-07-02): consumer splash + open loader → **light** (`#FAFAFA` + black `mixed` cut, flows into the light app); iOS app name → **MyWeli**; **swept every remaining `CircularProgressIndicator` → `BrandLoader`/`LoadingIndicator`** (screens, sheets, **`AppButton`** — white cut on the black primary; determinate upload-progress bars kept) so *all* loading is the `mark_loader`; splash holds ~3.8 s so the open animation is visible. 26 files · analyze 0 · 338 tests. **Pull-to-refresh now uses the `mark_loader` too** (`BrandRefresh` wrapping `custom_refresh_indicator`'s `CustomMaterialIndicator`, replacing all 13 `RefreshIndicator`s) → **loaders fully unified across the app**. Design: [docs/design/branding-integration.md](design/branding-integration.md).
- 🟢 **Subscriptions / monetization view (`FR-PRO-SUB-001`) — view done (2026-06-28).** Pro "Mon abonnement": plan + **3-month** trial status **derived from signup** (`GET /me/subscription`, no table/migration — there's no billing state in V1), Pro entitlements, **anchor pricing** (70 000 FCFA/mo provisional — config in `subscription_plans.dart`; planned base 20–40k), ROI line + "Nous contacter" (WhatsApp, `SUPPORT_WHATSAPP` env). Backend self-scoped + threat model **T25**; 221 backend + 316 mobile tests. **Paid collection still deferred** (no-custody/incorporation + OQ-3; early salons manual). Design: [docs/design/pro-subscription.md](design/pro-subscription.md).
- 🟢 **Auto-sync provider-entered bookings (`FR-APPT-008`) — done (2026-06-28).** `GET /appointments` (consumer) now also returns manual bookings made to the account's **OTP-verified phone**, matched **server-side** in `listForUser(matchPhone)` (`user_id = me OR client_phone = my_verified_phone`) — no mutation, no ownership transfer. Index migration `0020` keeps it index-backed; threat model **T26** (server-resolved phone only; recycling residual noted). App: a "Réservé par votre salon" badge on such cards. 224 backend + 318 mobile tests. Design: [docs/design/appointment-auto-sync.md](design/appointment-auto-sync.md).
- 🟢 **Notification preferences (`FR-NOTIF-004`) — done (2026-06-28).** Per-user opt-out prefs (`reminders` · `marketing` · `push`, default on; migration `0019`) stored server-side and **respected at send time** in `BookingNotifier` (reminders/marketing gate messaging + push by category; `push` gates the channel; transactional confirmations always send; the in-app feed is always recorded). Backend: routes `GET`/`PUT /me/notification-preferences` (self-scoped) + threat model **T24**. App: a `NotificationPreferencesScreen` (three toggles, optimistic + revert-on-failure) replacing the dead Profile switch, wired through `NotificationServiceInterface` (mock + Api). 213 backend + 309 mobile tests. Design: [docs/design/notification-preferences.md](design/notification-preferences.md).
- 🟢 **Add-to-calendar (`FR-APPT-006`) — done (2026-06-27).** Directions/maps ✅; native add-to-calendar shipped (`add_2_calendar`, one-tap from the appointment detail; iOS plist keys set, Android `<queries>` noted as TODO for when `android/` is scaffolded). Design: [docs/design/appointment-calendar.md](design/appointment-calendar.md).

**🔵 Deferred by design (correct).** Admin **impersonate / act-as** (`WEB-AD-002`); full-team **booking journal** (`FR-PRO-CAL-002`, behind `futureProviderFeatures=false`); à-domicile/home-service (V2); **Mobile Money aggregator** (non-goal, OQ-1/OQ-4); loyalty/memberships/gifting + the 8 `screens/provider/features/*` (V2/V3).

**🟣 Cross-cutting / launch readiness (deploy → activate → harden, mostly account-gated):**
- **Deployment/infra** — none in repo (no Dockerfile/host config); backend never run vs real Postgres/R2. The critical path to "on."
- **Activation** — Twilio account + WhatsApp template approval + reminder **cron**; **Firebase** project + creds; app-side **`firebase_messaging`** plugin; flip apps to `USE_API_BACKEND`.
- **Quality** — **no E2E/integration tests vs a real backend** (unit/handler only); low-end-Android **perf pass** (budgets); **release hardening** (obfuscation, no debug logs, APK size); store **data-safety** disclosures.
- **Analytics & instrumentation (§17)** — funnel events not instrumented (deferred; needs a product-analytics stack). **Crash reporting** ⏳ (needs DSN). **PII-at-rest / data residency** (OQ-7) — hosting decision.

**❓ Open questions still live:** OQ-2 (pricing — field validation), OQ-3 (store IAP vs web billing — gates subscriptions + web), OQ-7 (ARTCI data residency), OQ-8 (web stack — gates the public web surface). *Resolved:* OQ-1 (no custody), OQ-4 (no aggregator), OQ-5 (Twilio).

**📝 Doc debt:** ✅ the PRD's stale inline status notes were refreshed (2026-06-27) — shipped FRs (AUTH-002, DISC-006, BOOK-007, NOTIF-001/002, PRO-KYC-001, PRO-ONB-001, PRO-MEDIA-001, PRO-CAL-001, PRO-BOOK-004, PRO-MKT-001) now read accurately and `PRO-CAL-002` is marked V2-deferred. Remaining: the foundational consumer screens (auth, discovery, booking hub, my-bookings, favorites) predate the spec-per-part rule and have **no `docs/design/<part>.md`** — retroactive specs optional.

---

## Part 2 — Strategy: frontend-first, endorsed *with guardrails*

You proposed finishing the frontend before the backend. **I agree — for this codebase specifically — but with three guardrails, because an unconstrained "finish the whole frontend" has two failure modes that would hurt a security/performance-first product.**

### 2.1 Why frontend-first fits here
- The **interface→mock architecture already exists**. Swapping `MockProviderService` for `ApiProviderService` is localized and low-risk. The codebase was *designed* for this.
- **UX is the product's riskiest assumption** and its differentiator. A polished, clickable app (on mocks) lets you validate flows with real Abidjan salons/customers and drive supply-side onboarding demos — before spending on backend.
- For a small team, finishing one coherent layer beats context-switching between FE and BE.

### 2.2 The three guardrails (these make or break the approach)
1. **Constrain to V1 scope.** "Finish the frontend" = finish the **V1** frontend (PRD §7.1). Do **not** polish V2/V3 screens (the 8 ERP features, loyalty UI, etc.) now. Building screens you may cut is the #1 way frontend-first wastes months.
2. **Lock the API contract early.** While finishing the FE, define the real **API/DTO schemas** and make the **mock services return those exact shapes**. This turns the eventual backend swap from a rewrite into a config change, and prevents the classic "mock data shape ≠ real data shape → rework" trap.
3. **Spike the two highest-risk integrations early — do NOT leave them for last.** Mobile Money **deposits** and **WhatsApp/SMS** cannot be validated on mocks, and they are the core CI value prop *and* the biggest unknowns (feasibility, security, operator coverage). Build a **thin vertical slice** of each (one real deposit end-to-end in a sandbox, one real WhatsApp template send) **during** the frontend phase. This retires the scariest risks while you still have flexibility.

### 2.3 What frontend-first must NOT mean
- It must not mean **deferring security and performance** to "the backend phase." Both are architected and gated **now** (Parts 5 & 6). A client app leaks secrets, mishandles sessions, or janks on a Tecno regardless of backend maturity.
- It must not mean **mock data that hides reality** (unbounded lists, instant responses, perfect images). Mocks must simulate **latency, pagination, errors, and large/missing images** so the UI is built for the real world.

### 2.4 The shape of the plan
```
Phase 0  Foundation & guardrails        (git, CI, lint, tests, security/perf baselines)
Phase 1  Finish V1 frontend on mocks    (screen-by-screen, tested as we go)
   └─ in parallel near the end:
Phase 2  API contract + backend design  (lock DTOs; mocks mirror them)
Phase 1b Risk spikes (deposit, WhatsApp)(thin real vertical slices)
Phase 3  Backend build + integration    (swap mocks via interfaces)
Phase 4  Productionize integrations      (payments, notifications, uploads, KYC)
Phase 5  Security hardening + perf pass   (pentest, SAST/DAST, profiling)
Phase 6  Closed beta (Cocody) → launch   (3 communes)
```

---

## Part 3 — Phases, step by step

Each phase lists its work, its **exit criteria** (Definition of Done), and the tests that gate it.

### Phase 0 — Foundation & guardrails
**Goal:** make the repo safe to build in, with quality gates wired before feature work.
- [ ] `git init`, `.gitignore` audit (ensure `build/`, `.dart_tool/`, secrets excluded), private remote, branch protection.
- [ ] CI pipeline: `flutter analyze` (must be 0), `flutter test`, format check, on every PR.
- [ ] Tighten lints: adopt a stricter ruleset (e.g., `flutter_lints` + selected `lints`/`very_good_analysis` rules); `analyze` clean to **zero**.
- [ ] Fix the correctness warnings (`appointment_card.dart`) and prune dead code/imports.
- [ ] Add test tooling: `mocktail`, `golden_toolkit`/`alchemist`, `integration_test`, coverage reporting.
- [ ] **Security baseline:** secrets strategy (`--dart-define`/env, never in VCS), dependency scanning in CI (`flutter pub outdated`, OSV/Snyk), no-secrets pre-commit hook, build obfuscation flags planned.
- [ ] **Performance baseline:** define budgets (Part 6), set up profiling on the reference device, record a baseline cold-start/jank trace.
- [ ] Error handling + logging strategy (typed failures off `ApiResponse`, no silent catches, crash reporting wired: Crashlytics/Sentry).
- [ ] Mock services upgraded to **simulate latency, pagination, and error states**.

**Exit:** CI green on a real PR; analyze = 0; ≥1 unit + ≥1 widget + ≥1 golden test running in CI; crash reporting receiving events; budgets documented.

**Status (2026-06-20) — Phase 0 substantially complete:**
- ✅ Git + private GitHub remote (`Zaslons/myweli`); CI (format + strict analyze `--fatal-infos --fatal-warnings` + tests with coverage) green.
- ✅ Analyzer cleared to 0 under a **stricter ruleset** (added `unawaited_futures`, `avoid_dynamic_calls`, `cancel_subscriptions`, `prefer_single_quotes`, etc.); real fixes (async-gap, fire-and-forget, dynamic-call, dead code), not suppression.
- ✅ One-time `dart format` applied; CI enforces formatting.
- ✅ Test tooling: `mocktail` + first unit/widget/provider tests; coverage uploaded as a CI artifact.
- ✅ Error/observability **seam**: `AppLogger` + `runZonedGuarded` + `FlutterError.onError` in both entrypoints (ready for Sentry/Crashlytics).
- ✅ Dependabot (pub + github-actions).
- ⏳ **Deferred (need a decision or external account):** crash-reporting SDK wiring (needs Sentry DSN / Firebase project — seam is ready); **golden + integration tests** (cross-platform golden rendering needs a pinned-platform CI job; integration needs an emulator job); **branch protection** (would block our current direct-push-to-`main` flow — enable when we move to PR-based work); dependency **CVE gate** beyond Dependabot (OSV-Scanner) and `strict-casts`/`strict-raw-types` language modes (larger cleanup).

### Phase 1 — Finish the V1 frontend (on mocks)
**Goal:** every V1 screen complete, accessible, fast, and tested — using mock services.
Work the PRD V1 surface, prioritized by the booking → deposit → show-up loop. For **each screen/flow**, the Definition of Done is in Part 7. Suggested order:

1. **Design-system audit** — confirm theme tokens, spacing, components (`AppButton`, `AppTextField`, `EmptyState`, `LoadingIndicator`); add loading/error/empty variants everywhere; golden-test the component library.
2. **Auth** — ✅ real-OTP UX (5-min expiry, attempts/lockout, resend cap, inline error states); ✅ session persistence (secure storage); ✅ SMS auto-read affordance (OS autofill hint).
3. **Discovery** — add commune filter, price ranges, WhatsApp link, verified badge, before/after gallery; perf-test long lists (pagination, image placeholders).
4. **Booking + deposit (UI)** — ✅ deposit step UI, ✅ duration-by-length, ✅ buffers, ✅ rebook; deposit wired to a mock payment service.
5. **Appointments** — ✅ policy-bound cancel/reschedule UI; ✅ rich visit history with auto-synced status.
6. **Reviews** — photos, verified-booking badge.
7. **Notifications** — real in-app feed + center (replace stub), preferences UI.
8. **Profile / photos** — ✅ account deletion (typed confirm) + data export (JSON); ✅ image-upload pipeline (mock) wired into salon gallery + staff avatar + consumer profile avatar. Remaining: real bytes/CDN (backend).
9. **Pro V1** — ✅ KYC submission + verification status; ✅ guided onboarding checklist; ✅ no-show marking; ✅ manual booking entry; ✅ price ranges + duration variants on services; ✅ buffer between appointments; ✅ per-staff working hours; ✅ break times. (Payouts retired under the no-custody model; commission tracking is V2.)
10. ✅ **Flag-hidden** the unrouted V2/V3 feature screens behind `FeatureFlags.futureProviderFeatures` — each renders a placeholder while off, so they can't ship.

**Exit:** all V1 screens meet the per-screen DoD; flows pass integration tests against mocks; analyze = 0; coverage gate met; perf budget met on reference device for every screen.

**Status (2026-06-21) — Phase 1 in progress (consumer side advancing):**
- ✅ **Discovery — commune filter** (FR-DISC-002): pill on home + list, picker (search / "Près de moi" / all), persisted, mock providers tagged by commune.
- ✅ **Discovery — trust polish** (FR-DISC-006 / FR-BOOK-005): WhatsApp contact row on provider detail (`wa.me`), and **price ranges** (`Service.priceMax`) shown on detail / selection / confirmation, with "À partir de" totals (deposit math unchanged, on the from-price).
- ✅ **Booking + deposit** (FR-PAY-001): confirmation shows Total / Acompte / Solde; the deposit sheet is the **no-custody facilitated transfer** (Wave deep link / copy-number + optional screenshot → pending booking the salon confirms). Provider-side **deposit settings** (toggle + % + receiving handle).
- ✅ **Notifications** (FR-NOTIF-002): in-app feed replaces the stub (flat list, unread cues, "Tout lire", tap → mark read + deep link).
- ✅ **Reviews** (FR-REV-001/002/003): verified-booking badge, stylist attribution, photo display **and attach** (before/after, via the image pipeline, cap 3). Submission routed through `ReviewServiceInterface` (was local-only) and entered from the completed-appointment detail as well as the provider page.
- ✅ **Booking — rebook** (FR-BOOK-009): tapping a *past* appointment in the salon-page "appointments at this salon" carousel re-opens the booking hub pre-filled with the same services + stylist (sanitized against current provider data) and lands on Date & heure; past tiles show a "Réserver à nouveau" hint, upcoming appointments still open the detail.
- ✅ **Appointments — policy-bound cancel + reschedule** (FR-APPT-003/004/005): per-salon `cancellationWindowHours` (default 24h, snapshotted onto each appointment at booking); cancel shows the deposit consequence (forfeited within the window vs refunded outside, mock refund); reschedule reuses the `/booking/date-time` picker (deposit carries over, no penalty). Pros set the window in the existing "Acompte" settings screen. *(Real refund lands with the payment backend.)*
- ✅ **Auth — real-OTP UX** (FR-AUTH-002): the mock now enforces a 5-attempt lockout, 5-minute code expiry, and a 3-resend cap, returning typed error codes (`otp_invalid`/`otp_expired`/`otp_locked`/`otp_resend_limit`); the OTP screen renders inline error / locked / expired states with red boxes + attempts-remaining, plus backspace-to-previous, paste-to-fill, auto-submit, and a debug-only demo-code hint. *(Session persistence + SMS auto-read deferred to follow-ups.)*
- ✅ **Profile — account deletion + data export** (FR-PROF-008): authenticated users can **export their data** ("Mes données" → profile + rendez-vous + favoris, copied as JSON) and **delete their account** (type "SUPPRIMER" to confirm → clears session + favorites → back to login). Both behind `AuthServiceInterface` (`deleteAccount`) with a pure, unit-tested export builder. *(Real file download/share + server-side erasure land with the backend.)*
- ✅ **Appointments — rich visit history + auto-sync** (FR-APPT-005): the "Passés" tab is now a visit history — past, non-cancelled appointments auto-sync to "Terminé" (pure `effectiveAppointmentStatus`, a placeholder until backend completion events), grouped by month with a spend summary (visit count + total) and one-tap "Réserver à nouveau".
- ✅ **Auth — session persistence** (FR-AUTH): the session (mock token + user) is persisted in **`flutter_secure_storage`** and restored on cold start, so users stay logged in across restarts; logout and account deletion clear it. New `Session` model + `SessionStore` interface (secure + in-memory impls), behind `AuthServiceInterface`. *(Real access/refresh + rotation land with the backend; chosen lifetime: until logout, with a ready `expiresAt` field.)*
- ✅ **Auth — SMS auto-read** (FR-AUTH): the OTP boxes form an `AutofillGroup` and the first box requests `AutofillHints.oneTimeCode`, so iOS surfaces the SMS code in the keyboard QuickType bar (one tap) and Android autofill can fill it; the OS code lands in the first box and the existing paste-distribution fills the rest and auto-submits. No dependency, no permission. *(Zero-tap Android SMS Retriever needs the backend SMS to embed the app hash — deferred.)*
- ✅ **Pro — KYC & verification** (FR-PRO-KYC-001): a pro submits KYC documents (pièce d'identité + photo du visage required; RCCM required for salons, optional for freelancers à domicile; justificatif d'adresse optional) and sees their status (en attente / vérifié / refusé + motif); the screen surfaces that deposits are gated on verification. Behind `ProKycServiceInterface`; `kycDocs[]` + `rejectionReason` on `ProviderUser`. *(Mock document selection — real upload lands with the image pipeline; admin approval is backend.)*
- ✅ **Pro — guided onboarding checklist** (FR-PRO-ONB-001): the `/pro/onboarding` hub shows live progress over the steps (profil · services ≥3 · équipe · disponibilités · acompte · vérification · photos), each linking to its screen, with a "Mettre en ligne" CTA gated on the self-serve essentials. Pure `buildOnboardingChecklist`/`canGoLive` helper; `ProOnboardingProvider` aggregates the data; entries on the pro dashboard + profile. *(Photos step is informational until the image pipeline; "go live" publish is backend.)*
- ✅ **Pro — no-show marking** (FR-PRO): new `AppointmentStatus.noShow` (handled across all status chips; excluded from the consumer's completed visit history); the pro marks a confirmed, past-due appointment "absent" from the detail screen, with a confirmation noting the deposit is kept per the salon's cancellation policy. `markNoShow` behind `ProServiceInterface`.
- ✅ **Pro — manual booking entry** (FR-PRO-CAL): a `/pro/appointment/new` form (entered via "+" on the appointments list) where the pro records a walk-in/phone booking — multi-select services, date + time (now+future), client phone (required, with a "sans numéro" walk-in opt-out), optional name + note, and a (deferred) "Envoyer la confirmation + lien par SMS" toggle for client acquisition. Created `confirmed`, no online deposit. `createManualBooking` behind `ProServiceInterface`; `clientName`/`clientPhone` added to `Appointment`. *(Real SMS invite lands with the notifications backend; framed as a transactional confirmation.)*
- ✅ **Scope hygiene — flag-hide V2/V3 screens** (#10): the 8 unrouted `provider/features/` screens are gated behind `FeatureFlags.futureProviderFeatures` (false) and each early-returns a "Bientôt disponible" placeholder, so the cut features can't ship even if accidentally wired.
- 🗑️ **Pro — payouts of collected deposits** (FR-PRO-PAYOUT-001) — **retired (removed).** Built in #21 assuming Myweli holds deposits; superseded by the no-custody decision (PRD OQ-1) — nothing to pay out under direct client→salon payment. Screen/route/provider/service + tests deleted; recoverable from #21 / git history if escrow via an aggregator is added post-incorporation.
- ✅ **Pro — service price ranges + duration variants** (FR-PRO-SVC-001): the service form now takes an optional **prix maximum** (unlocking the range the consumer UI already renders via `formatPriceRange`) and an optional **"varie selon la longueur"** toggle with **court / moyen / long** durations (`DurationVariants` on `Service`, shaped per §548). The provider profile shows the variant durations. *(Booking slots driven by the client's chosen length — `FR-BOOK-006` — is the deferred follow-up; it touches the slot engine.)*
- ✅ **Booking — duration by hair length** (FR-BOOK-006): when a selected service declares duration variants, the booking hub shows a **"Longueur des cheveux"** selector (court/moyen/long, auto-defaulting to the middle bucket) that drives the **estimated duration → slot availability** (via the pure `booking_duration` helper); the choice is carried through `/booking/confirm` and shown in the summary. Single length per booking (the client's hair). *(Step-by-step screens `/booking/artist`→`/booking/date-time` and persisting the chosen length onto the booking DTO are follow-ups.)*
- ✅ **Booking — buffer between appointments** (FR-BOOK-004 / FR-PRO-AVAIL-001): the pro sets a provider-wide buffer (Aucun / 10 / 15 / 30 min, default Aucun) on the availability screen; consumer slot computation pads each existing booking by that buffer on both sides so new bookings keep the gap. `Availability.bufferMinutes`; mock `updateAvailability` now persists so the choice reaches slots.
- ✅ **Pro — per-staff working hours** (FR-PRO-AVAIL-001 / FR-PRO-STAFF-001): a staff member can follow salon hours (default) or have custom weekly hours (one range per day) via a shared `WeeklyHoursEditor` in the artist form; consumer slot computation only offers times that member works (within salon hours), in both the artist-specific and "any eligible artist" paths. `Artist.workingHours`; mock `create/updateArtist` persist into `MockData.providers` so slots reflect it.
- ✅ **Image-upload pipeline (frontend on mock)** (#8): `ImageUploadServiceInterface` + mock (simulated progress + hosted-URL contract; real impl = picker → compress/resize → CDN scan). Wired into a pro **"Photos du salon"** screen (`Provider.imageUrls`, add/remove, cover, four states) and **staff avatar** in the artist form, via a dependency-free mock image picker. `ProService.get/updateGalleryPhotos` persist into `MockData.providers` so the consumer hero gallery reflects edits.
- ✅ **Consumer — profile avatar** (#8): the user can set a profile photo (edit-profile screen, mock picker + upload) shown on their profile; `User.avatarUrl`, saved via `AuthService.updateUser`.
- ✅ **Pro — break times** (FR-PRO-AVAIL-001): a recurring daily break (e.g. lunch, one range/day) set on the availability screen via the shared `WeeklyHoursEditor`; consumer slot computation offers nothing overlapping a break (`Availability.breaks` + `overlapsBreak` helper). Demo: provider1 lunch 13:00–14:00 Tue–Sat.
- ✅ **Booking — step-screen length parity** (FR-BOOK-006): the hub's hair-length selector is now a shared `LengthVariantSelector` widget, also used by `date_time_selection_screen` (the reschedule/rebook + step-flow path), so that screen computes slot length from the chosen variant and carries it through to confirmation — matching the hub.
- 🧪 **Risk spike — WhatsApp/SMS messaging seam** (FR-NOTIF-001): scaffolded the outbound-messaging seam — `MessagingServiceInterface` + mock (WhatsApp-first with SMS fallback, delivery status, transactional-vs-promotional opt-in, in-memory outbox), a typed template catalog for the FR-NOTIF-001 events, and the booking-confirmed message wired (best-effort) at confirmation. The real **WhatsApp Business BSP** (template approval) + **reminder scheduler** (24h/2h) are the backend behind this seam.
- ✅ **WhatsApp/SMS — done (code).** Real Twilio BSP adapter + OTP-over-SMS + booking lifecycle events + 24h/2h reminder scheduler all shipped behind the seam (see Phase 3); only the ops step remains (register Twilio, approve templates, set the cron).
- 🚫 **Mobile Money aggregator spike — deliberate non-goal (user decision 2026-06-27).** Myweli stays **no-custody, screenshot-based** (PRD OQ-1): the customer pays the salon's Mobile Money directly and uploads a screenshot the salon verifies; Myweli never holds or settles funds — so no aggregator, merchant account, or incorporation dependency. The real-aggregator / escrow path is **not planned** and would only be revisited **post-incorporation** if the business ever chooses to hold funds. The V1 deposit flow is considered complete as-is. See [docs/design/consumer-deposit.md](design/consumer-deposit.md).
- ⏳ **Deferred to later phases:** à-domicile end-to-end. (Real image bytes/CDN/scan behind the mock pipeline are backend.)

### Phase 1b — Risk spikes (run during Phase 1, not after)
- ✅ **Mobile Money deposit (V1, no custody)** (FR-PAY-001/002):
  - **Salon side:** `Provider`/`Appointment`/`DepositPolicy` deposit-handle + screenshot fields; `depositRequired` default → **off**; deposit-settings screen takes the **receiving Mobile Money handle** (operator + number); `waveDeepLink` helper. Demo: provider2 requires 50% to its Wave number.
  - **Consumer side:** the deposit sheet is now a **facilitated transfer** — *Payer avec Wave* deep link (when the salon uses Wave) / number + **Copier** for the others, an **optional screenshot**, then **"J'ai payé"** → the booking is created **`pending`** (never auto-confirmed). The pro sees the deposit amount + screenshot on the appointment detail and **confirms** after receiving it. The old mock-pay path (`payDepositAndBook` + the entire `PaymentService` interface/mock + DI registration) is removed. *(Aggregator sandbox + escrow deferred to post-incorporation.)*
- ✅ **WhatsApp spike — done (code):** Twilio adapter + template send path built behind the seam (Phase 3); the actual approved-template send needs creds (ops).
- 🚫 **Mobile Money aggregator spike — not pursued.** Resolved as a non-goal: no-custody, screenshot-based deposits are the V1 answer (OQ-1; user decision 2026-06-27). Revisit only post-incorporation.
- [ ] Decision memo on OQ-3 (store IAP vs web billing) — legal/architecture lead time. *(OQ-1 deposit custody is resolved: no custody, no aggregator.)*

### Phase 2 — API contract & backend design (overlaps end of Phase 1)
- ✅ **Stack chosen: Dart + `dart_frog`** (REST), in `backend/` of this monorepo. One language app↔server (shared Dart DTOs, zero drift); public web stays Next.js/React on the same API via generated TS. *(PRD §8.2 decision, 2026-06-23.)*
- 🟡 **API contract** seeded as **OpenAPI 3.1** — [`docs/api/openapi.yaml`](api/openapi.yaml) — mirroring the app DTOs field-for-field (B0). First slice locked: health, `/providers` read, `/auth/otp/*`; more added per slice.
- ✅ **B0 foundation shipped:** `backend/` scaffold (dart_frog), `/health` route, strict analyze + tests, a dedicated **backend CI job** (`dart analyze --fatal-infos --fatal-warnings` + `dart test`).
- ✅ **Backend engineering guide + security model** — [`docs/BACKEND.md`](BACKEND.md): layering, conventions, the server-side security model (**JWT access + rotating refresh**, OTP rate-limit/lockout, secrets via env, deny-by-default authz, input validation), performance budgets, testing strategy, a living **STRIDE threat model**, and the backend PR DoD. Wired into the dev-guardrails skill. **CI security gates added:** secret scanning (gitleaks) + dependency-vulnerability scan (OSV). Built **before** the auth slice, by design.
- [ ] Auth model (phone/OTP, JWT/refresh) + the **slot engine** design (server-authoritative) — detailed as B2/booking slices land.
- [ ] Threat model the API (Part 5) before the first write endpoints.

### Phase 3 — Backend build + integration
- 🟢 **In-app notification center — complete (FR-NOTIF-002, PR2 of 2 / app).** `ApiNotificationService` (consumer `RefreshingHttpClient`, silent refresh) implements `NotificationServiceInterface` against `GET /me/notifications` + the read / read-all POSTs, so the existing notification center + unread badge read the **real feed** in API mode (mock stays for demo). Swapped in via DI behind `AppConfig.useApiBackend`; no screen change (the screen already consumed the interface). `flutter analyze` 0; 296 mobile tests (parse feed · markRead/markAllRead paths · not-connected guard); admin web build ok. **FR-NOTIF-002 done** (backend PR1 + app PR2) — the last V1 feature gap is closed. Design: [docs/design/notification-center.md](design/notification-center.md).
- 🟡 **In-app notification center — backend (FR-NOTIF-002, PR1 of 2).** A per-consumer **feed** (table `notifications`, migration `0018`) populated when booking lifecycle events fire: `BookingNotifier` now also writes an in-app entry (type mapped to `AppNotificationType`, title/body reused from the push, `route: /bookings`) alongside WhatsApp/SMS + push. Routes (consumer, **self-scoped**): `GET /me/notifications` (newest first, ≤50) · `POST /me/notifications/{id}/read` (404 on foreign/absent) · `POST /me/notifications/read-all`. `NotificationsRepository` (InMemory + Postgres). Contract (`Notification` schema + paths) + threat model **T23**; 202 backend tests (repo scoped CRUD · route list/mark/read-all + 401/404 · BookingNotifier writes the feed · manual booking → none); analyze 0; route-gen + boot smoke pass. **Next:** PR2 app — `ApiNotificationService` so the existing notification center reads the real feed. Design: [docs/design/notification-center.md](design/notification-center.md).
- 🟢 **Discovery sort & filter — complete (FR-DISC-007, PR2 of 2 / app).** The discovery filter bar gains a **"Trier"** pill → "Trier par" sheet (Pertinence / Mieux notés / Prix croissant) and a **"Disponible aujourd'hui"** toggle, wired through `ProviderProvider` (`setSort`/`setAvailableToday` → re-query) + `ProviderServiceInterface.getProviders(sort, availableToday)` (Api forwards; Mock applies — rating desc / min-active-price asc + an "open today" proxy). New `ProviderSort` enum. `flutter analyze` 0; 293 mobile tests (provider forwards sort/availableToday · mock orderings · available-today subset); admin web build ok. **À domicile = V2** (deferred). FR-DISC-007 done. Design: [docs/design/discovery-sort-filter.md](design/discovery-sort-filter.md).
- 🟡 **Discovery sort & filter — backend (FR-DISC-007, PR1 of 2).** `GET /providers` gains `sort` (relevance | rating | price) + `availableToday`. Sort is a **pure** `sortProviders` helper (rating desc · min-active-service-price asc, no-price last · relevance = repo order); `availableToday` filters via `SlotService.availableSlots(today)` (≥1 free slot), **before paging** so `total` stays correct. No repo change (composes in the route). Contract + tests (sortProviders unit · route sort=rating · availableToday = SlotService-derived set); 198 backend tests; analyze 0; route-gen + boot smoke pass. **À domicile = V2** (deferred). **Next:** PR2 app — "Trier" sheet + "Disponible aujourd'hui" toggle. Design: [docs/design/discovery-sort-filter.md](design/discovery-sort-filter.md).
- 🟢 **Provider before/after showcase — complete (FR-DISC-006, PR3 of 3 / consumer).** The consumer salon-profile **"Avant / Après"** section (between Photos and Avis, shown only when the salon has pairs): a **drag-to-reveal slider** (`BeforeAfterSlider` — a clip-rect wipe with a draggable handle, low-end-Android-cheap) per pair, optional caption, a thumbnail strip to switch pairs, and tap → fullscreen. Renders `Provider.beforeAfters` (already flowing from the backend/mock); a demo pair seeded on a mock salon. `flutter analyze` 0; 289 mobile tests (+ widget test: labels/caption · thumbnail strip · handle); admin web build ok. **Feature complete** across backend (PR1) + pro editor (PR2) + consumer (PR3). Design: [docs/design/provider-before-after.md](design/provider-before-after.md).
- 🟡 **Provider before/after showcase — pro management screen (FR-DISC-006, PR2 of 3).** The salon's "Avant / Après" editor (pro profile → next to "Photos du salon"): list pairs (before+after thumbs + caption, delete), **Ajouter une paire** → pick avant → pick après → optional caption → two uploads via the existing image pipeline → `PUT /providers/{id}/before-after`. New `BeforeAfterPair` model + `Provider.beforeAfters`; `ProServiceInterface.getBeforeAfters`/`updateBeforeAfters` (Api + Mock); `ProBeforeAfterProvider` (mirrors `ProGalleryProvider`); route `/pro/before-after` + profile entry + registration. `flutter analyze` 0; 286 mobile tests (model round-trip · mock persistence · provider load/add-two-uploads/remove · empty-source guard). **Next:** PR3 consumer drag-reveal slider on the profile. Design: [docs/design/provider-before-after.md](design/provider-before-after.md).
- 🟡 **Provider before/after showcase — backend (FR-DISC-006, PR1 of 3).** Salons curate before/after pairs of their work. Stored as `beforeAfters: [{before, after, caption?}]` in the provider's `data` jsonb (**no migration**), mirroring the gallery. `GET`/`PUT /providers/{id}/before-after` (provider-only + ownership → 403; validated: ≤12 pairs, each URL non-empty/≤2048/origin-allowlisted, caption ≤120) via `ProviderCatalogService.beforeAfters`/`updateBeforeAfters` + `ProvidersRepository.updateBeforeAfters` (InMemory + Postgres read-modify-write). Surfaces on the public `GET /providers/{id}` for free. Contract (`BeforeAfterPair`/`BeforeAfterList` + Provider schema) + threat model **T12**; 193 backend tests (+ DB-gated) — read/replace/validation/ownership/route; analyze 0; route-gen + boot smoke pass. **Next:** PR2 pro management screen · PR3 consumer drag-reveal slider. Design: [docs/design/provider-before-after.md](design/provider-before-after.md).
- 🟡 **Push notifications (FCM) — backend foundation.** The third FR-NOTIF-001 channel. **Device-token registry** (`POST`/`DELETE /me/devices`, self-scoped; `DeviceTokenRepository` InMemory + Postgres, migration `0017_device_tokens`). Provider-agnostic `PushProvider` (dev `LogPushProvider` + **`FcmV1PushProvider`** — service-account → OAuth2 access token via `AccessTokenSource` → `messages:send`; env-selected, fail-fast in prod). `PushService` fans out to a user's devices + **prunes invalid tokens**. Wired into `BookingNotifier` so the same lifecycle events also push to the consumer's devices (best-effort). Contract (`/me/devices`) + `.env.example` (`FCM_*`) + threat model **T22**; 191 backend tests (device repo · push-service prune · FCM v1 adapter mock-http · `/me/devices` self-scoping); analyze 0; route-gen + boot smoke pass. **App seam built (2026-06-28, on mocks — consumer #2 + pro #2b):** `PushNotificationService` + `DeviceRegistrationService` interfaces + mocks + `ApiDeviceRegistrationService` (`/me/devices`) + a `PushRegistration` coordinator (register-on-login-if-granted · **ask once** rationale sheet · unregister-on-logout · re-register on token refresh), wired into **both apps** — consumer (after first booking, consumer session) and pro (first dashboard visit, **provider session**); analyze 0; +tests. **Remaining = accounts phase:** create the Firebase project, set `FCM_*`, ship the real `FcmPushNotificationService` (`firebase_messaging` — permission/token/refresh + tap deep-link) + platform config (`google-services.json` via the `android/` scaffold), then flip the DI line. App design: [docs/design/push-notifications-app.md](design/push-notifications-app.md); backend: [docs/design/push-notifications-fcm.md](design/push-notifications-fcm.md).
- 🟢 **Messaging & notifications — booking events + reminder scheduler (WhatsApp + SMS, Twilio; PR B of B).** Completes FR-NOTIF-001 on the seam. `BookingNotifier` resolves the recipient (manual `clientPhone` else the consumer's phone) + salon name + params and fires **best-effort** at each transition: **accept→bookingConfirmed**, **reject→bookingDeclined**, **cancel→cancelled**, **reschedule→rescheduled**, **deposit submit→depositReceived** (refund copy rides bookingDeclined). The **24h/2h reminder scheduler** (`ReminderScheduler.tick`, idempotent via `appointment_reminders` PK + `AppointmentRepository.confirmedInWindow`) is driven by an internal cron route `POST /internal/cron/reminders` (`CRON_SECRET`, 404 when off). Migration `0016_appointment_reminders`; threat model **T21**; OpenAPI + `.env.example` (`CRON_SECRET`). 184 backend tests (notifier recipient-resolution · scheduler 24h/2h + idempotency); analyze 0; route-gen + boot smoke pass. **WhatsApp/SMS is now code-complete** — remaining is the ops step (register Twilio, approve templates, set the cron). Design: [docs/design/messaging-notifications.md](design/messaging-notifications.md).
- 🟡 **Messaging & notifications — foundation (WhatsApp + SMS, Twilio; PR A of B).** The FR-NOTIF-001 backend seam + the WhatsApp BSP spike (OQ-5). Provider-agnostic `MessagingProvider` (interface + dev `LogMessagingProvider` + `TwilioMessagingProvider`, env-selected, fail-fast in prod) behind `MessagingService` (`sendTemplate` — opt-out-gated promo, WhatsApp→SMS fallback, outbox record, best-effort; `sendOtp` — SMS, never persisted/logged; `updateStatus`). Outbox + opt-out repos (InMemory + Postgres, migration `0015_messaging`); template catalog mirrors the app field-for-field. **OTP now routed via the seam** (`/auth/otp/request` → SMS; `OtpRequestResult.code` carries the plaintext for delivery, `devCode` stays dev-only). **Delivery-status webhook** (`/webhooks/messaging/status`, shared-secret guarded). Contract + `.env.example` (Twilio keys) + threat model (**T18/T19/T20**); 179 backend tests; analyze 0; route-gen + boot smoke pass. **PR B:** booking lifecycle events + the 24h/2h reminder scheduler. Design: [docs/design/messaging-notifications.md](design/messaging-notifications.md).
- 🟢 **Admin / ops console — UI (Flutter Web): feature-complete.** All ops surfaces shipped on the shared admin design system: auth/shell/dashboard/KYC (UI-1) · review moderation (UI-2) · provider/user management + support views (UI-3) · disputes (UI-4) · audit-log viewer (UI-5). Built UX-first throughout (mockup → sign-off → build); `flutter analyze` 0, 280 mobile tests, `flutter build web --target lib/main_admin.dart` green. Design: [docs/design/admin-console-ui.md](design/admin-console-ui.md).
- 🟡 **Admin / ops console — UI: audit-log viewer (Flutter Web, UI-5).** UX-first (mockup signed off). **Journal** under Opérations (`/admin/audit`): read-only `AdminDataTable` (Quand · Action · Cible · Motif · Acteur, newest first) + an **action-filter dropdown** (French labels via `auditActionLabel`/`kAuditActions` → `?action=`) + **Précédent/Suivant pagination** (driven by `total`; the one list where it matters). `AdminService.audit` + `AdminAuditProvider`. `flutter analyze` 0; 280 mobile tests (loads entries+total · hasNext · setAction resets to page 1 + forwards filter · next/prev navigation); **`flutter build web` (main_admin) succeeds**. Design: [docs/design/admin-console-ui.md](design/admin-console-ui.md).
- 🟡 **Admin / ops console — UI: disputes (Flutter Web, UI-4).** UX-first (mockup signed off). New **Opérations** nav group → **Litiges** (`/admin/disputes`): filter (Ouverts/Résolus/Tous) + `AdminDataTable` (motif · statut · ouvert le) → **dispute detail** (`/admin/disputes/:id`) = Litige + Réservation cards + **Preuve** panel (signed deposit-screenshot, tap-to-zoom via new `AdminEvidenceImage`) + sticky **Résoudre** (resolution required; resolved shows the outcome). **No-custody** stated in-UI: resolution is advisory, no money moves. Disputes are **opened from a booking row** in the salon/client support views (`onOpenDispute` on `AdminBookingsCard` → reason dialog → `POST /admin/disputes`). `AdminService` (disputes/detail/open/resolve) + `AdminDisputesProvider`/`AdminDisputeDetailProvider`. `flutter analyze` 0; 277 mobile tests (list filter-forwarding · open posts · detail splits dispute/appointment/evidence · resolve updates); **`flutter build web` (main_admin) succeeds**. **Next UI:** audit-log viewer. Design: [docs/design/admin-console-ui.md §3](design/admin-console-ui.md).
- 🟡 **Admin / ops console — UI: salon/client support views (Flutter Web, UI-3b).** UX-first (mockup signed off). Clicking a row in Salons/Clients opens a read-only **support view** (`GET /admin/providers/:id` · `/admin/users/:id`): header + **Profil** card + **Derniers rendez-vous** table (date · statut · montant FCFA · client) + **sticky action bar** carrying the same actions (salon: ★ feature + Suspendre/Réactiver; client: Bannir/Réactiver) — acting also refreshes the underlying list. Mirrors the KYC-detail pattern; new shared widgets `AdminProfileCard`/`AdminBookingsCard`/`AdminActionBar`/`AdminDetailError`, `AdminScaffold.showBack`. Rows navigate via `push` (back returns to the list). `flutter analyze` 0; 274 mobile tests (detail load splits entity+bookings · suspend/ban update · load error); **`flutter build web` (main_admin) succeeds**. **Next UI:** disputes, then audit-log viewer. Design: [docs/design/admin-console-ui.md §3](design/admin-console-ui.md).
- 🟡 **Admin / ops console — UI: provider/user management (Flutter Web, UI-3a).** UX-first (mockup signed off). Two list screens under a new **Marché** nav group: **Salons** (`/admin/providers`) and **Clients** (`/admin/users`). Each = top-bar **search** + **status filter** (`AdminSegmentedControl`) + `AdminDataTable` with inline actions — salons: **★ mettre en avant / retirer** + **Suspendre / Réactiver** (reason); clients: **Bannir / Réactiver**. Acted rows update/drop optimistically (respecting the active filter) + snackbar. New shared widgets: `AdminSegmentedControl`, `AdminSearchField`. `AdminService` + `AdminProvidersProvider`/`AdminUsersProvider`. `flutter analyze` 0; 271 mobile tests (load · suspend/feature/ban update · filter-drop · error · search-forwarding); **`flutter build web` (main_admin) succeeds**. **Next UI:** row→detail support views (UI-3b: profile + recent bookings), then disputes, then audit. Design: [docs/design/admin-console-ui.md §3](design/admin-console-ui.md).
- 🟡 **Admin / ops console — UI: review moderation (Flutter Web, UI-2b).** The "Avis" screen on the design system, **UX-first** (mockup signed off). A **segmented** screen: **Signalés** (`AdminDataTable` of reported reviews → inline **Masquer** [reason dialog] / **Ignorer**, report-count `StatusChip`) and **Masqués** (hidden reviews → **Restaurer**). `AdminService` gains reportedReviews/hiddenReviews/hide/dismiss/restore; `AdminModerationProvider` (both queues + optimistic row removal); "Avis" nav item + `/admin/reviews` route. `flutter analyze` 0; 266 mobile tests (provider load/hide/dismiss/restore/error); **`flutter build web` (main_admin) succeeds**. **Next UI:** provider/user management, then disputes, then audit. Design: [docs/design/admin-console-ui.md §3](design/admin-console-ui.md).
- 🟡 **Admin / ops console — hidden-reviews list (B-admin, UI-2b backend):** `GET /admin/reviews/hidden` — currently-hidden reviews, paginated (the entry point the **"Avis masqués" / Restaurer** view needs, since hidden reviews leave the open-reports queue). `ReviewsRepository.listHidden` (InMemory + Postgres) + `ModerationService.hiddenQueue`. OpenAPI + spec updated; 169 backend tests (hiddenQueue lists hidden · restore removes them). Pairs with the UI-2b app slice (moderation screen: Signalés + Masqués segments). Design: [docs/design/admin-console-ui.md §3](design/admin-console-ui.md).
- 🟡 **Admin / ops console — UI (Flutter Web): UX plan + design system (UI-2a).** Course-correction after UI-1 shipped without a real UX pass: a full **UX/design plan was written and signed off** (mockup + comfortable density) — see [admin-console-ui.md](design/admin-console-ui.md) — then a shared **admin design system** was built and the shipped screens **reworked onto it**. New widgets: `AdminScaffold` (grouped **sidebar** + top bar — replaces the NavigationRail), `AdminDataTable` (comfortable rows + loading/empty/error states), `StatCard`, `StatusChip` (semantic pills), `showReasonDialog`. Dashboard → KPI **StatCard grid** + "À traiter" panel; KYC queue → **data table**; KYC detail → two-column + **sticky action bar** + reason dialog. Desktop-first, monochrome identity reusing the app tokens. `flutter analyze` 0; 262 mobile tests; **`flutter build web --target lib/main_admin.dart` succeeds**. **Next UI slices:** moderation (UI-2b) · provider/user mgmt · disputes · audit. Design: [docs/design/admin-console-ui.md](design/admin-console-ui.md).
- 🟡 **Admin / ops console — UI (Flutter Web, UI-1):** the admin backend is now human-usable. A **3rd Flutter entrypoint** `main_admin.dart` (Flutter Web, internal, behind admin login) — stack decided **Flutter Web over React** (internal+behind-auth → SEO irrelevant; reuse the existing models/services/theme; PRD §6.1 updated). Reuses `RefreshingHttpClient` (silent refresh at `/admin/auth/refresh`) with an **isolated** secure session key `myweli_admin_session`. UI-1 ships: **login** → dashboard → **KYC approval queue** → detail (signed doc viewing → approve / reject). One `AdminService` (auth + data) + 3 providers. **(Reworked to the design system in UI-2a above.)** Design: [docs/design/admin-console-ui.md](design/admin-console-ui.md).
- 🟢 **Admin / ops console (B-admin, Slice 3 A5b — North Star; Slice 3 complete):** `GET /admin/analytics/north-star?weeks=12` — completed bookings per **ISO week × commune** (the PRD North Star). `AppointmentRepository.completedForAnalytics(from)` returns completed bookings in the window; `AnalyticsService` buckets by week **in Dart** (Monday start, so both repo backends agree) and resolves commune from `listForAdmin`. Output `{weeks, fromWeek, totalCompleted, series:[{week, commune, completed}]}`. 168 backend tests (week×commune bucketing · window exclusion). **Admin Slices 1–3 done** (foundation · KYC · moderation · provider/user mgmt · featured · disputes · analytics overview + North Star). Remaining: Slice 4+ (anti-fraud signals, compliance export/delete, sub-roles) and the **console UI** (Flutter Web vs React — undecided). Design: [docs/design/admin-console.md](design/admin-console.md).
- 🟡 **Admin / ops console (B-admin, Slice 3 A5 — analytics overview, backend):** a read-only marketplace-health KPI snapshot (FR-WEB-AD-006). `GET /admin/analytics/overview` returns users (active/banned), providers (active/suspended), provider verification (pending/verified/rejected), **bookings by status**, **no-show + cancellation rates**, open/total **disputes**, and **reported-reviews** count. `AnalyticsService` composes existing repo `.total`s + one new `AppointmentRepository.countsByStatus()` (InMemory iterate · Postgres `GROUP BY`); on-the-fly aggregates, no new table. Event-based funnels (search→book) stay deferred (need a product-analytics stack, Slice 4). 167 backend tests (status counts · no-show 1/3 + cancel 1/4 rates · suspended-provider + open-dispute counts). **Next: A5b** (North Star — completed/week × commune). Design: [docs/design/admin-console.md](design/admin-console.md).
- 🟢 **Admin / ops console (B-admin, Slice 2 A4 — disputes; Slice 2 complete):** admin **dispute case management** on a booking (FR-WEB-AD-003). `POST /admin/disputes {appointmentId, reason}` opens a case (validates the booking); `GET /admin/disputes?status` lists; `GET /admin/disputes/{id}` returns the **evidence** (dispute + booking + a signed deposit-screenshot URL — admins are now authorized on `DepositService.screenshotUrl`); `POST /admin/disputes/{id}/resolve {resolution}` closes it with an outcome. **No money moves** (no-custody) — the consequence is applied via suspend/ban. All audited. Migration `0014_disputes`; `DisputesRepository` (InMemory + Postgres). Threat model **T17/T16** updated. 166 backend tests (open validates booking+reason · detail returns signed evidence · resolve records outcome + clears queue · audits). **Slice 2 done** (provider mgmt + featured · consumer ban · disputes). Remaining admin: Slice 3 (marketplace-health analytics), Slice 4+ (anti-fraud, compliance export, sub-roles, console UI). Design: [docs/design/admin-console.md](design/admin-console.md).
- 🟡 **Admin / ops console (B-admin, Slice 2 A3b — consumer management, backend):** admin can **ban** a consumer (`users.status`) → **login blocked** (`verifyOtp` → `account_suspended`, 403; short-lived tokens mean existing sessions die fast), **unban** reverses it. Read-only **support views** (`GET /admin/users`, `GET /admin/users/{id}` with recent bookings) — **no act-as impersonation** (deferred). All mutations audited. Migration `0013_user_status`; `AuthUser`/`User` DTO carry `status`. Threat model **T17** updated. 163 backend tests (ban blocks login + audit · unban restores · list filter + detail). **Next: A4** (admin dispute records) — finishes Slice 2. Design: [docs/design/admin-console.md](design/admin-console.md).
- 🟡 **Admin / ops console (B-admin, Slice 2 A3 — provider management + featured, backend):** marketplace controls on the admin trust boundary. Admin can **suspend** a provider (`providers.status`) → **hidden from discovery** (`ProvidersRepository.query` filters) **and new bookings rejected** (`BookingService` → `provider_suspended`, 409), while login still works to manage existing clients; **restore** reverses it; **feature** toggles homepage placement (discovery orders **featured-first**, then rating). Read-only **support views** (`GET /admin/providers`, `GET /admin/providers/{id}` with recent bookings) — **no act-as impersonation** (deferred per decision). All mutations audited. New real columns `providers.status`/`featured` (migration `0012_provider_mgmt`; merged into the jsonb-`data` DTO); the public Provider DTO now carries `featured`. Threat model **T17** updated. 161 backend tests (suspend hides+blocks+audits · restore · feature orders-first · list filter + detail). **Next: A3b** (consumer ban + login enforcement), then **A4** (disputes). Design: [docs/design/admin-console.md](design/admin-console.md).
- 🟡 **Admin / ops console (B-admin, Slice 1 A2 — review moderation, backend):** consumer **report-a-review** (FR-REV-005) + admin moderation, on the A1 trust boundary. `POST /reviews/{id}/report` (consumer; idempotent per reporter; the review **stays visible until an admin acts** — no weaponized auto-hide) feeds `GET /admin/reviews/reports` (queue, one row per reported review). Admin **hide** (excluded from the public feed **and** the rating recompute — reuses `ReviewsService.recomputeRatings`), **dismiss** (reports resolved, review kept), **restore** — all **audited**. Reviews gain `moderation_status`; new `review_reports` table (unique per reporter); the four public review reads + both rating aggregates now filter out `hidden`. Migration `0011_review_moderation`. Threat model **T14** updated. 157 backend tests (report idempotency/not-found · hide removes from feed+rating+resolves+audit · dismiss keeps visible · restore · report-route 200/403/401). **Slice 1 complete.** Next admin work: Slice 2 (suspend/ban/impersonate, disputes, featured). Design: [docs/design/admin-console.md](design/admin-console.md).
- 🟡 **Admin / ops console (B-admin, Slice 1 A1 — foundation + KYC, backend):** the internal admin **trust boundary** + the launch-blocking KYC approval. New `admin` role gates `/admin/*` (deny-by-default middleware; `/admin/auth` is the only open path) — the one surface that intentionally bypasses tenant ownership, so it's backed by an **append-only `audit_log`** (every mutation: actor + action + target + reason). Admin auth is **email + password (bcrypt)**, seeded super-admin from `ADMIN_EMAIL`/`ADMIN_PASSWORD` (no self-signup), rate-limited login + rotating/family-revoke refresh. **KYC queue:** list pending → `GET /admin/kyc/{id}` returns docs with short-TTL **signed-GET** view URLs (reuses `presignGet`) → **approve** / **reject + reason**, both audited — closing the deferred admin half of KYC so verified providers can take deposits. Migration `0010_admin` (admins, admin_refresh_tokens, audit_log). Threat model **T17** (+ T15 updated). 152 backend tests (admin auth: login/lockout/rotate-reuse; KYC queue/approve/reject + audit; trust-boundary 401/403/bypass). **Next: A2** (review report + moderation). Console **UI** is a later, separate effort (backend-first). Design: [docs/design/admin-console.md](design/admin-console.md).
- 🟢 **Consumer deposit / Mobile Money flow (B-deposit, B2 app):** the app now drives the B1 backend end-to-end. The deposit sheet uploads the screenshot to **private** storage via the consumer session (`uploadDepositScreenshot` → key; real picker in API mode, mock otherwise) and previews the local file — no more public-gallery upload. **Pay-later** lives on the consumer appointment-detail screen: a state-aware **Acompte card** (à envoyer → en attente de confirmation → confirmé) with "Envoyer l'acompte" (submit-mode sheet → `submitDeposit`) and "Voir ma capture" (signed-GET). The **pro accept screen** fetches the screenshot via a **signed URL** (`ProService.depositScreenshotUrl`) with proof/none/loading/error states (tap to enlarge). Screenshot stays **optional at booking** (PRD FR-PAY-004). `flutter analyze` 0; 256 mobile tests (api upload/submit/view, pro signed-GET, provider submit wrapper). Design: [docs/design/consumer-deposit.md](design/consumer-deposit.md).
- 🟡 **Consumer deposit / Mobile Money flow (B-deposit, B1 backend — the PRD top risk):** the no-custody deposit flow (PRD §6.1 / §9.4) hardened on the real backend. Deposit screenshots are now **private** payment proofs, not public: `POST /uploads/sign?purpose=deposit` is a **consumer-only**, self-scoped upload into a **dedicated private bucket** (`R2_DEPOSIT_BUCKET`, separate from KYC for independent retention/scope; bucket selection is a typed `StorageBucket` enum), and `StorageService.presignGet` adds **SigV4 signed-GET** so only the booking's consumer **or** its salon can view (`GET /appointments/{id}/deposit-screenshot`). `POST /appointments/{id}/deposit` lets a consumer attach/replace the screenshot on their own `pending` booking (pay-later). Myweli still moves no money — the salon confirms receipt by **accepting**. Threat model **T16**; contract + spec updated. Tests: signed-GET structure, deposit purpose (consumer 200 / provider 403), submit (owner/non-owner/non-pending/foreign-key), view auth (consumer + salon 200 / stranger 403 / 404). 141 backend. **Next: B2 app** (real picker + private upload + signed display). Design: [docs/design/consumer-deposit.md](design/consumer-deposit.md).
- 🟢 **Booking duration-overlap exclusion (backend, T11 follow-up):** a Postgres **`btree_gist` EXCLUDE constraint** (migration `0009`) now rejects any **time-range overlap** per provider — `tstzrange(appointment_date, appointment_date + duration_minutes) WITH &&` for `pending`/`confirmed` — closing the concurrency gap the exact-start unique index left (two different-start, overlapping-duration bookings). Bookings store a server-computed `duration_minutes` (sum of service durations); `create` + reschedule `update` map the `23P01` exclusion violation to `slot_unavailable` (the app slot engine is unchanged; the in-memory repo unaffected; buffer stays app-level). Half-open range → back-to-back allowed. Contract: `Appointment.durationMinutes`. Threat model **T11** updated. DB-gated tests (overlap rejected · back-to-back ok · per-provider · cancel frees · reschedule-overlap null) + the in-memory duration assertion. 131 backend (+ DB-gated). Design: [docs/design/booking-overlap-exclusion.md](design/booking-overlap-exclusion.md).
- 🟡 **Pro KYC — app uploader (PR B2):** `KycDocument` gains a storage `key`; `ApiProKycService` (provider session) uploads each picked doc to **private** storage via `/uploads/sign?purpose=kyc` (multipart → key), submits `{documents}` to `POST /me/kyc`, and reads status from `GET /me/kyc`; `KycStatus.fromJson` added. The KYC screen picks real files (`file_picker`, **images + PDF**) with a per-doc upload spinner (demo mode keeps the sample filename); `ProKycProvider.addDocument` is now an async upload. DI selects it under `useApiBackend`. 251 mobile, analyze 0. **Completes the provider-side KYC** (approve/reject + in-app doc viewing remain the deferred admin slice; provision a private `R2_KYC_BUCKET` to store real files). Design: [docs/design/pro-kyc.md](design/pro-kyc.md).
- 🟡 **Pro KYC — backend (PR B1):** `GET`/`POST /me/kyc` (provider-auth, self-scoped) submit ID-document metadata + private storage keys and read verification status; `POST /uploads/sign` gains a **`kyc` purpose** → a **separate private bucket** (`R2_KYC_BUCKET`), prefix `kyc/{accountId}`, returns the `key` only (no public URL; accepts PDF too). Submit validates each key is under the caller's own prefix, stores docs (migration `0008` adds `kyc_docs` to `provider_users`), and sets `verificationStatus=pending` (clearing rejection). `KycService` over `ProviderAuthRepository`; status/reason server-owned. Contract (`KycStatus`/`KycDocument` + `/me/kyc` + the kyc upload purpose) + threat model **T15**; storage/service/route/DB-gated tests. 131 backend (+ DB-gated). **PR B2 (app uploader + screen + `KycDocument.key`) is next; approve/reject + signed-GET viewing are a deferred admin slice.** Design: [docs/design/pro-kyc.md](design/pro-kyc.md). _(Provision a private `R2_KYC_BUCKET` to go live.)_
- 🟢 **Pro staff (artists) management (backend + app):** `GET`/`POST /providers/{id}/artists` + `PATCH`/`DELETE /providers/{id}/artists/{artistId}` (provider-auth + ownership → 403) — the salon's team CRUD, mirroring the services slice (read-modify-write `provider.data.artists`; new `ProvidersRepository.addArtist`/`updateArtist`/`deleteArtist` + artist methods on `ProviderCatalogService`). `id`/`providerId` server-set; **`rating`/`reviewCount` are server-owned** (from reviews); per-staff `workingHours` round-trips. `ApiProArtistService` (provider session; `artistId`-only edits resolve the salon from the session; `workingHours` JSON-normalized). No migration. Contract (`Artist`/`ArtistInput` + paths) + threat model T12; service/route/DB-gated + app tests. 247 mobile, 125 backend (+ DB-gated). Design: [docs/design/pro-artists.md](design/pro-artists.md). _(Remaining: KYC, next; notifications + messaging deferred per PRD.)_
- 🟢 **Consumer reviews (backend + app):** per-visit, **artist-attributed** reviews (Fresha/Booksy model). `POST /appointments/{id}/review` (consumer-auth; the appointment must be the caller's own **`completed`** one) — the server derives provider/artist/service/reviewer/`verified` from the appointment (client sets only rating/text/photos), one per appointment (upsert), and **recomputes the provider's + the artist's rating/reviewCount**. `GET /providers/{id}/reviews` is public + paginated; `GET /providers/{id}` embeds the latest 10. New `reviews` table (migration `0007`) + `ReviewsRepository` (in-memory + Postgres) + `ReviewsService` + `ProvidersRepository.updateRatings`. App: `ApiReviewService` (submit + `getProviderReviews`), `Review` gains `appointmentId`/`serviceName`, submit flows from a completed appointment, the pro reviews screen loads real data. Contract (`Review` + 2 paths) + threat model **T14**; service/repo/route/DB-gated + app tests. 243 mobile, 123 backend (+ DB-gated). Design: [docs/design/consumer-reviews.md](design/consumer-reviews.md). _(Remaining consumer mocks: notifications + messaging, deferred per PRD.)_
- 🟢 **Consumer favorites (backend + app):** `GET /me/favorites` + `POST`/`DELETE /me/favorites/{providerId}` (consumer-auth, **self-scoped by the token** — no user id accepted, provider token → 403) move favorites from on-device prefs to the account (follow the user across devices). New `favorites` table (migration `0006`) + `FavoritesRepository` (in-memory + Postgres, `ON CONFLICT DO NOTHING` idempotent add) + `FavoritesService` (validates provider existence → 404 on add). Add/remove idempotent → 204; list → `{providerIds}` (ids only; the app resolves them). New `ApiFavoritesService` on the consumer session (silent refresh); `isFavorite` derived locally. Contract (3 paths) + threat model T5 updated; repo/service/route (incl. cross-user isolation) + DB-gated + app tests. 239 mobile, 117 backend (+ DB-gated). Design: [docs/design/consumer-favorites.md](design/consumer-favorites.md). _(Next consumer mock: reviews. Notifications + messaging deferred per PRD.)_
- 🟢 **Pro deposit policy (backend + app):** `GET`/`PUT /providers/{id}/deposit-policy` (provider-auth + ownership → 403) read/replace the salon's deposit terms (required %, cancellation window, Mobile Money destination) — fields that already live in `providers.data`, so **no migration** and **no booking change**: `BookingService.book` already derives each deposit from them, proven by an integration test (set 40% → next booking carries a 40% deposit). New `ProvidersRepository.updateDepositPolicy` (in-memory + Postgres, atomic read-modify-write) + `depositPolicy`/`updateDepositPolicy` on `ProviderCatalogService` with money-grade validation (% 0..1 & >0 when required; window 0..720h; operator enum; E.164 number; **MoMo handle required when a deposit is required**). `ApiProService.{get,update}DepositPolicy` now hit it (silent refresh) — **the last pro mock; `ApiProService` no longer has any mock fallback.** Contract (`DepositPolicy` schema + paths) + threat model T12 updated; service/route/integration/DB-gated + app tests. 234 mobile, 114 backend (+ DB-gated). Design: [docs/design/pro-deposit-policy.md](design/pro-deposit-policy.md).
- 🟡 **Image upload pipeline — app picker + uploader (PR B2):** real `ApiImageUploadService` (selected under `useApiBackend`) **compresses on-device** (`flutter_image_compress`, ~1600px/q80) → `POST /uploads/sign` (provider silent refresh) → **multipart POST straight to R2** (the presign is the auth; bytes never touch our API) → returns the CDN URL, which flows into the gallery `PUT` (PR A). A real camera/gallery picker (`image_picker`) replaces the mock sheet against the backend (demo mode keeps the sample-image sheet); iOS `Info.plist` camera/photo permission strings added. Tested with `MockClient` + an injected compressor (sign→upload→publicUrl, 401→refresh, error paths). 233 mobile, analyze 0. **Completes the gallery pipeline** (provision R2 + `R2_*` secrets to go live). Design: [docs/design/pro-image-upload-pipeline.md](design/pro-image-upload-pipeline.md).
- 🟡 **Image upload pipeline — R2 signing (backend, PR B1):** `POST /uploads/sign` (provider-auth) issues a short-lived **presigned multipart POST** to Cloudflare R2 (S3-compatible) via a new `StorageService` (in-house SigV4 POST-policy signer — no AWS SDK; `FakeStorageService` for dev/CI/tests, `R2StorageService` for prod, selected by env like `DATABASE_URL`). The object **key is built server-side from the token's salon** (no cross-salon writes); the signed policy pins key + content-type allowlist (jpeg/png/webp) + a **`content-length-range`** size cap, so bytes go client→R2 directly and oversized/mistyped uploads are rejected at the storage boundary. Also tightens the gallery `PUT` to **allowlist our own origin** when public delivery is configured (completes T12). Env keys in `.env.example` (`R2_*`); prod fail-fast if unconfigured. Contract (`/uploads/sign` + `UploadTicket`) + threat model **T13** + T12. Tests: Fake + R2 SigV4 signer, signing service (key-scoping, content-type, unlinked 403), route (200/400/401/403/405), gallery origin allowlist. 111 backend (+ DB-gated). **PR B2 (app picker + uploader) is next.** Design: [docs/design/pro-image-upload-pipeline.md](design/pro-image-upload-pipeline.md). _(You provision the R2 bucket + `R2_*` secrets to go live; until then uploads run on Fake/mocks.)_
- 🟡 **Pro gallery photos — URL list (backend + app):** `GET`/`PUT /providers/{id}/gallery` (provider-auth + ownership → 403) read/replace the salon's `imageUrls` (in `providers.data` jsonb — no migration) via new `ProvidersRepository.updateGallery` (in-memory + Postgres, atomic read-modify-write of `data`) + `gallery`/`updateGallery` on `ProviderCatalogService`. Validated: list of non-empty URL strings, ≤20, ≤2048 chars. `ApiProService.getGalleryPhotos`/`updateGalleryPhotos` now hit it (silent refresh). **This is PR A of 2** — the byte→URL **upload pipeline** (Cloudflare R2, S3-compatible) is the companion slice [docs/design/pro-image-upload-pipeline.md](design/pro-image-upload-pipeline.md); byte upload stays on the mock `ImageUploadService` until it lands. Contract (`Gallery` schema + paths) + threat model T12 updated; service/route/DB-gated repo + app tests. 228 mobile, 104 backend (+ DB-gated). Design: [docs/design/pro-gallery.md](design/pro-gallery.md).
- 🟡 **Pro-side reschedule (backend + app):** `POST /appointments/{id}/reschedule` is now **role-aware** — a `provider` token moves one of its own salon's bookings (ownership by linked `providerId` → 403 cross-salon/unlinked), the `user` token keeps its self-ownership path. New `AppointmentLifecycleService.rescheduleByProvider` reuses the shared slot-validation + date-move logic (new time must be an open/free slot → 409; same `pending`/`confirmed` state guard) — no new path, no migration, no repo change. `ApiProService.rescheduleAppointment` now hits it (silent refresh; 200 → true). Contract description + threat model T11 updated; lifecycle service/route (provider 200 + cross-salon 403 + user regression) + app tests. 226 mobile, 100 backend (+ DB-gated). Design: [docs/design/pro-reschedule.md](design/pro-reschedule.md). _(No pro reschedule screen wires it yet — UX is a later slice.)_
- 🟡 **Provider earnings detail (backend + app):** `GET /providers/{id}/earnings?startDate=&endDate=` (provider-auth + ownership → 403) returns `EarningsData` (total + per-appointment transactions) via a new `ProviderEarningsService` over `listForProvider(status:'completed')` (no migration). Earnings = **`completed` only** (realized money — deliberately narrower than the dashboard's confirmed+completed revenue), newest-first, optional inclusive UTC date range. `ApiProService.getEarnings` now hits it (silent refresh; `EarningsData`/`EarningsTransaction` `fromJson` added). Contract (`EarningsData`/`EarningsTransaction` + path) + threat model T5 updated; service/route + app tests. 224 mobile, 100 backend (+ DB-gated). Design: [docs/design/provider-earnings.md](design/provider-earnings.md).
- 🟡 **Manual / phone booking (backend + app):** `POST /providers/{id}/appointments` (provider-auth + ownership → 403) lets a salon enter a booking itself — walk-in, after-the-fact, or a **future phone booking** — via `BookingService.bookManual`. Server-priced from the salon's services; created **`confirmed`** with **no online deposit** and a sentinel `userId:"manual"` (the `user_id` column is NOT-NULL/no-FK, so no migration). **No slot-engine validation** (the salon owns its calendar; any past/future/off-grid time), but the exact-start unique index still guards double-booking (409) and a future booking correctly occupies the slot for app consumers (slot engine excludes it). `clientPhone` validated E.164 when provided; `sendSmsInvite` accepted but a no-op (notifications deferred). `ApiProService.createManualBooking` now hits it (silent refresh free). Contract + threat model T5 updated; service/route/security-negative + app tests. 223 mobile, 96 backend (+ DB-gated). Design: [docs/design/pro-manual-booking.md](design/pro-manual-booking.md).
- 🟡 **Provider dashboard stats (backend + app):** `GET /providers/{id}/dashboard` (provider-auth + ownership → 403) returns server-computed `DashboardStats` via a new `ProviderDashboardService` over the existing `listForProvider` (no migration). Stats are UTC: revenue = Σ `totalPrice` of **confirmed + completed** in today/Mon-week/calendar-month windows; `todayAppointments` excludes cancelled; `pendingRequests`/`totalAppointments` as expected (fixes the mock's confirmed-only revenue drop-off). `ApiProService.getDashboardStats` now hits it (silent refresh free; `DashboardStats.fromJson` added). Contract (`DashboardStats` + path) + threat model T5 updated; service/route + app tests. 222 mobile tests, 90 backend (+ DB-gated). Design: [docs/design/provider-dashboard-stats.md](design/provider-dashboard-stats.md).
- 🟡 **Pro catalogue wired to the backend (app):** `ApiProService`'s services + availability methods now hit the real endpoints — `getProviderServices`/`createService`/`updateService`/`deleteService`/`setServiceActive` + `getProviderAvailability`/`updateAvailability` — through `RefreshingHttpClient` (provider session + silent refresh). The `serviceId`-only edits resolve the salon id from the persisted `ProviderSession`; ownership is re-checked server-side (403). `toggleServiceAvailability` is replaced by **`setServiceActive(serviceId, bool)`** on `ProServiceInterface`. Dashboard/gallery/earnings/deposit-policy/manual-booking/pro-reschedule still delegate to `MockProService`. Behind `useApiBackend`; mocks default; no UX/contract change. 221 mobile tests (catalogue GET/POST/PATCH/DELETE/active, 401→refresh, forbidden, unlinked fail-fast). Design: [docs/design/pro-catalogue-app-wiring.md](design/pro-catalogue-app-wiring.md).
- 🟡 **Provider catalogue management endpoints (backend, PR 2 of 2):** six ownership-scoped, provider-authenticated endpoints — `GET/POST /providers/{id}/services`, `PATCH/DELETE /providers/{id}/services/{serviceId}`, `GET/PUT /providers/{id}/availability` — over a new `ProviderCatalogService` (validation + ownership) + repository write methods (in-memory + Postgres; `replaceAvailability` in a transaction). A salon edits only its own catalogue/hours (token `account.providerId` must equal `{id}` → 403; unlinked → 403). Adds **`Service.active`** (toggle = `PATCH active`; a disabled service is rejected by booking) across the contract + Dart model. Contract (`Service.active`, `ServiceInput`, `Availability`/`TimeSlot`) + threat model **T12** updated; service/route/security-negative + DB-gated write tests. Completes the design spec. *(Next: the `ApiProService` app swap so the pro screens hit these.)*
- 🟡 **Provider catalogue normalized (backend foundation):** services & availability are moved **out of the providers JSONB document into first-class tables** — `provider_services` + `provider_availability` / `provider_working_hours` / `provider_breaks` / `provider_blocked_dates` (migration `0005`), backfilled out of `data` at startup (then stripped → single source of truth). `PostgresProvidersRepository` now **reassembles** the full provider DTO (core + services + availability) on read with batched lookups (no N+1), so `GET /providers*`, the slot engine, and booking are **byte-identical at their boundary**. Behaviour-preserving foundation (no new endpoints); `TimeSlot`s reconstruct to a canonical date (the engine uses time-of-day only). DB-gated tests cover backfill, assembly, and the stripped doc. **PR 1 of 2** — the management endpoints follow. Design: [docs/design/provider-services-availability-backend.md](design/provider-services-availability-backend.md).
- 🟡 **Pro appointments wired to the backend (+ provider silent refresh):** `ApiProService` puts the pro app's **appointment surface** on the real backend — `getProviderAppointments` (the salon's bookings via role-scoped `GET /appointments`) + the `accept` / `reject` / `complete` / `no-show` transitions. Authenticated calls go through `RefreshingHttpClient` pointed at the **provider** session + `/auth/provider/refresh`, so a pro acting after the ~15-min access token expires is silently re-authenticated (the provider half of silent refresh — `ProviderSession` now persists the `refreshToken`). Everything without a backend yet (dashboard, services, gallery, availability, earnings, deposit policy, manual booking, pro reschedule) **delegates to an embedded `MockProService`**, so the pro app keeps working. Wired in by DI behind `useApiBackend`; mocks remain the default. Backend `forbidden`/`not_found`/`invalid_state` map to clear French messages. 212 mobile tests (incl. the 401→provider-refresh→retry). No backend/contract change (endpoints already shipped).
- 🟡 **Provider appointments list (backend):** `GET /appointments` is now **role-scoped** — a **provider** token returns the bookings of the salon it manages (resolved via the account's linked `providerId`; an unlinked provider → **403**), while a user token is unchanged. `AppointmentRepository.listForProvider` gains an optional `status` filter (in-memory + Postgres; the slot engine still gets every status). Contract + threat model **T5** updated; tests cover the salon list, the status filter, cross-role isolation, and the unlinked-provider 403 (+ DB-gated). Unblocks wiring the pro app's appointment surface to the backend.
- 🟡 **Server boot smoke (CI):** a dedicated `backend-boot-smoke` job validates the **production build path** that unit tests never exercise — `dart_frog build` code-gens the whole route tree, then the built server boots against a real Postgres service, runs `initializeDatabase()` (migrations + provider seed) via the custom `main.dart` entrypoint, and answers live HTTP: `/health` → ok, `/providers` → seeded data, and an OTP request→verify **auth round-trip** → tokens. Catches route-tree code-gen breaks and startup/migration faults before a deploy. Locally de-risked (in-memory boot + endpoints green); no product or contract change.
- 🟡 **Provider auth wired to the backend:** `ApiAuthService`'s five provider methods now call the real backend (`/auth/provider/otp/request`, `/register`, `/otp/verify`) instead of delegating to the mock. Register dispatches a code without logging in; **verify** returns `{provider, accessToken}` and persists a new **`ProviderSession`** (`{token, provider}`) under its **own** secure-storage key (`myweli_provider_session`), so the consumer and provider sessions coexist on one device and the pro app survives cold start. `provider_not_found` / `provider_exists` get clear French messages. With `--dart-define=USE_API_BACKEND=true` the pro app now signs in against the real backend; mocks remain the default. 196 mobile tests. No backend/contract change (routes already shipped in B-prov + B3e). *(Provider-side **silent refresh** in the app remains — the backend `/auth/provider/refresh` endpoint now exists.)*
- 🟡 **B3a — repository seam:** `ProvidersRepository` + `AuthRepository` are now **interfaces** with `InMemory*` impls, built in one composition root (`backend/lib/src/dependencies.dart`) and provided to routes via middleware. Makes the Postgres swap a one-place change. Pure refactor — no contract change.
- 🟡 **Provider refresh tokens:** provider login now issues a **rotating opaque refresh token** alongside the access JWT (hashed at rest, rotated each use, **reuse → family revoke**), mirroring the consumer flow exactly. Verify returns `{provider, accessToken, refreshToken, expiresAt}` (flat + additive) and a new **`POST /auth/provider/refresh`** exchanges it. Migration `0004` adds a `provider_refresh_tokens` table (FK to `provider_users`, separate from the consumer `refresh_tokens`). Both repos (in-memory + Postgres) + the contract + threat model T4 updated; security-negative tests (rotate / reuse→family-revoke / invalid / replay→401) green, incl. DB-gated. Backend-only — the app-side **silent refresh** (consumer + provider) is the next slice that consumes this.
- 🟡 **B3e — Postgres for provider accounts:** `PostgresProviderAuthRepository` behind the interface (parameterized; mirrors the in-memory security logic — hashed OTP + attempt/resend budgets, registration-before-login, provider-role JWT), selected when `DATABASE_URL` is set. Migration `0003` adds `provider_users` + a **separate** `provider_otp_codes` table (so a phone used as both consumer and provider doesn't collide). DB-gated tests (register/duplicate, verify-requires-registration, lockout, `accountById` link) run against the CI Postgres service. **No in-memory persistence left under `DATABASE_URL`** (all of auth, providers, appointments, provider-accounts are now Postgres). Contract unchanged. *(KYC docs remain a follow-up.)*
- 🟡 **B3d — Postgres for appointments:** `PostgresAppointmentRepository` (parameterized; `service_ids` jsonb; UTC) behind the interface, selected when `DATABASE_URL` is set. Migration `0002` adds the `appointments` table + a **partial unique index** on `(provider_id, appointment_date)` for non-cancelled statuses → **atomic double-booking prevention** (concurrent same-slot insert → `ON CONFLICT DO NOTHING` → `create` returns null → `slot_unavailable`), closing the TOCTOU race the app-level check couldn't. DB-gated tests (create/list/byId/update + the unique-index conflict) run against the CI Postgres service. Threat model T11. *(Provider-accounts Postgres + duration-overlap exclusion are follow-ups.)*
- 🟡 **Client silent refresh (consumer):** a reusable **`RefreshingHttpClient`** wraps authenticated calls — on a **401** it exchanges the stored refresh token at `/auth/refresh`, **rotates** the persisted session, and retries the request **once**; a *rejected* refresh ends the session (back to sign-in) while a *transport failure* keeps it (a flaky network never logs you out). `ApiAuthService` (`/me`) + `ApiAppointmentService` (book/list/detail/cancel/reschedule) route through it; `Session` now persists the `refreshToken`. So "stay logged in until logout" is finally real for the consumer app, not just true until the ~15-min access token expires. 202 mobile tests (incl. the 401→refresh→retry, rejected-refresh-clears, transport-fail-keeps cases). Store-shape-agnostic, so the **provider app reuses it verbatim** (provider store + `/auth/provider/refresh`) in the next slice. No backend/contract change.
- 🟡 **App wired to the backend (consumer core):** `ApiAppointmentService` implements `AppointmentServiceInterface` over HTTP (book/list/detail/cancel/reschedule + public `/availability` slots), reading the access token from the persisted `Session`. With `--dart-define=USE_API_BACKEND=true` the app now runs **consumer auth + provider reads + the full appointments flow** against the real backend (other services stay mock); mocks remain the default. `slot_unavailable` surfaces as *"Ce créneau n'est plus disponible…"*; the no-deposit booking copy now says *"en attente de confirmation du salon"* (consistent with the pending model). 192 mobile tests. *(Provider-auth + consumer silent-refresh now wired too — see above; provider-side silent-refresh is the remaining follow-up.)*
- 🟡 **Double-booking prevention (backend):** `POST /appointments` and reschedule now **validate the requested time against the slot engine** server-side — a closed/past/break/already-booked/non-aligned time is rejected with `slot_unavailable` (409). Closes the ROADMAP Phase 3 "double-booking prevention" gap at the app level; a DB unique-constraint/transaction for true concurrency lands with the Postgres appointment impl. 72 backend tests (incl. a real double-book attempt → conflict). Threat model T11.
- 🟡 **Appointment lifecycle (pro, backend):** `POST /appointments/{id}/{accept,reject,complete,no-show}` — **provider-role token only**, and the account's `providerId` must match the appointment's salon (cross-salon or unlinked → **403**), with a state-guarded machine (pending→confirmed→completed; pending→cancelled; →noShow; bad transition → **409**). Provider accounts gain an optional `providerId` link (set at registration) + `ProviderAuthRepository.accountById`; `ProAppointmentService`. 69 backend tests (state machine, role 403, cross-salon 403, ownership). This closes the core booking state machine — a `pending` booking can now be confirmed by its salon.
- 🟡 **Appointment lifecycle (consumer, backend):** `POST /appointments/{id}/cancel` + `/reschedule` (auth + **ownership** → 403; state guard → 409 on terminal status). Cancel records status only (deposit is salon↔client, no custody); reschedule moves the date with deposit/balance carried over. `AppointmentLifecycleService` + `AppointmentRepository.update`. 64 backend tests. *(Pro-side transitions — accept/complete/no-show — are a follow-up: they need the provider-account ↔ Provider link + provider authz.)*
- 🟡 **Slot engine (backend):** `GET /availability?providerId=&date=` (public) — **server-authoritative** bookable start times computed from the provider's weekly schedule, blocked dates, breaks, a setup/cleanup buffer, the requested duration (from `serviceIds` or `durationMinutes`), and existing non-cancelled bookings (buffer-padded). UTC throughout (Abidjan is UTC+0). `SlotService` (reads `ProvidersRepository` + `AppointmentRepository`); seed providers gained a Mon–Sat 09:00–18:00 schedule. 59 backend tests (open day, closed day, duration-fit, booking exclusion, blocked date, break, route 400/404/405). *(Provider-level v1 — per-artist working hours + eligible-artist-by-service is a follow-up.)*
- 🟡 **Appointments (backend, book + read):** authenticated `POST /appointments` (created **`pending`**; the salon confirms, never auto-confirmed — PRD OQ-1), `GET /appointments` (caller's, status filter), `GET /appointments/{id}` (ownership → **403** for another user's booking). **Server-authoritative pricing**: total/deposit/balance computed from the provider's service prices + deposit policy — a client-sent price is ignored. `AppointmentRepository` + `BookingService` (reads `ProvidersRepository`); contract + `Appointment` schema. 48 backend tests. *(Slot engine / `getAvailableTimeSlots`, lifecycle (cancel/reschedule/pro accept-complete-no-show), Postgres impl, and app wiring are follow-ups.)*
- 🟡 **Provider auth (backend):** `POST /auth/provider/otp/request` · `/auth/provider/register` · `/auth/provider/otp/verify` — hashed OTP + attempt/resend budgets (same rigor as consumer), registration (duplicate → 409), and a **provider-role JWT** on verify; **registration is required before login** (verify → 404 if absent — tighter than the old auto-creating mock). `ProviderAuthRepository` interface + in-memory impl + routes + contract (`ProviderUser` schema). 41 backend tests green. *(App-side `ApiAuthService` swap off the mock + a Postgres provider impl are follow-ups; provider auth still uses in-memory regardless of `DATABASE_URL` for now.)*
- 🟡 **B3c — Postgres persistence:** `PostgresProvidersRepository` + `PostgresAuthRepository` (`package:postgres`, parameterized queries, transactions on multi-write paths) behind the same interfaces; selected in the composition root when `DATABASE_URL` is set (unset → in-memory). Embedded SQL migrations + provider seed (`lib/src/db/migrations.dart`), run at startup via a custom `main.dart` entrypoint. **A Postgres service container** in the backend CI job exercises the DB-backed repo tests (OTP lockout, refresh reuse→revoke, update/delete, provider query/byId); they skip locally without `DATABASE_URL`. *(JSONB-document storage for providers; normalized tables for users/otp/refresh. Real DB verified in CI, not locally.)*
- 🟡 **B3b — async repository interfaces:** the repository methods now return `Future`s (I/O-ready), with the in-memory impls + routes + tests updated. Required before a Postgres impl can satisfy them. 33 backend tests green, analyze 0, no contract change. *(**B3c** adds the Postgres impls + SQL migrations + a CI Postgres service, where the real DB is exercised in CI.)*
- 🟡 **B2 — auth slice shipped:** backend `POST /auth/otp/{request,verify}` + `/auth/refresh` + protected `PATCH/DELETE /me`. **JWT access (HS256, ~15 min) + rotating opaque refresh** (hashed at rest, reuse → family revoke); OTP **hashed + server-side rate-limit/lockout**; deny-by-default `/me` scoped to the token `sub`. `ApiAuthService` swaps `AuthServiceInterface` behind `AppConfig.useApiBackend` (provider-auth delegates to the mock until its own slice; mocks stay default). Backend security/negative tests (lockout, expiry, refresh reuse→401, ownership) + app `MockClient` tests. *(Client silent-refresh wiring + real SMS are later slices.)*
- 🟡 **B1 — provider read slice shipped:** backend `GET /providers` (search/filter/paginate, rating-sorted) + `GET /providers/{id}` over a seeded in-memory store mirroring the mock data; `ApiProviderService` implements `ProviderServiceInterface` and is wired in by DI **only when `AppConfig.useApiBackend=true`** (`--dart-define`), so mocks remain the default for tests/demos. Contract-faithful, with backend + app tests. Next: auth (B2) → Postgres (B3).
- [ ] Build endpoints by domain; swap mock implementations for real ones **one interface at a time** (auth → providers → booking → favorites → pro), behind a build flag so mock mode still runs for tests/demos.
- [ ] **Contract tests** ensure each real service matches the interface the UI already depends on.
- [ ] Server-authoritative availability + **double-booking prevention** under concurrency.

### Phase 4 — Productionize integrations
- [ ] **Payments:** deposits, balance, refunds, payouts, subscription billing; signed callbacks; reconciliation.
- [ ] **Notifications:** push (FCM) + SMS fallback + WhatsApp orchestration with delivery tracking.
- [ ] **Image upload pipeline:** compress/resize/CDN + moderation hooks.
- [ ] **KYC** submission + admin approval (web console).
- [ ] **Offline:** cached reads + queued mutations + sync/conflict handling.

### Phase 5 — Security hardening & performance pass
- [ ] External **penetration test**; SAST/DAST in CI; dependency CVE gate; MASVS checklist.
- [ ] Cert pinning, obfuscation, jailbreak/root awareness as appropriate; secrets audit.
- [ ] Load test the slot engine + payment callbacks; perf profiling on reference device under real data volumes; APK size budget enforced.

### Phase 6 — Closed beta → launch
- [ ] Cocody closed beta (15–25 verified providers), field-onboarded; validate deposit→show-up lift.
- [ ] Fix retention loop; staged store rollout; expand to Marcory + Plateau on density gates (PRD §19).

---

## Part 4 — Testing strategy (test everything on the way)

Testing is continuous and gates each phase. Flutter test pyramid:

| Layer | Tooling | What we test | Gate |
|---|---|---|---|
| **Unit** | `flutter_test`, `mocktail` | Models (serialization, equality), providers (state transitions, error handling), utils (formatters: FCFA, phone, duration; validators), the booking draft/slot logic | ≥80% on `models/`, `providers/`, `core/utils/` |
| **Widget** | `flutter_test` | Each screen renders loading/empty/error/success; key interactions; auth gating; `returnTo` continuity | Every V1 screen has ≥1 widget test |
| **Golden** | `golden_toolkit`/`alchemist` | Visual regression of the component library + key screens (incl. small/large text scale) | Components + critical screens covered; runs in CI |
| **Integration / E2E** | `integration_test`, optionally `patrol` | Full flows on mocks: login→book→deposit→confirm→cancel; pro accept→complete; rebook | Critical flows green in CI |
| **Contract** | custom harness | Real service impls satisfy the same interface + DTO shapes the mocks do (Phase 3) | Every interface has a contract test |
| **Manual QA** | Reference low-end Android + iOS | Real-device feel, jank, slow-network behavior, French copy, RTL-safety | Pre-release checklist per build |
| **Backend** | stack-dependent | Unit, API contract, **load** (slot engine, payment callbacks), **security** (authz, rate limits) | Gates Phase 3/4 |

**Principles:** write tests alongside each screen in Phase 1 (not after); CI blocks merge on analyze≠0 or red tests or coverage drop; mocks simulate latency/errors so tests cover the unhappy paths; every bug fix ships with a regression test.

---

## Part 5 — Security (first-order, continuous)

Security is gated from Phase 0, not deferred. Reference: **OWASP MASVS** (mobile) + **OWASP ASVS** (backend).

### During the frontend phase (now)
- **No secrets in the client or VCS.** Keys via `--dart-define`/secure config; CI secret scanning + pre-commit hook.
- **Secure storage** for tokens (already using `flutter_secure_storage`) — verify usage; no PII in `shared_preferences` or logs.
- **Input validation** on every field (phone, OTP, amounts); never trust client-entered prices/IDs — design so the server re-validates.
- **Session model designed now:** short-lived access + refresh, rotation, explicit logout clears storage, OTP rate-limit/lockout UX.
- **Build hardening planned:** obfuscation (`--obfuscate --split-debug-info`), no debug logging in release, no `print` of sensitive data.

### Backend & integration phase
- **Authentication/authorization:** every endpoint authz-checked; users can only access their own data; provider scoping; admin RBAC + audit log.
- **Payments:** signed/verified Mobile Money callbacks; idempotency keys; server-side amount authority; reconciliation; **deposit custody decision (OQ-1)** drives AML/KYC obligations.
- **PII & KYC docs:** encryption at rest + transit; least-privilege, access-audited KYC storage; data-subject delete/export (PRD §18, ARTCI).
- **Abuse:** rate limiting, OTP/SMS-pumping protection, referral/promo fraud checks, off-platform-leakage monitoring.

### Pre-launch (Phase 5)
- External **penetration test**; SAST + DAST in CI; dependency CVE gate; threat-model review; MASVS/ASVS checklist signed off.

---

## Part 6 — Performance (first-order, continuous)

Budgeted against the **reference low-end Android (2–3 GB RAM, Android 9)** from day one.

### Budgets (enforced in CI/manual QA)
- Cold start **p95 < 3.5s**; key screens interactive **< 1.5s** on 3G.
- Steady **60fps** scroll on lists/galleries; no frame >16ms in critical flows.
- Initial **APK < 30 MB**; monitor size per release.
- Memory stable on image-heavy screens (provider detail, galleries, stories).

### Practices
- **Continuous profiling:** DevTools timeline + memory on the reference device each milestone; record baselines, alert on regressions.
- **Images:** compress/resize server-side; cached, lazy-loaded (existing `TimedCachedImage`); never autoplay media on cellular; placeholders to avoid layout jank.
- **Lists:** pagination everywhere (already in interfaces); `const` constructors (analyze flagged misses); avoid rebuild storms (scoped `Consumer`/`Selector`).
- **Data thrift:** small payloads, cache reads offline, dedupe in-flight requests (the booking hub already tracks a `slotsRequestId` — good pattern to generalize).
- **Startup:** defer non-critical init; lazy-load heavy deps (maps, calendar).

---

## Part 7 — Definition of Done & quality gates

**Before building any part:** write a detailed **design spec** in [`docs/design/`](design/README.md) first (per [`docs/design/TEMPLATE.md`](design/TEMPLATE.md)) — invoke the guardrails skill, re-confirm fit with this ROADMAP + the security model + the architecture, align on the spec, then build and cross-link it. Specs are the per-part source of truth.

**Per screen/feature (Phase 1):**
- [ ] Loading, empty, error, and success states implemented.
- [ ] Wired to a mock service that simulates latency + errors.
- [ ] Widget test + golden; flow covered by an integration test if it's part of a critical path.
- [ ] French copy reviewed; text scaling + tap targets + contrast checked.
- [ ] Meets perf budget on the reference device; no analyze issues.
- [ ] No secrets, no sensitive logging; inputs validated.

**Per PR (CI gates):**
- [ ] `flutter analyze` = 0; format clean; tests green; coverage not decreased; dependency scan clean.

**Per phase:** the phase's exit criteria (Part 3) are met and signed off.

---

## Part 8 — Immediate next actions (ordered)

1. **`git init` + first commit + private remote** (today).
2. **CI** running analyze + test on every push.
3. **Clear analyze to zero**; fix `appointment_card.dart` correctness warnings; prune dead code.
4. **Test harness** + first unit/widget/golden tests on existing models, providers, and the booking hub.
5. **Upgrade mocks** to simulate latency/pagination/errors.
6. **Lock V1 scope**; flag-hide V2/V3 feature screens.
7. **Start Phase 1** screen-by-screen, **and** kick off the **deposit + WhatsApp spikes** (Phase 1b) in parallel.
8. **Draft the API contract** so mocks mirror real DTOs.

> The single biggest risk to "frontend-first" is treating payments + WhatsApp as a backend-phase afterthought. They're the CI value prop and the biggest unknowns — spike them **while** the frontend is being finished, not after.
