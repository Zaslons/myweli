# Design specs

Every non-trivial part of Myweli gets a **design spec here before any code is written**. A spec is the single, authoritative description of how that part should work — so the build is deliberate, professional, and nothing is missed.

This is a standing rule, not a suggestion (see the `myweli-dev-guardrails` / `myweli-backend-guardrails` skills and the `design-spec-per-part` memory).

> **Before any UI/design work, read [SYSTEM.md](SYSTEM.md)** — **the design system** for the apps: identity (incl. the brand-black vs ink rule), every token, the component catalog, the four-states contract, accessibility, forms, feedback, microcopy, the enforcement gates, and §21 — the honest register of what the code still violates. Plus the part's spec below. (Rule: `check-design-standards-first`.)
>
> **Working on the web (`web/`)?** Read **[WEB-SYSTEM.md](WEB-SYSTEM.md)** (the web half — semantic HTML, focus, ARIA, responsive/desktop, SEO, the install push; shared rules stay in SYSTEM.md) + **[../WEB.md](../WEB.md)** (web architecture/conventions) first, and invoke the **`myweli-web-guardrails`** skill.
>
> *(`DESIGN-STANDARDS.md` and `WEB-DESIGN-STANDARDS.md` are superseded stubs that redirect here.)*

## The workflow — for every part

1. **Invoke the relevant guardrails skill** (`myweli-dev-guardrails`, and `myweli-backend-guardrails` for anything under `backend/`).
2. **Re-confirm fit** with the agreed ROADMAP, rules, security model, patterns, structure, and architecture — don't drift.
3. **Write the spec** as `docs/design/<part>.md`, copying [`TEMPLATE.md`](TEMPLATE.md). Fill in every applicable section in detail.
4. **Align** on the spec with the user *before* building.
5. **Cross-link** the spec from the files it governs — the [ROADMAP](../ROADMAP.md) entry, the code files (a `// Design: docs/design/<part>.md` reference near the top), and the [API contract](../api/openapi.yaml) — so the design stays discoverable as the source of truth.
6. **Build** to the spec. If reality forces a change, **update the spec in the same PR** (keep it honest, like the PRD/ROADMAP).

When is a spec required? Any new feature, slice, endpoint, screen, or integration. Trivial fixes (typos, a one-line bug, a dependency bump) don't need one.

## Spec status legend

`Draft` (being written / under review) → `Approved` (aligned, ready to build) → `Built` (shipped; spec reflects what exists) → `Superseded` (replaced — link the successor).

## Index

| Part | Spec | Status |
|------|------|--------|
| Upload orphans — why a lifecycle rule is not the answer (storage) | [backend-upload-orphans.md](backend-upload-orphans.md) | **Analysis + decision owed** — the size cap runs at CLAIM time, so an object nobody claims is never size-checked at all: the one path where the limit does not exist. A bucket lifecycle rule cannot fix it, because it expires by age and prefix while 'claimed' lives in Postgres — an age rule would delete galleries, retained KYC evidence, or payment proofs for open disputes. Three designs costed (rate-limit / `pending/` prefix promoted on claim / sweep cron); **`pending/` recommended and free to adopt while all three buckets are empty**, verified by listing them. Not theoretical: `r2_live_test.dart` left two orphans of its own, now purged and its cleanup hardened. |
| Upload claim hardening — the four surfaces `pending/` never protected (storage) | [backend-upload-claim-hardening.md](backend-upload-claim-hardening.md) | **Built** — round two of enrolling surfaces into `pending/`. The booking-time deposit key was stored raw, so the read endpoint (which authorizes on the APPOINTMENT, never on the key) presigned keys the booking did not own, T61 never ran on it, and every honestly-attached proof was deleted by the daily expiry. The salon gallery was **write-once** and a rejected KYC could not be **partially** resubmitted — the mirror bug: both re-send server-issued keys into the *claim* form. Adds `promoteNewKeys` (the key-shaped twin of `promoteNewUrls` — KYC has no public base, so the url-shaped fix cannot apply), makes promotion **copy-all-then-delete-all** so a mid-batch failure is retryable instead of orphan-making, and refuses `.`/`..`/empty key segments because a `startsWith` prefix check is not an ownership check. |
| Upload size cap, claim-time (storage) | [backend-upload-size-verification.md](backend-upload-size-verification.md) | **Built** — the regression opened by the presigned-PUT fix. R2 ignores a signed `content-length`, so the signing-time cap became advisory and any authenticated client could PUT an arbitrarily large object into a bucket we pay for. `UploadVerificationService` HEADs at claim time, refuses and **deletes** anything over the cap, and **fails closed** when storage is unreachable. Wired into all four claims — and they do not share a shape: deposit and KYC carry keys, review and gallery carry URLs whose key is derived only after the origin allowlist passes. Threat model **T61**. Residual: an upload never claimed is an orphan, bounded only by a bucket lifecycle rule. |
| iOS flavors — two apps from one codebase (mobile) | [mobile-ios-flavours.md](mobile-ios-flavours.md) | **Built** — Android shipped a consumer/pro flavor split; iOS never did. It had ONE bundle id, `com.sadreddine.myweli.pro` — a personal namespace matching neither convention, while `Info.plist` said `MyWeli`, the *consumer* name — and **no way to build the consumer app for iOS at all**. Now six build configurations, two shared schemes, per-flavor xcconfig and `$(APP_DISPLAY_NAME)`, giving `com.myweli.app` / `com.myweli.pro` to match Android. Scripted with the `xcodeproj` gem (`mobile/ios/tool/setup_flavours.rb`, idempotent) because a hand-edited `project.pbxproj` can open in Xcode yet fail `xcodebuild` — which it did, twice. **CI never builds iOS**, so all three build paths were verified locally against the built `Info.plist`. Unblocks Apple Sign-In: the bundle id IS the `aud` for native iOS. |
| Q1b — the funnel smoke authenticates against production (auth) | [backend-q1b-smoke-seam.md](backend-q1b-smoke-seam.md) | **Built** — the Q1 harness reads `devCode`, which production suppresses, so the acceptance gate G1 §8 depends on had never been runnable in the environment it certifies. Adds the **only** production OTP-disclosure path in the codebase, gated on **two independent** conditions: a constant-time `SMOKE_OTP_SECRET` match (≥32 chars) **and** an identity in the **RFC 2606 `.test` TLD**, a compile-time constant no environment value can widen to a real address — so a fully leaked secret buys sessions on unroutable throwaways and nothing else. Absent by default; announced in the boot log when active. Threat model **T60**. Open: the funnel writes to the live database, fine once at cutover, not as a recurring gate (§7). |
| Observabilité — remontée d'erreurs (3 surfaces) | [observability-error-reporting.md](observability-error-reporting.md) | **Backend livré ; web et mobile à suivre.** Ferme LAUNCH.md §5.2 — *« saurions-nous que c'est cassé ? »* — qui **bloque le plan de lancement** : le déploiement progressif (§1.4) fonctionne en surveillant le taux de plantage entre les paliers, donc sans ce chiffre le mécanisme qui rend une version d'app irrévocable survivable ne fonctionne pas. **Sentry**, choisi parce qu'il capture les erreurs *gérées* — un 500 sur la réservation, un justificatif de dépôt qui n'arrive pas — et non seulement les plantages ; Crashlytics ne couvre ni le backend ni le web. Côté backend ce n'était pas « ajouter Sentry » mais **construire la couche d'erreurs que BACKEND.md décrivait depuis des mois** : §1, §2 et §3.6 nommaient un request-id et une conversion erreur→enveloppe, et la chaîne n'était que des fournisseurs d'injection. Le nettoyage PII est la partie sensible : liste blanche, et l'assertion décisive porte sur la charge utile sérialisée, pas champ par champ. |
| Délais des migrations — et le verrou qu'il ne fallait pas borner | [backend-migration-timeouts.md](backend-migration-timeouts.md) | **Livré le 2026-08-16.** Ferme la garde que [infra-staging.md](infra-staging.md) §3.2 prescrivait depuis la conception du staging. **La mesure a changé la conception** : contre PostgreSQL 16, `lock_timeout` **et** `statement_timeout` interrompent tous deux un `pg_advisory_lock()` **en attente** — or ce verrou est contendu *par conception* (les instances qui démarrent à froid doivent faire la queue derrière celle qui migre), donc les poser sur la connexion de `withSchemaLock` aurait mis une échéance sur une contention **saine** et cassé tout déploiement normal. Le vrai danger est l'autre : les migrations tournent avant l'ouverture du port, **pendant que la révision précédente sert encore**, et un `ALTER TABLE` bloqué fait la queue devant **tous les lecteurs suivants** de la table. Donc `SET LOCAL` (jamais `SET` — ce pool sert aussi les requêtes ; mesuré : rien ne survit au commit) en tête des **quatre** transactions du chemin de démarrage, verrou consultatif délibérément **non borné**. Échappatoire testée pour une migration légitimement lente. Regardé rouge : garde retirée, l'`ALTER` bloqué **ne revient jamais** ; garde en place, il échoue en 3 s. |
| Volume et migrations — les 60 s, mesurées | [backend-migration-volume.md](backend-migration-volume.md) | **Mesuré le 2026-08-16.** Ferme [backend-migration-timeouts.md](backend-migration-timeouts.md) §8 q3 et la garde de chronométrage d'[infra-staging.md](infra-staging.md) §3.2 — que la restauration PITR ne pouvait pas fournir, la production étant vide. Sur un `db-f1-micro` (même palier que la production), une contrainte `EXCLUDE USING gist` sur `appointments` — une forme que `migrations.dart` emploie **deux fois** — prend **14,3 s à 100 k lignes et 264 s à 150 k**. **Une falaise, pas une pente** : 1,5× de données pour 18× de temps, de sorte qu'extrapoler depuis les petits paliers se trompe d'un ordre de grandeur (cette enquête a prédit ~32 s pour 200 k et en a mesuré 339). **Ce n'est pas `maintenance_work_mem`** — testé à 64/256/512 Mo, aucun effet. **C'est la machine** : l'utilisation CPU reste plate à 18–19 % pendant quatorze minutes de construction, la signature d'un cœur partagé bridé. **Et le vrai plafond n'est pas le délai** : les migrations tournent avant l'ouverture du port, donc c'est la sonde de démarrage (**300 s**) qui borne, et la même instruction la dépasse au-delà de ~200 k lignes — augmenter `statement_timeout` ne fait que transformer « interrompu en une minute » en « révision jamais prête après cinq ». Donc 60 s **reste**, et la réponse au-delà de 150 k est un **changement de palier** ou ne pas construire l'index au démarrage. Confirmé au passage : `ADD COLUMN … NOT NULL DEFAULT now()` est à temps constant (~358 ms), donc la migration 0009 est sûre à toute taille. Outil : `backend/tool/volume_probe.dart`. |
| Restauration de la base — la répétition, les cinq pièges, et la promotion | [infra-dr-restore.md](infra-dr-restore.md) | **Restauration répétée le 2026-08-16 ; PROMOTION répétée le 2026-08-17 (§8) ; PITR activé ET EXERCÉ sur le staging le 2026-08-17 (§8.4–8.5).** La promotion **n'est pas** un changement de `DATABASE_URL` — ce que quatre documents affirmaient, dont celui-ci. `DATABASE_URL` contient `127.0.0.1:5432` (le side-car proxy) et **ne nomme aucune instance** ; l'instance est désignée **deux fois** dans le manifeste du service (l'annotation `cloudsql-instances` et l'argv du proxy). Donc promouvoir = **un diff de deux lignes + un déploiement de 17 s**, tous les secrets inchangés et **aucune étape IAM** (`roles/cloudsql.client` est accordé au niveau du projet). **Le même piège auto-effaçant que l'épinglage de trafic** : un manifeste modifié à la main est écrasé par le déploiement suivant du manifeste **committé** — il faut committer les deux lignes. La restauration domine tout : **12 min 33 s** pour un clone depuis sauvegarde contre **17 s** pour le repointage, donc une estimation de RTO est une estimation de restauration. Vérifié par la seule preuve qui pouvait échouer : les logs du side-car nomment l'instance réellement servie (`/providers` à 200 ne prouve **rien**, le clone étant identique à sa source). Trouvé au passage : **le staging ne conserve qu'UNE sauvegarde et n'a pas de PITR**, là où la production en garde 7 avec 7 jours de journaux. **Répété le 2026-08-16.** Ferme [LAUNCH.md](../LAUNCH.md) §5.5 (« tant que ce n'est pas fait, nous ne savons pas que nous avons des sauvegardes ») et donne à [infra-rollback.md](infra-rollback.md) §5.3 une procédure au lieu d'un renvoi. Un clone PITR de la production est remonté avec **31 migrations et 39 tables** intactes, en **26 min 14 s** — production et staging jamais touchées, l'instance restaurée n'était rattachée à aucun service, puis supprimée. **Cinq pièges, et chacun ressemble à un autre problème** : l'instance annonce `RUNNABLE` vingt minutes avant la fin du clone (attendre l'**opération**, pas l'état) ; les modifications de configuration sont **refusées** pendant le clone ; la protection contre la suppression est **héritée**, donc le nettoyage fait deux commandes ; `--authorized-networks` exige de l'IPv4 ; et le pire — le rôle `postgres` de Cloud SQL **ne voit pas** les tables de l'application (elles appartiennent à `myweli_app`), si bien qu'une restauration parfaitement saine annonce **zéro table**, ce qui est indiscernable d'un échec et oriente l'enquête vers la sauvegarde plutôt que vers la connexion. **26 minutes est un plancher**, mesuré sur 10 Mo. Et la raison invoquée pour la lancer était partiellement fausse : la production ne contient que **5 lignes d'utilisateurs et aucun salon**, donc cette restauration **ne peut pas** valider le `statement_timeout` de 60 s — il faudra du volume synthétique en staging. |
| Rollback — revenir en arrière, et ce que le rollback n'atteint pas | [infra-rollback.md](infra-rollback.md) | **Écrit ET RÉPÉTÉ le 2026-08-16** — le cycle complet épinglage → déploiement bloqué → libération a été joué sur staging (§12), et le tableau des trois champs de §2 est désormais une **mesure** et non un raisonnement. Écrit parce qu'**aucune procédure de rollback n'existait**, et parce que depuis [#393](https://github.com/Zaslons/myweli/pull/393) une fusion vers `main` déploie le backend en staging sans intervention humaine. Le geste d'urgence est `update-traffic --to-revisions` — quelques secondes, aucun build — mais **il s'annule tout seul** : les deux manifestes committent `latestRevision: true`, que le `replace` suivant réécrit, si bien que l'épinglage cesse silencieusement de tenir et que la révision dont on se protégeait redevient celle qui sert. C'est donc un garrot, jamais un correctif : l'étape 2 est toujours un `git revert`. Trois pièges consignés : **seul `status.traffic[].revisionName` dit la vérité** (après un épinglage, `spec.template…image` et `status.latestReadyRevisionName` désignent tous deux la mauvaise révision, et ils sont d'accord entre eux) ; les secrets sont résolus en `key: latest`, donc un rollback d'image ne restaure **pas** les anciennes valeurs ; et **le rollback n'atteint pas la base** — les migrations sont sans retour, sans instruction `down`. Il reste sûr aujourd'hui parce que `runMigrations` n'interroge que ses propres identifiants (une image ancienne ignore les lignes qu'elle ne connaît pas) et que les 31 migrations sont additives — désormais **vérifié par un test** plutôt qu'affirmé. Deux garde-fous ajoutés : le workflow refuse de lever un épinglage sans `unpin: yes`, et une migration destructrice doit se déclarer (`// rollback-unsafe:`). |
| Environnement de préproduction (staging) — conception | [infra-staging.md](infra-staging.md) | **Phases 1 et 2 livrées ; aucune ressource staging n'existe encore.** Répond à « comment développer une fois le produit public » : **$13–17/mois**, base de données SÉPARÉE (jamais partagée), données synthétiques par défaut et copie *anonymisée* de production pour répéter une migration. Trois changements de code devaient précéder toute ressource — `ENV` en énumération à trois valeurs (`guardsOn` ≠ `isProd`, car staging diverge dans les DEUX sens), le seed des salons de démonstration verrouillé sur `dev`, et la base d'API qui échoue en fermeture. Décisions : URL en `*.run.app` (le BFF rend l'argument cookie/CORS caduc), identifiants de bundle séparés reportés à la phase 8. |
| Durcissement de la production (phase 2) | [infra-prod-hardening.md](infra-prod-hardening.md) | **Livré.** Six défauts de production trouvés en auditant le projet GCP réel. **Trois étaient mal diagnostiqués et deux des correctifs proposés auraient provoqué une panne** — l'IP publique de Cloud SQL est porteuse (le proxy n'a pas `--private-ip`, Cloud Run n'a aucune sortie VPC), et patcher `max_connections` redémarre une instance ZONAL dont l'application ne sait pas se reconnecter, sur 0,6 Go de RAM. Le plus grave : FCM supprimait un jeton push sur **tout** 400, atteignable à distance via un `businessName` sans borne (T62). Deux commandes appliquées au projet vivant (protection contre la suppression, `ENCRYPTED_ONLY`) — aucun redémarrage, avant/après consignés. |
| G1 — the backend moves to Google Cloud (infrastructure) | [infra-gcp-migration.md](infra-gcp-migration.md) | **Draft** — the Render free Postgres appears to have expired (every DB-backed route 500s), so the database must be re-provisioned regardless — which makes this the one moment the migration carries **no data-migration cost**. Cloud Run + Cloud SQL + Cloud Scheduler + Secret Manager + Artifact Registry, deployed from GitHub Actions via **Workload Identity Federation** (no long-lived key). **R2 and Vercel deliberately stay** — zero egress matters when serving photos to West-African phones, and Vercel is the best host for Next. The move forces **three** correctness fixes that ship FIRST: `DATABASE_URL` has **no prod fail-fast**, so a Cloud Run revision missing it would serve a green API on in-memory repositories and lose every booking on recycle; `initializeDatabase()` runs migrations on boot, which was safe on one always-on Render instance and **races** under Cloud Run's horizontal scaling; and — found while writing the secret checklist — **production could not boot at all** with SMS deferred, since it demands Termii/Twilio credentials and rightly refuses the lying `log` provider, so `MESSAGING_PROVIDER=disabled` makes "messaging deliberately off" an explicit, honest posture (it reports every send as *failed*, so the outbox stays truthful) without weakening the accidental-misconfiguration guard. Also makes the two cron routes visible at last — they exist, are guarded by `CRON_SECRET`, and **404 silently when it is unset**, which is why reminders have never run. |
| Q1 — the funnel, proven against a real server (backend / CI) | [backend-q1-funnel-smoke.md](backend-q1-funnel-smoke.md) | **Built** — `docs/ROADMAP.md:191` says there is **no E2E vs a real backend**, and it is right: 656 backend `test()` calls and **not one opens a socket**. The only live-HTTP gate is the `backend-boot-smoke` CI job — 11 checks, **7 of them `grep` substrings**, **zero refusals**, and one of them drives the phone-OTP route that production **404s** (`render.yaml` sets `AUTH_METHODS=google,apple,email`), so CI is green on a configuration nobody runs. Q1 grew it into a **47-assertion funnel** on the same production build path: a salon is registered → is invisible while draft (row 82's three vocabularies, executable at last) → is refused by the go-live gate → completed → publishes → is found → booked → the slot is consumed → accepted → cancelled → the slot comes back; plus the six **required** security refusals over real HTTP (missing/invalid/**expired**/**replayed** tokens, lockout, **cross-tenant → 403** with a bystander). No product surface, no new seam — the dev `devCode` echo and the log email provider already exist, so **not one SMS is sent**. Measured: the job is **53 s** today against an 8-minute critical path, so the budget is real. |
| Chaîne d'outils — Flutter 3.38.6 → 3.44.9 | [mobile-flutter-3.44-upgrade.md](mobile-flutter-3.44-upgrade.md) | **Livré** — trois symptômes, une seule cause. Deux PR dependabot ne pouvaient **pas** fusionner (`sign_in_with_apple` 8.1.0 exige Dart ^3.11.0 ; `flutter_native_splash` 2.4.8 exige `meta ^1.18.0` alors que notre `flutter_test` épinglait `meta 1.17.0` — la résolution échouait avant toute compilation), et un `pubspec.lock` de dependabot réclamait un `meta` que ce SDK ne pouvait pas honorer : `pub get` le **réécrivait en silence**, donc la CI était verte contre un fichier de verrouillage qui n'était pas celui du dépôt. Mesuré et non déduit : `meta 1.18.0` arrive avec Flutter **3.44.0**. Le nouveau SDK a trouvé deux choses réelles : un lint qui ne pouvait pas se déclencher sous l'ancienne version de langage (Dart 3.12 autorise les paramètres nommés privés), et surtout **quatre surfaces sans aucun retour tactile** — le `ListTile` peint son encre sur le `Material` ancêtre, donc une boîte colorée entre les deux la cache : l'ondulation se peignait **derrière la carte** (§21 ligne 88). Les 46 goldens rebasés sur Linux : 12 **identiques au pixel**, et sur l'ensemble seulement **37 pixels** dépassent Δ100 — la signature de l'anticrénelage, rien qui bouge. |
| Soumission aux stores — signature de release et préparation | [mobile-store-submission.md](mobile-store-submission.md) | **Livré (côté dépôt)** — trois choses rendaient la soumission **impossible**, chacune avec un échec tardif qui ressemble à autre chose. (a) Les builds Android release étaient signés avec la clé de **debug** — le `TODO` du gabarit Flutter, intact : Play rejette à l'envoi et rien dans le build ne le dit. (b) L'app n'avait **aucun manifeste de confidentialité**, alors que les dix-huit SDK tiers du dépôt en fournissent un. (c) Pas d'`ITSAppUsesNonExemptEncryption`, donc chaque envoi s'arrête sur la conformité export. La règle encodée côté Android : **jamais de repli silencieux sur la clé de debug** — sans `key.properties` le build reste **non signé**, ce qui échoue bruyamment plutôt que de produire un artefact qui *paraît* livrable. `NSPrivacyAccessedAPITypes` est **vide**, et c'est mesuré et non recopié : `shared_preferences_foundation` et `sqflite_darwin` fournissent le leur, et `flutter_cache_manager` — pur Dart, donc compilé dans NOTRE binaire — décide de la fraîcheur via ses propres métadonnées sqflite, pas via `File.lastModified()`. Sur-déclarer n'est pas gratuit. Les types de données collectées restent **hors du manifeste** (décision du propriétaire) : App Store Connect est la source unique, avec une feuille de réponses dérivée du code en §4. R8 délibérément **pas** activé — son mode d'échec est à l'exécution et aucune barrière ici ne le voit. **Première archive de release du projet** (217,6 Mo) et premier `.aab` (64,0 Mo), tous deux vérifiés : manifeste présent dans le bundle, conformité export dans le plist construit, et **aucun `META-INF/*.RSA`** — non signé, pas signé-debug. |
| Mobile A15 — un titre d'`AppBar` qui ne tient pas (design system A15, ligne 79) | [mobile-a15-appbar-titles.md](mobile-a15-appbar-titles.md) | **Livré** — §13.3 exige que le texte se **replie** plutôt qu'il ne se tronque, et le titre d'une `AppBar` est le seul endroit du produit qui en est **structurellement incapable**. La ligne 79 disait « rien ne peut le mesurer » : **faux**, `didExceedMaxLines` vaut `true` sur un titre élidé et la branche existe déjà, désactivée par un défaut qu'aucun appelant ne surcharge. **Le banc d'essai était le premier défaut** : la matrice remettait ses sujets à `home:`, donc aucun bouton retour n'était dessiné et elle mesurait une barre **72dp trop large** — 13 des 16 sujets sont désormais poussés sur une vraie route. La règle est la **longueur** : **312dp au plafond de 1,34×**. Mais l'estimation « 63 titres / 14 perdants » de §2 est corrigée par la mesure en §2.1 — le corpus est lu dans `lib/` (**56 littéraux**, dont les quatre `helpText:` que le relevé à la main avait manqués, y compris l'exemple même de la ligne 79) et **4** titres perdaient réellement. **Et la moitié du problème n'était pas une longueur** : le libellé d'une action est mis à l'échelle par le facteur système entier, le plafond n'enveloppant que le titre — sur une barre porteuse d'action le titre n'est jamais le défaut, et trois actions textuelles deviennent icône + infobulle. Déclaration `// clip-ok:` avec règle d'orphelin et montage à quatre cas passant par la fonction livrée. |
| Le salon qui n'est pas en ligne — ce qu'il peut faire, et ce qu'on dit à chacun (ligne 82) | [salon-state-and-refusals.md](salon-state-and-refusals.md) | **Spécifié** — un seul code (`provider_suspended`) porte **deux statuts et quatre significations**, et **aucune des quatre surfaces** ne le traite : tout tombe dans une excuse générique, dont deux qui disent « Réessayez » pour un état que réessayer ne corrige jamais. Le pire cas est celui que **tout** salon rencontre — sa toute première réservation manuelle. Trois décisions : un salon en brouillon **possède son agenda** (`bookManual` ne refuse plus que `suspended`, ce qui fait **disparaître** le pire cas au lieu de mieux l'habiller) ; le code se **scinde** (`provider_not_published`), parce que le client mobile n'a même pas de champ `status` et ne peut structurellement pas trancher ; et la lecture publique se **ferme** pour `draft` et `suspended`, alignant enfin la route sur la promesse que le contrat fait déjà. Cinq phrases, où le temps porte toute la distinction : « pas encore » contre « ne … plus ». |
| **The design system — apps** (identity · tokens · components · a11y · forms · feedback · enforcement · the violations register) | **[SYSTEM.md](SYSTEM.md)** | **Canonical** — supersedes `DESIGN-STANDARDS.md`. Read before ANY UI work. |
| **The design system — web** (Tailwind mapping · semantic HTML · focus · ARIA · responsive/desktop · SEO · install push) | **[WEB-SYSTEM.md](WEB-SYSTEM.md)** | **Canonical** — supersedes `WEB-DESIGN-STANDARDS.md`. Shared rules live in SYSTEM.md. |
| Mobile A13 — deux coupures, une épingle et douze pluriels (design system A13) | [mobile-a13-copy-and-breaks.md](mobile-a13-copy-and-breaks.md) | **Livré** — la moitié mobile de la campagne que B9 a ouverte : §21 lignes 62, 71 et 41. La vérification a **corrigé trois des quatre lignes avant la moindre ligne de code**, et l'une des corrections change le correctif et pas seulement la prose : la carte « story » **se déclare `Semantics(button: true)` et son titre EST le nom accessible du contrôle**, donc §13.3 l'interdisait déjà — ce ne fut jamais une question de titre. Mesuré depuis le Roboto du SDK : `Week‑End` (trait d'union **insécable**, U+2011) déborde dès **1,20×**, et les trois titres cassent — remplacer le trait d'union n'en corrigerait qu'un sur trois, et un seuil à 1,3× arriverait trop tard. Une **largeur qui suit l'échelle** (`textScaledBound`) rend exactement 92,0 à 1×, donc l'image de référence à 1× est identique octet pour octet. L'en-tête du salon **s'empile** au-delà de ~1,6× : rétrécir le logo ne suffit pas (même le supprimer passe tout juste), et l'en-tête n'a **aucune marge verticale à 2×**. Ligne 41 : **12 défauts de pluriel**, pas 1, et le français met **0 au singulier** — la seule valeur où les quatre idiomes de l'app se contredisaient, ce qui explique qu'ils aient coexisté sans qu'on le voie. Et le « Client » de la ligne 41 n'était pas un trou de mock : le backend écrit `clientName: null` pour **toute** réservation faite depuis l'app, donc un salon voyait le placeholder pour la majorité de son carnet. Un nouveau `clientDisplayName`, pris sur la fiche client du salon (déjà anonymisée à l'effacement), **masqué avec le téléphone hors des jours travaillés**. |
| Mobile A12 — une liste qu'on a lue n'est pas un recensement (design system A12) | [mobile-a12-census.md](mobile-a12-census.md) | **Livré (D0→D9)** — A11 a clos sa tranche par un recensement de ce qu'elle n'avait PAS couvert (§21 ligne 68 : douze écrans plus `EmptyState`). La vérification a trouvé que **ce recensement avait été LU, pas balayé** : un balayage à parenthèses équilibrées donne **20** occurrences de la ligne non-flexée là où la ligne en nommait 9, et **16** boîtes à hauteur figée là où elle en nommait 2. Quatre de ses chiffres étaient faux (le `_StatCard` veut ~141dp et non 165 — calculé à la mauvaise taille de police), une entrée **contredisait la ligne 66**, et une nommait un écran qu'aucun utilisateur ne peut atteindre — supprimé, 325 lignes, zéro appelant. **Deux classes de défaut qu'aucune assertion du dépôt ne pouvait voir** reçoivent enfin une règle et un instrument : l'étiquette flexée écrasée sous un préfixe lisible (`CompactAppointmentTile` affichait le nom d'un salon comme **rien qu'une ellipse**, 0 caractère, sur deux écrans gardés depuis C5) et la boîte à hauteur figée autour du texte — une phrase de §13.3 depuis A5, jamais appliquée. Le seuil de lisibilité est **mesuré** (mode rapport d'abord, puis 8, strictement entre le pire défaut à 7 et le meilleur cas légitime à 9 : une marge d'un caractère, consignée telle quelle). §21 ligne 60 close : **6 fichiers sur 11 → 11 sur 11** épinglent un téléphone. Les instruments se sont trompés **trois fois** avant d'être justes ; chaque faux positif est désormais un sujet permanent de `primitives_test.dart`. Puis l'appareil et une revue adverse ont trouvé **cinq défauts de plus**, et la forme de la queue est le vrai constat : **chacun n'était atteignable que par un instrument dont le balayage ne disposait pas** — un vrai téléphone, un bouton qu'aucun test ne pressait, un écran qu'aucun sujet ne montait. **18 défauts corrigés · 1009 tests · 34 images · registre §21 lignes 60, 66, 68 corrigées, 69–73 ouvertes ou closes.** |
| Mobile A11 — une largeur n'est pas toutes les largeurs (design system A11) | [mobile-a11-width.md](mobile-a11-width.md) | **Livré (C1→C8)** — §10 définit `compact` comme une **plage** (`< 600`, sans plancher), et le code l'a lue comme un **point** : 390. `golden.dart:40-41` écrit le raisonnement — *« les apps n'ont pas de points de rupture, donc c'est la seule surface qui compte »* — et **l'inférence est fausse** : une mise en page qui ne peut PAS s'adapter doit fonctionner sur toute la plage, précisément parce qu'elle ne s'adapte pas. Trois mesures : **`test/a11y/` n'épingle aucune surface** (tout y est testé à 800×600, plus large que n'importe quel téléphone), **§20 n'a aucune ligne Layout** (§10 est la seule section substantielle sans garde-fou — exactement le diagnostic de la ligne 35 pour §17), et **`contentMaxWidth` a 1 mention dans la doc et 0 usage dans le code**. Le web a fermé la question dès B0 (§15 ligne 1, *« Layout works at every width »*) ; le mobile a hérité de la réponse sans poser la question. Plancher fixé à **360dp** — l'appareil médian du parc visé (`PRD.md:126`) **Résultat** : neuf sujets gardés sur `{360, 375, 390} × {1×, 2×}` (27 rouges sur 48 au départ), quatre widgets partagés, `contentMaxWidth` enfin dans le code et dans le thème web, §13.3 étendue à la coupure au milieu d’un mot — et **un vrai appareil de 360dp**, le premier du projet, qui a trouvé trois débordements de plus (étiquette de bouton, ligne « Pas encore de compte ? », listes déroulantes sans `isExpanded`) qu’aucune image ni aucun test ne pouvait voir, parce que le bandeau de débordement de Flutter n’existe qu’en debug. **884 tests · 33 images · registre §21 lignes 51–68.** |
| **L1 — la surface légale** : quatre documents, le premier `<footer>` du site, et la suppression qui ne supprimait pas | [legal-l1.md](legal-l1.md) | ✅ **Livré** — bloqueur de lancement #5. Les deux stores exigent une URL de politique de confidentialité et (Google Play, depuis 2023) une URL de suppression de compte : **aucun document légal n'existait dans aucune surface**. Trois écrans affichaient déjà « vous acceptez nos conditions d'utilisation » en **texte mort**, pointant vers des documents inexistants — et l'inscription **pro** n'avait **aucune** mention de consentement, alors que c'est le parcours qui précède l'envoi de pièces d'identité (KYC). `WEB-SYSTEM.md` §4 impose un repère `<footer>` qui **n'existait nulle part**. Ship : Politique de confidentialité · CGU · Mentions légales · Suppression de compte |
| L1b — l'effacement de compte : ce que « supprimer » voulait vraiment dire | [account-deletion-erasure.md](account-deletion-erasure.md) | ✅ **Livré** — `DELETE /me` renvoyait 204 en laissant l'essentiel : les **avis gardaient le nom** de la personne, les rendez-vous son nom, son téléphone et ses notes, les captures d'acompte n'étaient **jamais** effacées, et surtout **`device_tokens` survivait — le téléphone d'un compte supprimé continuait de recevoir des notifications.** Bug réel, pas question de formulation. Corrigé ici parce que la politique de confidentialité le décrit : **enfants d'abord, identité en dernier**, chaque étape idempotente, donc un échec laisse un compte vivant que l'on peut re-supprimer |
| Mobile A10 — the clock seam: the screens that could not be photographed (design system A10) | [mobile-a10-clock.md](mobile-a10-clock.md) | ✅ **Shipped** — §21 row 23 → **0**, rows 38–43 added. — §21 row 23. **The row is a copy of a code comment and inherited its errors**: `MockProService.getDashboard()` does not exist, and the "weekly stat cards" it blames are never rendered (`weekRevenue` is computed and read by no screen). The journal's header is stable too — the real flake is the **week strip**, seven day-numbers and a highlight that moves one slot every day. It is also a large undercount: `earnings_screen` is the screen the row's description actually fits, and two goldens that **already exist** contain clock reads, stable by luck rather than design. `DateTime.now()` counts 106, but that grep is the wrong instrument — **19 render-path sites carry no such token**, calling the seam with `now:` omitted. The seam shape was **reversed mid-plan**: `package:clock`'s zones miss field initializers, timers created outside the wrap, and `static final` mock seeds — a function pointer reaches all three. Shipped with **four** new goldens (`pro_dashboard`, `pro_journal`, `pro_journal_day`, `pro_earnings`) and the two consumer ones frozen, proven under **three** frozen instants — including a month-edge run, because byte-identity alone turned out to prove nothing for a screen whose value did not move. The adversarial review found a **product bug the new golden had pinned**: `earnings_screen` queried with the account id, not the salon's (row 42) |
| Mobile A9 — French everywhere: the framework's English, a calendar in English, and a booking date that books the wrong day (design system A9) | [mobile-a9-french.md](mobile-a9-french.md) | ✅ **Shipped** — §21 row **29 → 0**, plus new rows **34–37**. The row read as a screen-reader footnote; measured, **a French user typing `07/01/2026` in the booking date picker booked 1 July** — `parseCompactDate`'s own source says *"Assumes US mm/dd/yyyy format"*, and no parameter overrides it. **The consumer booking calendar already rendered "July 2026"** because `Intl.defaultLocale` is never set — and the delegates do nothing for it, since `table_calendar` reads `Intl`, not `Localizations`. Plus **51 `AppBar`s** saying "Back". Two mechanism findings: iOS takes the selection toolbar from `CupertinoLocalizations` (Material delegates alone leave it English on every iPhone), and 24h time needed **no code**. The **plural** `.delegates` is mandatory — the singular pairing turns 8 of 11 assertions red. Found on the way and unrecorded anywhere: **eight** status vocabularies with an English enum leaking in admin, and §17 having **no typography rule and no gate** — the only section with neither. Six things the slice got wrong first, each named in the spec; the typography sweep even rewrote its own pin, and a failing test found a hole the green pin could not see |
| Mobile A8 — motion: five tokens, and the app finally listens when the OS says stop (design system A8) | [mobile-a8-motion.md](mobile-a8-motion.md) | ✅ **Shipped** — §21 rows **8 · 20 → 0**. Both figures in row 8 were wrong in opposite directions (the "12 durations" counted mock latency and OTP cooldowns; "0 constants" was false); the real set was **7 motion sites, 6 values**, all now `AppMotion`. Rows 8 and 20 turned out nearly **disjoint** — the vestibular harm was `repeat()` and involuntary movement, none of it a counted number. §9's rule was an **iOS no-op** as written (the framework reads `reduceMotion` nowhere), so A8 honours both flags and adds §9.1. Gates were written **before** each sweep and watched fail. Two things it refused to ship: a fix for the story-dwell bug (two gates for it would not go red) and a frozen-Lottie loader (the golden showed it renders an empty frame). New: register row **33**, the framework-side iOS gap that is not ours to close. **The adversarial review then found three defects in A8's own work** — a crash in the story reel (`Duration.zero` asserts on `PageController`), a splash that rendered blank for 3800ms, and a caption overflowing an avatar by 44px — all in reduced-motion paths the four gate commits never pumped, because every one of them pumped `BrandLoader`. Gate-first is not coverage |
| Mobile A7 — field-anchored errors: an error belongs to its field, not to a toast (design system A7) | [mobile-a7-forms.md](mobile-a7-forms.md) | **Shipped** — the census disproved the row: the single `errorText` caller is dead code (zero working field-anchored errors) and the dominant pattern is the **silent disabled button ×22**, not the toast. One `FieldErrors` (validate on submit, re-validate once errored, merge, server faults under their field) feeding `errorText`; one canonical `Validators` (5 e-mail regexes → 1, OTP 4→6 digits, the unvalidated Mobile Money number); strict §14 rule 5; focus follows the first error; closes SYSTEM §21 row 19 |
| Mobile A6 — the feedback consolidation: one `AppSnackBar`, one `ConfirmDialog`, the confirm ladder (design system A6) | [mobile-a6-feedback.md](mobile-a6-feedback.md) | **Shipped** — 117 snackbar calls behind one entry point (§15's 3/6/10s durations + a second greyscale-surviving cue) and all 13 `AlertDialog`s behind one `ConfirmDialog` (cancel-gets-focus, verb labels, type-to-confirm); the first undo affordances; the modal-blocked snackbars become inline errors; closes SYSTEM §21 rows 17 · 18 |
| Mobile — the mirror-debt close: gold ≥ 3:1 on every surface + the reading paragraphs at 16 (design system, mobile A-series) | [mobile-gold-reading.md](mobile-gold-reading.md) | **Shipped** — the two rows the web burn-down surfaced from the outside: `gold #B8860B` → `#B5830A` (it read 2.98:1 on `surfaceVariant`, the darker surface A1's contrast test never measured) mirrored to `tokens.ts`; the salon description + empty-state body `bodyMedium`→`bodyLarge`; closes WEB-SYSTEM §15 row 23 + SYSTEM §21 row 27 |
| Web B3 — the token mirror gate: Dart parser + blocking comparator + `gen:tokens` (design system B3) | [web-b3-token-mirror.md](web-b3-token-mirror.md) | **Built** — `scripts/dart-tokens.mjs` parses the Flutter theme as text (self-checked counts), `tests/tokens.mirror.test.ts` blocks CI on ANY mirror drift (5 Dart families + motion/zIndex doc-pins), `npm run gen:tokens` prints the healing blocks; drifts 4/5 (`warningLight`/`infoLight`) closed; closes WEB-SYSTEM §15 row 19 |
| Web B7 — the desktop-grade pro dashboard: Card/StatusChip/DataTable, the two-pane Aujourd'hui, contentMaxWidth (design system B7) | [web-b7-desktop.md](web-b7-desktop.md) | **Built** — the product's first `xl:`/`2xl:` layouts: two-pane dashboard (+ the pendingRequests parity fix), four DataTable conversions (Équipe/Clients/Revenus/Catalogue), journal ←/→/T shortcuts + full-width column stretch, kind-tinted StatusChip everywhere, `max-w-content` applied per §10 with the exclusion list; closes WEB-SYSTEM §15 rows 18 · 7j |
| Mobile A14e — bloquer une semaine en un geste, et l'effacement que la forme rend impossible (design system A14e) | [mobile-a14-pickers.md](mobile-a14-pickers.md) | **Livré** — bloquer quatorze jours coûtait quatorze réécritures complètes de la disponibilité du salon, et une plage ne pouvait pas exprimer « tous les dimanches d'août ». Le sélecteur renvoie un **delta**, pas un ensemble : le `firstDate` étant l'aujourd'hui du salon, une date bloquée passée ne peut pas figurer dans une sélection, donc un ensemble complet les **supprimerait toutes**, en silence et définitivement, sans que rien ne s'affiche. Rougi deux fois et muté deux fois, dont une mutation qui fait rougir un **autre** test pour prouver que la paire ne teste pas deux fois la même chose. La confirmation change de côté — retirer confirmait moins qu'ajouter, alors que c'est débloquer qui produit une réservation non voulue. Et la question ouverte d'A14b est mesurée : la rangée `_PickerField` **déborde de 49 pixels** dès qu'une date est choisie. |
| Mobile + web A14d — la fenêtre réservable, décidée par le salon et imposée par le serveur (design system A14d) | [mobile-a14-pickers.md](mobile-a14-pickers.md) | **Livré** — la ligne 76 demandait jusqu'où à l'avance un salon accepte des réservations ; il n'existait **aucune règle serveur**, et cadrer la réponse a montré que le bord **proche** avait le même défaut — un préavis de `60` nu, sans constante ni test, redoublé dans le mock mobile. Les deux bouts partent ensemble, avec des valeurs par défaut qui **ne changent aucun agenda** le jour de la livraison. Le préavis a forcé une restructuration : la règle d'avant ne pouvait **structurellement pas** exprimer plus d'un jour. Quatre pièges regardés rouges — deux listes blanches qui effacent un champ en silence de part et d'autre du fil, un `beyond_horizon` qui serait parti en **400**, et un `SlotPicker.horizon` qu'**aucun des deux points d'appel ne passait** depuis A14c. Un jour vide a gagné **quatre raisons** avec un saut en un geste ; le web a d'abord dû retrouver son **quatrième état** (`fetchSlots` réduisait toute panne à `[]`) et six phrases ont cessé d'affirmer qu'on venait de prendre votre créneau. |
| Mobile A14c — le calendrier que personne n'avait jamais mesuré, et le paquet qui refusait de grandir (design system A14c) | [mobile-a14-pickers.md](mobile-a14-pickers.md) | **Livré** — la ligne 75 avait raison sur les deux points, et la raison de l'angle mort tient en une ligne : Calendrier est l'onglet **par défaut**, et les deux instruments tapaient vers « Liste » à leur deuxième instruction. « lun. » réclamait **20,0 dp dans 16,0**, à **1×**. La conversion a révélé que la page n'avait pas la place d'un calendrier honnête (40 px de débordement, et c'était de la **largeur**), que le tirer-pour-rafraîchir était mort les jours creux, et qu'un salon sans rendez-vous **ne pouvait pas ouvrir son calendrier**. Plus trois défauts dans notre propre grille, dont une formule d'échelle fausse dans **trois fichiers** qu'aucune épingle ne pouvait voir. Côté consommateur : deux écrans supprimés, `SlotPicker` partagé, le **quatrième état** enfin distinct, et le report qui perdait la durée de la réservation. `table_calendar` retiré ; la couture `Intl.defaultLocale` conservée mais désormais défendue par une épingle. |
| Mobile A14b — le sélecteur qui refuse de grandir, et le portail qui ne pouvait pas le voir (design system A14b) | [mobile-a14-pickers.md](mobile-a14-pickers.md) | **Livré** — défaut **différent** de la ligne 73, pas son jumeau : le sélecteur d'heure de Material **ne coupe pas**, il **refuse de grandir**. `TextScaler.noScaling` **en dur** sur l'heure et les minutes (`time_picker.dart:387`), boîte plafonnée à **1,1×**, et des chiffres de cadran qui grandissent jusqu'à 2× sur des anneaux distants de **28 dp fixes** — soit une ligne de 48 dp sur 28 dp d'écart (chevauchement de 20 dp **à 12 h et 6 h** ; deux brouillons ont écrit « à chaque position » et « ~4 dp d'encre », tous deux surévalués et corrigés). **Pire qu'une coupe : une coupe se voit.** Et **aucune assertion du dépôt ne peut échouer dessus** — le cadran est un `CustomPaint` dans un `ExcludeSemantics` sans un seul `RenderParagraph`. Le thème n'y accède pas : `dialSize` apparaît **zéro fois** dans `time_picker_theme.dart`. **Trois contrôles** (feuille, plage, combiné), six points d'appel, **deux** états d'erreur supprimés parce que la contrainte devient exprimable — dont un `return` **silencieux** après deux fenêtres modales. Un troisième **survit** : le dossier disait qu'il mourrait, mais l'horloge avance pendant que le formulaire est ouvert. Portail rougi en plantant un `24.0` puis un `noScaling`. **Six premières photos d'un sélecteur d'heure**, qui ont trouvé une date coupée **au milieu du jeton** (« 11/03/2 » / « 026 ») que le portail ne voyait pas — il ne regardait qu'un contrôle sur trois. Les **quatre dettes d'A14a** soldées. |
| Mobile A14a — notre propre calendrier, et le pixel et demi qui a décidé de sa forme (design system A14a) | [mobile-a14-pickers.md](mobile-a14-pickers.md) | **Livré** — la ligne 73 (le sélecteur de Flutter affiche « 2 21 2 2 2 2 2 » à 200 %, trouvé **sur un appareil**) est **juste**, ce qui est rare ici. Le portail, écrit et rougi avant tout code, l'a mesurée plus finement : **1,5 dp**, et **seulement à 360** — 375 et 390 passent. Le plein écran ne rapporte que ~0,9 dp ; ce qui corrige, c'est de **posséder la cellule** : un cercle doit rester carré et réclame **48 dp à 2×** contre 46,9 de colonne, donc la cellule est un **rectangle arrondi**. Compromis consigné, déjà pris par `_WeekStrip` et la ligne 7h du web : **sept cibles de 48 dp ne tiennent pas dans 360**, donc hauteur plancher 48, largeur contrainte, cellule entièrement tactile. `expectTokensWhole` existe parce qu'aucun assistant voisin ne pouvait voir un chiffre manquant — le plancher de 8 caractères passe au-dessus d'un jour à deux chiffres — et il est **prouvé faillible dans trois directions**. Cinq points d'appel convertis, cinq horizons contradictoires nommés (et **aucune règle serveur** pour les arbitrer). Première photo d'un sélecteur du dépôt, 1× et 2×, plus une re-vérification **sur le matériel d'origine**. |
| Web B12 — l'attente qui ne pouvait pas se calmer, et le scan qui ne vérifiait rien (design system B12) | [web-b12-axe-anchors.md](web-b12-axe-anchors.md) | **Livré** — une PR **mobile** est passée au rouge sur `Web — e2e` : `axe.spec.ts` expirait sur `page.waitForLoadState('networkidle')`, et `main` était vert. B10 avait clos la ligne 30 sur « 0 » ; le job a rougi **trois fois depuis** (deux sur B11, une sur A14b), toujours le même test. `next.config.mjs` autorise `cdn.stub` dans `remotePatterns`, donc `next/image` émet un `/_next/image` **de même origine** et c'est le **processus Next** qui résout l'hôte injoignable, sans `AbortSignal` ni délai (`image-optimizer.js:722`) — de sorte qu'aucun `page.route` sur `cdn.stub` ne peut l'intercepter. **Consigné comme plausible, non prouvé** : Playwright impute un dépassement à l'appel en cours, et « le journal était inondé d'ENOTFOUND » est une preuve faible puisque **chaque exécution verte l'inonde aussi**. **Le second constat est le pire** : le portail axe n'avait **aucune ancre de contenu sur ses 19 routes** — une 404 ou un `ErrorState` aurait été scanné, trouvé propre, et validé (B8 avait déjà pris ce défaut ailleurs : une route mesurait la page 404 depuis des mois). `ErrorState` rendant le `<h1>` de la page, même un titre ne suffit pas — c'est `role="alert"` qui discrimine. Corrigé des deux côtés : trafic image intercepté (image 1×1 **remplie**, pas avortée, pour que la mise en page ne bouge pas), chaque route attend une ancre **reprise d'un spec déjà vert**, avec un **second étage** là où la page peint son titre avant son contenu — `networkidle` attendait implicitement les deux. Deux ancres devinées à tort, corrigées par le vidage d'arbre d'accessibilité de l'échec plutôt que par une seconde supposition. `trace: 'retain-on-failure'` ajouté : avec `retries: 0`, une exécution rouge est le seul regard qu'on ait, et celle-ci n'a laissé qu'une pile. **166 tests verts, deux fois de suite ; le spec axe passe de 30 s d'expiration à 22,5 s.** |
| Web B11 — 320 pixels, et les pages que le portail n'ouvrait pas (design system B11) | [web-b11-reflow.md](web-b11-reflow.md) | **Livré** — le web n'avait **aucun** contrat de reflow (0 occurrence de 200 %, 1.4.10 ou zoom ; rien n'était jamais descendu sous 375 px). « Ajouter 320 à la matrice » n'en est **pas** un : la clause « sans perte d'information » restait vide parce que le garde **saute** `text-overflow: ellipsis`, donc dix sites coupaient du texte en passant au vert. Matrice à trois fenêtres (un `describe` chacune — `test.use` est une déclaration), plus deux gardes de **perte** : coupe verticale (`hidden`/`clip` seulement — sur le web `visible` peint **en dehors** de la boîte, rien n'est détruit ; porter le prédicat de Flutter tel quel aurait rougi sur chaque page longue) et troncature **réelle** (ellipse **et** débordement, jamais la classe seule). Les deux étaient verts d'emblée — et c'était la découverte : les quatre pertes vivaient sur des routes jamais ouvertes. Six routes ajoutées, dont le **tunnel de réservation**. Résultat : les tuiles du tableau de bord donnaient 46 px à « Demandes en attente » (cassé aussi à 375, depuis toujours) ; l'e-mail du roster, seule identité de la ligne, coupé de 213 à 108. Chaque troncature restante est une décision écrite (`// clip-ok:`, épinglée sur l'**AST** — la prose sur la troncature n'est jamais signalée). Le bouton des notifications s'arrêtait au tiers de sa course à 24 px de racine : son `ds-ignore` calculait **en px** sur trois opérandes **rem**. Ligne 26 fermée, ligne 29 à moitié, deux lignes ouvertes (migration `rem` ; géométrie px du journal). |
| Web B10 — un portail qui ne parlait plus que de la météo (design system B10) | [web-b10-flake.md](web-b10-flake.md) | **Livré** — WEB-SYSTEM ligne 30 disait trois choses et la mesure en a démenti trois. Le « 2 sur 3 » n'existe qu'**en local**, où `reuseExistingServer: !CI` laisse survivre le stub d'une exécution à l'autre : trois passages contre le même processus donnent **3 → 14 → 16** échecs, avec des erreurs qui sont de l'**état** et non du minutage. En configuration CI le vrai chiffre est **5 rouges sur 29**, et **deux tests seulement** sont réellement instables. « Un `await` manquant » est **faux** pour les deux suspects — ils attendent déjà. Les vraies causes : un **bug produit** (`BookingFlow.tsx` ne gardait que `loadSlots`, alors que `settle` et `onVariant` écrivent aussi après un aller-retour — un clic tombant dans la fenêtre était annulé) et un **conflit d'écriture entre deux fichiers** sur l'unique offre du stub, où `toHaveURL` interrogeait quatorze fois une URL **stable** : un 403, qu'aucune attente ne corrige. Correctifs à la racine, **sans `test.slow()`, sans délai relevé, sans réessai** ; le serveur Next n'est plus réutilisé non plus, parce que le réutiliser **saute `npm run build`** et teste donc silencieusement la version précédente. `retries: 0`, mesuré sur **17 exécutions de l'arbre livré — 16 vertes, 1 rouge, nommée et non réessayée** — et par l'expérience ciblée : **84 exécutions, 1 échec → 0**. **Deux** assertions incapables d'échouer réparées, dont `\(\d+\/3\)` — **`\d+` accepte `0`**. Et la revue adverse a rattrapé une **régression introduite par le correctif lui-même** : un compteur de génération unique abandonnait le verdict du serveur sur l'heure en même temps que l'auto-avance, ce qui conservait un créneau que le salon ne peut plus honorer — pire que le défaut d'origine. Corrigé en **deux** compteurs aux responsabilités disjointes. |
| Web B9 — cinq copies d'une bande d'onglets que personne ne possédait (design system B9) | [web-b9-tabs.md](web-b9-tabs.md) | **Livré** — §21 ligne 54 et WEB-SYSTEM ligne 28 décrivent le même défaut dans deux registres, et **les deux se trompent** : la raison annoncée (« le portail ne parcourt que `PUBLIC_ROUTES` ») est **fausse** — le spec s'authentifie déjà et se tient sur `/pro/rendez-vous`, la page qui porte la première bande, en n'y vérifiant qu'une `line-height` ; et les deux registres ne s'accordent pas sur le compte (5 contre 2). Cinq bandes **identiques au caractère près**, parce que WEB-SYSTEM §10 **ne décrit aucun composant d'onglets**. Un `<Tabs>` partagé qui **enveloppe** (pas de défilement : sur desktop rien ne suggère un onglet hors écran), le plancher de 48px que les cinq rataient pendant que la ligne 7h annonçait « 0 restant », et la matrice de débordement étendue aux routes authentifiées **avant** que le composant n'existe. **Mesuré, et non calculé** : un seul débordement réel (bande n°2, **340px pour 327** — 13px, pas les ≈41 que l'arithmétique prédisait), quatre latentes ; les boutons à **38px** ; et **la bande la plus cassée passait le contrôle de cible tactile à 56px** — le débordement forçait un libellé à revenir à la ligne et `align-items: stretch` étirait ses voisins. Corriger le débordement seul aurait fait rougir un sujet vert. Le recensement des connexions dupliquées annonçait quatre copies : il y en avait **neuf**. |
| Web B8 — the reading-text resize: bodyLarge where you read, 16px where you type, 14 where you scan (design system B8) | [web-b8-reading-text.md](web-b8-reading-text.md) | **Shipped** — row 17's three-way correction (308 not 356 · bodyLarge at ZERO not once · mobile itself reads paragraphs at 14): the HYBRID split (reading copy → 16, operator density stays 14 by doctrine) + ALL inputs type at 16 (M3 parity, iOS stops zooming); mobile's own 14px paragraphs become a new SYSTEM §21 row |
| Web B6 — the shared-components slice: Loading/Skeleton, EmptyState/ErrorState, Icon, Rating, Chip (design system B6) | [web-b6-components.md](web-b6-components.md) | **Built** — the four states get their primitives (35 loaders → Loading/Skeleton per §12's shape rule; ~20 empties → EmptyState anatomy; 12 dead-end errors gain REAL retry + the missing h1s); `<Icon>` unifies the svg channels (row 7i's honest recount); `<Rating>` brings the French comma; `<Chip>` closes §16's borderStrong violation across ~24 pills; OtpLoginForm deleted; closes WEB-SYSTEM §15 rows 16 · 7i |
| Web B5 — live regions, the focus-trapped `<Modal>`, honest headings, axe as a gate (design system B5) | [web-b5-feedback.md](web-b5-feedback.md) | **Built** — `<Toast>`/`useToast` (§15 durations, persistent `role="status"` region) + the full §7 sweep; `<Modal>` (trap/Escape/restore/scroll-lock) over all 6 dialogs; `/recherche` count h2 + `/mon-compte/[id]` h1; stars → real radio group; FilePick sr-only; `@axe-core/playwright` over 12 routes + Lighthouse a11y ≥ 0.95 blocking; closes WEB-SYSTEM §15 rows 12 · 13 · 14 · 15 · 7f · 21 · 22 |
| Web B4 — the focus ring, `<TextField>`, and the 48px floor (design system B4) | [web-b4-controls.md](web-b4-controls.md) | **Built** (#257) — `borderFocus` + §5's ring + skip link; `<TextField>`/`PhoneField` shells + `useFieldErrors` (§14); Button `text`/`isLoading` + `min-h-12`; the 48px sweep; jsx-a11y strict 16→0, zero disables; closes WEB-SYSTEM §15 rows 3 · 8 · 9 · 10 · 11 · 7h |
| Web pro mobile nav — the sidebar goes off-canvas (design system B0) | [web-pro-mobile-nav.md](web-pro-mobile-nav.md) | **Built** — persistent sidebar at `lg+`, off-canvas drawer below (one `<ProSidebar>`, CSS-repositioned; scrim/Escape/focus-restore/`inert`); fixes the 240px-on-a-375px-phone bug; closes WEB-SYSTEM §15 row 1 |
| Multi-pays — the end version, built (program MP1→MP3) | [multi-pays-end-version.md](multi-pays-end-version.md) | **COMPLETE — MP1 backend (#235) + MP2 mobile (#236) + MP3 web built** — locality tree + /localities on every surface, per-salon timezone (package:timezone through all day-math + all three clients), per-salon currency stamping + threading, operator catalog end-to-end, nested SEO tree (`/coiffure/abidjan/cocody`) + 308 redirects, locality pickers writing areaId; user decision 2026-07-14 |
| Salon time — the timezone seam (multi-pays slice 1) | [timezone-salon-time.md](timezone-salon-time.md) | **Built** — salon-time rule behind the `Africa/Abidjan` seams (`web/lib/time.ts` + `core/utils/salon_time.dart`), leak fixes (incl. the app earnings twin) + sweep + grep pins, « heure du salon » hint (consumer), TZ-pinned harnesses + Render, currency-param formatters (« FCFA » unified); end version in [modules/multi-pays.md](../modules/multi-pays.md) |
| Auth overhaul — Google + Apple + Email OTP (phone → contact) | [auth-social-email.md](auth-social-email.md) | **Building** — P1 backend built (verifiers · email OTP · migration 0022 · AUTH_METHODS gate); web → mobile → pro next |
| Pro auth — Google + Email OTP for salons (P4) | [pro-auth-social.md](pro-auth-social.md) | **Built** — login-only social + identity-inline registration (backend + pro app + pro web) + AUTH_METHODS gate on pro phone |
| Clients C1 — salon client base (list · card · notes · tags · badge) | [clients-c1.md](clients-c1.md) | **Built** — backend + web + app (#184/#185/C1c); first slice of [modules/clients.md](../modules/clients.md) |
| Journal J1 — the web journal grid (+ « Client arrivé ») | [journal-j1-grid.md](journal-j1-grid.md) | **Built** — backend (#188) + web « Journée » grid; C2 mini-card inside |
| Journal J1b — the pro-app day timeline (« Ma journée ») | [journal-j1b-app.md](journal-j1b-app.md) | **Signed off** — mobile-first equivalent of the web grid; app-only (backend live); flutter_slidable + timeline-default |
| Multi-chair capacity + web booking hub | [booking-capacity-web-hub.md](booking-capacity-web-hub.md) | **Built** — per-artist slot engine (K1) + the order-free web booking hub porting the app flow (K2) + app artistId fix (K3) |
| Web pro registration + identity matrix | [web-pro-registration.md](web-pro-registration.md) | **Built** — /pro/inscription (Google/email identity + business fields), sign-in=sign-up copy, verified auth matrix documented |
| Web manual booking (multi-service) | [web-manual-booking.md](web-manual-booking.md) | **Built** — the app's manual-booking flow on web: multi-service dialog + note + SMS seam, standalone date/time, rendez-vous header + client-card entry points |
| Web pro reviews (« Avis ») | [web-pro-reviews.md](web-pro-reviews.md) | **Built** — the pro app's ReviewsScreen on web: summary card (average + 5→1 distribution) + review cards + « Charger plus »; sidebar entry |
| Web pro KYC (« Vérification ») | [web-pro-kyc.md](web-pro-kyc.md) | **Built** — the pro app's KYC flow on web: status banner + four document tiles (private direct-to-storage upload) + submit; Profil entry with status hint |
| Team access R6 — multi-salons | [team-access-r6-multi-salons.md](team-access-r6-multi-salons.md) | **Built (R6 complete)** — R6a backend (`?salonId=` selector, GET/POST /me/salons, KYC badge fan-out, deletion-all) · R6b app (switcher + add-salon) · R6c web (sidebar switcher + validated salon cookie + epoch remount) |
| Web team access R5 — /pro/equipe, invitation bridge, offer picker, role-shaped web | [web-team-access-r5.md](web-team-access-r5.md) | **Built** — R5a (owner surfaces: équipe table, 202 login bridge, offer picker, publish `offer` key, catalogue rename) · R5b (membership context, capability sidebar, own-calendar, slim member profil, revoked sign-out) |
| Team access R4 — the role-shaped pro app | [team-access-r4-role-shaped-app.md](team-access-r4-role-shaped-app.md) | **Built** — R4a backend (membership-aware /me/provider, T40 own-scope + off-day contact masking, not_a_member) · R4b app (role-gated dashboard/profil, staff 3-tab « Ma journée » shell, revoked sign-out) |
| Team access R3 — the pro app's team & offers UI | [team-access-r3-app.md](team-access-r3-app.md) | **Built** — Équipe screen (invite/rôles/révocation), login « Invitations » step (202 bridge), authed invitations, offer picker (Pro/Business/Réseau, 3 mois offerts), publish-gate `offer` mirror |
| Team access R2b — invitations & the Équipe API | [team-access-r2b-invitations.md](team-access-r2b-invitations.md) | **Built** — the invitation lifecycle, the login bridge (202 + bare member accounts), branded emails, audit, T36–T40 |
| Team access R2a — salon offers & billing states | [team-access-r2a-offers.md](team-access-r2a-offers.md) | **Built** — provider_subscriptions (Pro/Business/Réseau, 3 mois offerts, grace→unpublish, seats), cron warnings, admin « marquer payé », publish gate, /me/subscription compat |
| Team access R1 — membership foundation | [team-access-r1-foundation.md](team-access-r1-foundation.md) | **Built** — provider_members + capability presets (4 roles) + per-request resolution; ownership checks re-plumbed; zero UX change |
| Pro account deletion + export (11.5) | [pro-account-deletion-export.md](pro-account-deletion-export.md) | **Built** — DELETE /me/provider (future-bookings gate, salon unpublished, identity erased) + client-side export on both surfaces |
| App P3 polish batch (final parity batch) | [app-p3-polish.md](app-p3-polish.md) | **Built** — Spécialiste on the consumer detail, gap-slot artist prefill, photo reorder, real Aide & Support |
| Web P3 polish batch | [web-p3-polish.md](web-p3-polish.md) | **Built** — calendar/proof/notes/spécialiste on the detail, gallery lightbox, salon visits + review invite, search hearts, artist photo, custom tags, support entry |
| Appointments & auth parity (P2c) | [appointments-auth-parity.md](appointments-auth-parity.md) | **Built** — real « Appeler »/WhatsApp on both details, « Client arrivé » on both detail pages, email-code resend with cooldown everywhere |
| Review photos end-to-end + « Signaler » (P2b) | [reviews-photos-reporting.md](reviews-photos-reporting.md) | **Built** — consumer `review` upload purpose (backend), app photo-upload fix + « Signaler » on both surfaces, web photo submit/display |
| Web search + funnel parity (P2a) | [web-search-funnel-parity.md](web-search-funnel-parity.md) | **Built** — /recherche « Trier » + « Disponible aujourd'hui », booking notes, mobile sticky confirm bar, service duration variants |
| Web notifications + « Revenus » (parity P1c) | [web-notifications-revenus.md](web-notifications-revenus.md) | **Built** — consumer notification center + préférences (/mon-compte/notifications + header bell) and the pro « Revenus » page (period tabs + ledger) + week card |
| Consumer web discovery map | [web-discovery-map.md](web-discovery-map.md) | **Built** — /recherche split view: list left + sticky Leaflet/OSM map right, two-way sync, popup mini-cards, « Autour de moi », mobile « Carte » toggle |
| Pro salon lifecycle (draft → publish) | [pro-salon-lifecycle.md](pro-salon-lifecycle.md) | **Built (B1 backend + B2 web)** — registration provisions a linked DRAFT salon (+ /me/provider self-heal for stuck accounts); « Mettre en ligne » gate (profile · ≥3 services · ≥3 photos · hours); drafts invisible publicly (T50/T51) |
| Journal J1b — the pro-app day timeline (« Ma journée ») | [journal-j1b-app.md](journal-j1b-app.md) | **Built** — « Ma journée » default view; swipe + long-press actions, gap slots, week strip |
| App consumer auth — Google + Email OTP, Apple seam (P3) | [app-auth-social.md](app-auth-social.md) | **Built** — LoginScreen (Google + email + MANDATORY phone step; Apple flag-hidden) · service seam · profile phone edit · iOS plist |
| Web consumer auth — Google + Apple + Email OTP (P2) | [web-auth-social.md](web-auth-social.md) | **Built** — LoginOptions (email-first; Google/Apple env-gated) + booking-confirm sign-in + MANDATORY contact phone + account phone edit |
| Brand & launch-asset integration (logo · loader · icons · splash) | [branding-integration.md](branding-integration.md) | **Approved** — phased (P1 web · P2 admin · P3 app assets+loader · P4 icon+splash · P5 flavors+pro) |
| Admin / ops console — backend (KYC · moderation · mgmt · disputes · analytics) | [admin-console.md](admin-console.md) | Slices 1–3 built |
| Admin / ops console — UI (Flutter Web) | [admin-console-ui.md](admin-console-ui.md) | Complete — dashboard · KYC · moderation · mgmt + support views · disputes · audit log (Journal) |
| Messaging & notifications (WhatsApp + SMS, Twilio) | [messaging-notifications.md](messaging-notifications.md) | Built (PR A foundation + OTP · PR B events + reminder scheduler); real BSP creds = ops |
| Termii SMS provider (cheap CI SMS) | [messaging-termii.md](messaging-termii.md) | Built — 2nd BSP behind the seam (~21× cheaper to CI than Twilio) + `MESSAGING_PROVIDER` selector; Termii creds = ops |
| Push notifications (FCM) — backend | [push-notifications-fcm.md](push-notifications-fcm.md) | Building (token registry + FCM v1 adapter + event wiring); app plugin + creds = ops |
| Push notifications (FCM) — app | [push-notifications-app.md](push-notifications-app.md) | Built (token-registration seam + permission UX on mocks; consumer + pro #2b); real firebase_messaging impl = accounts phase |
| Web — images + OG / brand | [web-images-og-brand.md](web-images-og-brand.md) | Built (next/image + CDN allowlist + OG image + favicon + logo.svg) |
| Provider before/after showcase (FR-DISC-006) | [provider-before-after.md](provider-before-after.md) | Complete — backend + pro editor + consumer drag-reveal slider |
| Discovery sort & filter (FR-DISC-007) | [discovery-sort-filter.md](discovery-sort-filter.md) | Complete — backend sort + available-today · app Trier sheet + toggle (à domicile = V2) |
| Web ↔ app parity audit (flow/user-story) | [web-parity-audit.md](web-parity-audit.md) | Recorded — most surfaces match; gaps G1 home discovery · G2 provider map/before-after/artistes · G3 pro Tableau de bord stats · G4 account rebook/avis/favoris; remediation M7.2→M7.3→M8 |
| Web surface — Next.js, SEO/AEO/GEO (FR-WEB-PP/MP) | [public-web.md](public-web.md) | Planned — OQ-8 resolved (Next.js for all web; shared API); milestone breakdown, awaiting sign-off |
| Auto-sync provider-entered bookings (FR-APPT-008) | [appointment-auto-sync.md](appointment-auto-sync.md) | Built — read-time match on the account's verified phone + "Réservé par votre salon" badge |
| Pro subscription — plan & trial (FR-PRO-SUB-001) | [pro-subscription.md](pro-subscription.md) | View built — derived trial (`GET /me/subscription`) + "Mon abonnement" screen; in-app billing deferred |
| Notification preferences (FR-NOTIF-004) | [notification-preferences.md](notification-preferences.md) | Complete — backend (prefs + send-path enforcement) · app preferences screen |
| Add appointment to calendar (FR-APPT-006) | [appointment-calendar.md](appointment-calendar.md) | Built — native add (add_2_calendar) from appointment detail; iOS plist set, Android `<queries>` TODO when scaffolded |
| In-app notification center (FR-NOTIF-002) | [notification-center.md](notification-center.md) | Complete — backend feed + write-on-events · app ApiNotificationService |
| Consumer deposit / Mobile Money flow | [consumer-deposit.md](consumer-deposit.md) | Built (B1 + B2) |
| Booking duration-overlap exclusion (btree_gist) | [booking-overlap-exclusion.md](booking-overlap-exclusion.md) | Built |
| Pro KYC (provider verification) | [pro-kyc.md](pro-kyc.md) | Built (provider side) |
| Pro staff (artists) management | [pro-artists.md](pro-artists.md) | Built |
| Consumer reviews | [consumer-reviews.md](consumer-reviews.md) | Built |
| Consumer favorites | [consumer-favorites.md](consumer-favorites.md) | Built |
| Pro deposit policy management | [pro-deposit-policy.md](pro-deposit-policy.md) | Built |
| `google_sign_in` v7 — the migration, and the cancel it would have broken | [app-google-signin-v7.md](app-google-signin-v7.md) | **Built (device check outstanding)** — v7 removed the unnamed constructor and `signIn()`, so the dependabot bump did not compile. The real work was not the compile fix: `authenticate()` THROWS on cancel where `signIn()` returned null, so a naive port ships a red « Connexion Google impossible. » every time a user closes the Google sheet. Verified from the plugins' native source that the backend receives an identical token from an identical audience and that no user is logged out. Adds a `GoogleIdTokenProvider` seam — before it, no test executed a single line of the Google paths. |
| Consumer avatar upload — a purpose of its own | [consumer-avatar-upload.md](consumer-avatar-upload.md) | **Built** — the profile-photo UI existed and had never worked in API mode, for two independent reasons: `AuthProvider` resolved the PRO upload instance (provider session + `purpose: gallery`), and the screen called the MOCK image picker unconditionally. Adds a consumer `avatar` purpose (public bucket, `avatar/{userId}`) rather than reusing `review` — the purpose string IS the storage namespace, and a profile photo filed under `review/` is both never erased today and swept by tomorrow's review-prefix cleanup. Closes the erasure gap (`DELETE /me` never deleted the avatar object) and makes the consumer web BFF REJECT an unknown purpose instead of coercing it to `deposit`. |
| Image upload pipeline (Cloudflare R2) | [pro-image-upload-pipeline.md](pro-image-upload-pipeline.md) | Built |
| Pro gallery photos (URL list) | [pro-gallery.md](pro-gallery.md) | Built |
| Pro-side reschedule | [pro-reschedule.md](pro-reschedule.md) | Built |
| Provider earnings detail | [provider-earnings.md](provider-earnings.md) | Built |
| Pro manual (walk-in/phone) booking | [pro-manual-booking.md](pro-manual-booking.md) | Built |
| Provider dashboard stats | [provider-dashboard-stats.md](provider-dashboard-stats.md) | Built |
| Pro catalogue app wiring (ApiProService → backend) | [pro-catalogue-app-wiring.md](pro-catalogue-app-wiring.md) | Built |
| Provider services & availability (backend) | [provider-services-availability-backend.md](provider-services-availability-backend.md) | Built |
| _Spec template_ | [TEMPLATE.md](TEMPLATE.md) | — |

> Add a row per spec as it's created. Keep newest at the top.
